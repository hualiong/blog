<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://hualiang.online</id>
    <title>Hualiang&apos;s Blog</title>
    <updated>2024-06-29T17:00:29.393Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://hualiang.online"/>
    <link rel="self" href="https://hualiang.online/atom.xml"/>
    <subtitle>&lt;i&gt;Unless I don&apos;t want to win, nobody can make me lose.&lt;/i&gt;&lt;br/&gt;&lt;br/&gt;I always believe 天道酬勤</subtitle>
    <logo>https://hualiang.online/images/avatar.png</logo>
    <icon>https://hualiang.online/favicon.ico</icon>
    <rights>All rights reserved 2024, Hualiang&apos;s Blog</rights>
    <entry>
        <title type="html"><![CDATA[如何用超星云盘做外链直链]]></title>
        <id>https://hualiang.online/post/ru-he-yong-chao-xing-yun-pan-zuo-wai-lian-zhi-lian/</id>
        <link href="https://hualiang.online/post/ru-he-yong-chao-xing-yun-pan-zuo-wai-lian-zhi-lian/">
        </link>
        <updated>2024-06-29T04:56:19.000Z</updated>
        <summary type="html"><![CDATA[<p>转自个人博客 “<a href="https://zxz.ee">小言u</a>” 的<a href="https://zxz.ee/100.html">《用超星云盘做外链直链》</a></p>
]]></summary>
        <content type="html"><![CDATA[<p>转自个人博客 “<a href="https://zxz.ee">小言u</a>” 的<a href="https://zxz.ee/100.html">《用超星云盘做外链直链》</a></p>
<!-- more -->
<h1 id="注意">注意</h1>
<p>仅是尝试阶段，具体什么时候会时效我也不知道，仅供参考使用，如果想要真正的投入到日常使用还是建议选择购买对象存储。</p>
<h1 id="图片方法步骤">图片方法步骤</h1>
<p>① 打开超星云盘网址：http://pan-yz.chaoxing.com/ 进行登入</p>
<p>② 随便上传一个图片 -&gt; 双击预览 -&gt; 右键 -&gt; 在新标签页中打开 -&gt; 复制新标签页中的图片网址</p>
<figure data-type="image" tabindex="1"><a href="https://p.itxe.net/images/2022/11/14/cx1.gif"><img src="https://p.itxe.net/images/2022/11/14/cx1.gif" alt="img" loading="lazy"></a></figure>
<p>复制出来的网址：<code>https://imageproxy.chaoxing.com/0x0,q15,jpeg,soE2Z31QoUXrtu-Pp15uwU6Lyr-Jk4wc01pXMqFqLG_I/http://p.ananas.chaoxing.com/star3/origin/093846f84d5608bb6d995a8828f4eb8b.png</code></p>
<p>你会发现图片被压缩了，接下来进行真实图片直链提取。</p>
<p>在复制出来的链接尾部里找到类似于 <code>https://p.ananas.chaoxing.com/XXXXXXXX</code> 上面的链接里的就是：<code>https://p.ananas.chaoxing.com/star3/origin/093846f84d5608bb6d995a8828f4eb8b.png</code></p>
<p>这个链接就是图片直链了，而且是没有被压缩过的原图</p>
<h1 id="视频方法步骤">视频方法步骤</h1>
<p>上传那些操作我就不阐述了，直接开始提取步骤。</p>
<p>点击视频文件进行预览，然后按F12审查元素 -&gt; 捕捉出视频的真实链接</p>
<figure data-type="image" tabindex="2"><a href="https://p.itxe.net/images/2022/11/14/cx2.gif"><img src="https://p.itxe.net/images/2022/11/14/cx2.gif" alt="img" loading="lazy"></a></figure>
<p>复制出来的视频直链网址：<code>https://s1.ananas.chaoxing.com/video/51/6e/5d/c13198119fd05ddc7c7966b6c20b7af7/sd.mp4?at_=1605929018942&amp;ak_=d8b1f503f6ba01b1f60995a2e38471f9&amp;ad_=e794ddb198db30558298ddbc8c564b2a</code></p>
<p>然后删除这条链接里的没用信息：<code>sd.mp4</code> 后面的，并将前面的 <code>s1.ananas</code> 改为 <code>s138.ananas</code>。</p>
<p>删除无用信息后的链接：<code>https://s138.ananas.chaoxing.com/video/51/6e/5d/c13198119fd05ddc7c7966b6c20b7af7/sd.mp4</code></p>
<h1 id="文件方法步骤">文件方法步骤</h1>
<p>txt、zip、7z等格式无法直接在浏览器预览的文件直链提取方法</p>
<p>需使用IDM配合提取，将IDM捕捉文件类型中加入自己想要提取的文件格式后缀，并点击下载，就会弹出下载提示框</p>
<figure data-type="image" tabindex="3"><a href="https://p.itxe.net/images/2022/11/14/cx3.png"><img src="https://p.itxe.net/images/2022/11/14/cx3.png" alt="img" loading="lazy"></a></figure>
<figure data-type="image" tabindex="4"><a href="https://p.itxe.net/images/2022/11/14/cx4.gif"><img src="https://p.itxe.net/images/2022/11/14/cx4.gif" alt="img" loading="lazy"></a></figure>
<p>复制出来的链接：<code>https://d0.ananas.chaoxing.com/download/270d7c5a4776acf040a285208c52934f?fn=Test</code>，<code>?fn=</code> 后面的是文件名</p>
<h1 id="提取的直链整合">提取的直链整合</h1>
<p>图片：<code>https://p.ananas.chaoxing.com/star3/origin/093846f84d5608bb6d995a8828f4eb8b.png</code></p>
<p>视频：<code>https://s138.ananas.chaoxing.com/video/51/6e/5d/c13198119fd05ddc7c7966b6c20b7af7/sd.mp4</code></p>
<p>文件：<code>https://d0.ananas.chaoxing.com/download/270d7c5a4776acf040a285208c52934f?fn=Test</code></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[2024 年如何绕过 Cloudflare]]></title>
        <id>https://hualiang.online/post/2023-nian-ru-he-rao-guo-cloudflare/</id>
        <link href="https://hualiang.online/post/2023-nian-ru-he-rao-guo-cloudflare/">
        </link>
        <updated>2024-06-21T08:20:35.000Z</updated>
        <summary type="html"><![CDATA[<p>转自 <a href="https://scrapeops.io">ScrapeOps</a> 官网的<a href="https://scrapeops.io/web-scraping-playbook/how-to-bypass-cloudflare/#option-1-send-requests-to-origin-server">《How To Bypass Cloudflare in 2024》</a>的机翻</p>
]]></summary>
        <content type="html"><![CDATA[<p>转自 <a href="https://scrapeops.io">ScrapeOps</a> 官网的<a href="https://scrapeops.io/web-scraping-playbook/how-to-bypass-cloudflare/#option-1-send-requests-to-origin-server">《How To Bypass Cloudflare in 2024》</a>的机翻</p>
<!-- more -->
<p>据估计，有 40% 的网站使用<a href="https://www.cloudflare.com/en-gb/">Cloudflares</a>内容分发网络 (CDN)，对于希望抓取互联网上一些最受欢迎网站的开发人员来说，绕过 Cloudflare 的反机器人保护系统已成为一项重要要求。</p>
<p>幸运的是，我们可以绕过 Cloudflares 反机器人保护。然而，这并不是一件容易的事。</p>
<p>您可以采取多种方法来绕过 Cloudflare，每种方法都有各自的优点和缺点。</p>
<p>它们的范围从简单的（例如使用现成的工具）到极其复杂的（例如 Cloudflare 如何检测和阻止抓取工具的完全逆向工程）。</p>
<p>因此，在本指南中，我们将逐一介绍这些选项，以便您可以选择最适合您的选项。</p>
<ol>
<li>
<p>向源服务器发送请求</p>
</li>
<li>
<p>抓取 Google 缓存版本</p>
</li>
<li>
<p>Cloudflare 求解器</p>
</li>
<li>
<p>使用强化的无头浏览器进行抓取</p>
</li>
<li>
<p>具有 Cloudflare 内置旁路的智能代理</p>
</li>
<li>
<p>逆向工程 Cloudflare 反机器人保护</p>
</li>
</ol>
<hr>
<figure data-type="image" tabindex="1"><img src="https://res.cloudinary.com/dyaskan9k/image/fetch/f_auto,q_auto/https://assets-scrapeops.nyc3.digitaloceanspaces.com/Images/Playbooks/Web-Scraping-Playbook/cloudflare/bypass-cloudflare-heading-option-1-origin-server.jpg" alt="如何绕过 Cloudflare - 选项 #1：向原始服务器发送请求" loading="lazy"></figure>
<h2 id="一-向原始服务器">一、向原始服务器</h2>
<p>这并不总是可行，但绕过 Cloudflare 的最简单方法之一是将请求直接发送到网站源服务器 IP 地址，而不是发送到 Cloudflare 的 CDN 网络。</p>
<p>在这里，您不必欺骗 Cloudflare 认为您的请求来自真实用户，而是通过查找托管网站的原始服务器的 IP 地址并将您的请求发送到该服务器来完全绕过 Cloudflare。</p>
<p>完全绕过 Cloudflare 及其所有保护！</p>
<figure data-type="image" tabindex="2"><img src="https://res.cloudinary.com/dyaskan9k/image/fetch/f_auto,q_auto/https://assets-scrapeops.nyc3.digitaloceanspaces.com/Images/Playbooks/Web-Scraping-Playbook/cloudflare/how-to-bypass-cloudflare-origin-server.png" alt="如何绕过 Cloudflare - 通过原始服务器" loading="lazy"></figure>
<p>Cloudflare 是一个复杂的反机器人保护系统，但它是由以下人员设置的：</p>
<ol>
<li>可能不完全理解 Cloudflare，</li>
<li>可能会偷工减料，或者</li>
<li>在 Cloudflare 上设置网站时会犯错误。</li>
</ol>
<p>因此，有时稍微窥探一下，您就可以找到托管网站主版本的服务器的 IP 地址。</p>
<p>找到此 IP 地址后，您可以将抓取工具配置为将请求发送到此服务器，而不是已启用反机器人保护的 Cloudflares 服务器。</p>
<p>例如，受 Cloudflare 保护的站点<a href="https://www.petsathome.com/">PetsAtHome.com</a>的原始 IP 地址可公开访问：</p>
<pre><code>Origin IP Address --&gt; 'http://88.211.26.45/'
</code></pre>
<p>有时，通过将源 IP 地址插入浏览器地址栏中来访问网站将不起作用，因为服务器可能需要 HTTP 标头<code>HOST</code>。在这种情况下，您可以使用curl或Postman等工具查询源服务器，它允许您设置<code>HOST</code>标头或向主机文件添加静态映射。</p>
<h3 id="查找源服务器">查找源服务器</h3>
<p>有多种方法可以查找网站服务器的原始 IP 地址。以下是最常用的 3 种方法：</p>
<h4 id="方法-1ssl证书">方法 1：SSL证书</h4>
<p>如果目标网站使用 SSL 证书（大多数网站都是如此），那么这些 SSL 证书将在 <a href="https://search.censys.io/">Censys</a> 数据库中注册。</p>
<p>尽管网站已将其网站部署到 Cloudflare CDN，但有时其当前或旧的 SSL 证书会注册到原始服务器。</p>
<p>您可以在 Censys 数据库中查找该网站，看看这些服务器中是否有任何服务器托管原始网站。</p>
<h4 id="方法2其他服务">方法2：其他服务</h4>
<p>有时，其他子域、邮件交换器 (MX) 服务器、FTP/SCP 服务或主机名托管在与主网站相同的服务器上，但不受 Cloudflare 网络的保护。</p>
<p>在这里您可以使用 <a href="https://search.censys.io/">Censys</a> 数据库或 <a href="https://shodan.io/">Shodan</a> 检查其他子域的 DNS 记录或 A、AAAA、CNAME 和 MX DNS 记录，以缓解主服务器的 IP 地址。</p>
<p>如果该网站没有使用第三部分电子邮件提供商，一个技巧是将电子邮件发送到目标网站上不存在的电子邮件地址，并且假设发送失败，您应该收到来自电子邮件服务器的通知，其中<code>fakeemail@targetwebsite.com</code>包含IP 地址。</p>
<h4 id="方法-3旧-dns记录">方法 3：旧 DNS记录</h4>
<p>每个服务器的 DNS 历史记录都可以在互联网上获取，因此有时网站仍托管在将其部署到 Cloudflare CDN 之前的同一台服务器上。因此，您可以使用 <a href="https://github.com/zidansec/CloudPeler">CrimeFlare</a>等工具来查找它。</p>
<p>CrimeFlare 维护 Cloudflare 上托管的网站的可能源服务器数据库，该数据库源自当前和旧的 DNS 记录。</p>
<p>以下是一些可帮助您查找服务器原始 IP 地址的最佳工具：</p>
<ul>
<li><a href="https://github.com/christophetd/CloudFlair">CloudFlair</a></li>
<li><a href="https://github.com/zidansec/CloudPeler">CrimeFlare</a></li>
<li><a href="https://search.censys.io/">Censys</a></li>
<li><a href="https://shodan.io/">Shodan</a></li>
<li><a href="https://github.com/vincentcox/bypass-firewalls-by-DNS-history">Bypass-firewalls-by-DNS-history</a></li>
</ul>
<p>有时，即使您找到网站服务器的实际 IP 地址，也无法访问它，例如当网站管理员正确限制服务器仅响应 Cloudflare IP 范围、将任何请求重定向到 Cloudflare CDN 时，或者如果 Origin <a href="https://developers.cloudflare.com/ssl/origin-configuration/origin-ca/">CA</a>使用<a href="https://developers.cloudflare.com/ssl/origin-configuration/origin-ca/">证书</a></p>
<p><strong>登台和开发服务器</strong></p>
<p>如果找到看起来像原始服务器的东西，它实际上可能是真实网站的开发或临时服务器。虽然你永远不能 100% 确定你找到的服务器是原始服务器，但如果你可以浏览一下，数据看起来与 Cloudflare 受保护站点相同，可以在“原始版本”上注册帐户并登录真实的服务器有了它的网站，那么就应该可以将该网站视为真正的网站。</p>
<p>有关查找源服务器 IP 地址的更多信息，请查看这些指南：</p>
<ul>
<li><a href="https://blog.detectify.com/2019/07/31/bypassing-cloudflare-waf-with-the-origin-server-ip-address/">使用源服务器 IP 地址绕过 Cloudflare WAF</a></li>
<li><a href="https://rhinosecuritylabs.com/cloud-security/cloudflare-bypassing-cloud-security/">CFire 简介：规避 CloudFlare 安全保护</a></li>
<li><a href="https://blog.christophetd.fr/bypassing-cloudflare-using-internet-wide-scan-data/">CloudFlair：使用互联网范围内的扫描数据绕过 Cloudflare</a></li>
</ul>
<p>如果完成上述操作后您仍找不到源服务器的 IP 地址，请不要担心。还有很多其他方法可以绕过 Cloudflare 保护。</p>
<hr>
<figure data-type="image" tabindex="3"><img src="https://res.cloudinary.com/dyaskan9k/image/fetch/f_auto,q_auto/https://assets-scrapeops.nyc3.digitaloceanspaces.com/Images/Playbooks/Web-Scraping-Playbook/cloudflare/bypass-cloudflare-heading-option-2-scrape-google-cache.jpg" alt="如何绕过 Cloudflare - 选项 #2：抓取 Google 缓存版本" loading="lazy"></figure>
<h2 id="二-抓取-google-缓存版本">二、抓取 Google 缓存版本</h2>
<p>根据数据需要的新鲜程度，另一种选择是从 Google 缓存而不是实际网站中抓取数据。</p>
<p>当 Google 抓取网络以索引网页时，它会为其找到的数据创建缓存。大多数受 Cloudflare 保护的网站都允许 Google 抓取其网站，因此您可以刮取此缓存。</p>
<p>抓取 Google 缓存可能比抓取 Cloudflare 保护的网站更容易，但只有当您要抓取的网站上的数据不经常更改时，这才是一个可行的选择。</p>
<p>要抓取 Google 缓存，只需添加<code>https://webcache.googleusercontent.com/search?q=cache:</code>到您要抓取的 URL 的开头即可。</p>
<p>例如，如果您想抓取<code>https://www.petsathome.com/shop/en/pets/dog</code>，那么抓取 Google 缓存版本的 URL 将为：</p>
<pre><code>'https://webcache.googleusercontent.com/search?q=cache:https://www.petsathome.com/shop/en/pets/dog'
</code></pre>
<p><strong>未缓存的网站</strong></p>
<p>某些网站（例如 LinkedIn）告诉 Google 不要缓存其网页，或者 Google 的抓取频率太低，这意味着某些页面可能尚未缓存。所以这个方法并不适用于每个网站。</p>
<hr>
<figure data-type="image" tabindex="4"><img src="https://res.cloudinary.com/dyaskan9k/image/fetch/f_auto,q_auto/https://assets-scrapeops.nyc3.digitaloceanspaces.com/Images/Playbooks/Web-Scraping-Playbook/cloudflare/bypass-cloudflare-heading-option-3-cloudflare-solvers.jpg" alt="如何绕过 Cloudflare - 选项 #3：Cloudflare Solver" loading="lazy"></figure>
<h2 id="三-cloudflare求解器">三、Cloudflare求解器</h2>
<p>好吧，如果您找不到源服务器并且无法使用 Google 缓存，那么我们需要直接绕过 Cloudflare。</p>
<p>绕过 Cloudflare 的一种方法是使用多种 Cloudflare 求解器之一来解决 Cloudflare 挑战：</p>
<figure data-type="image" tabindex="5"><img src="https://assets-scrapeops.nyc3.digitaloceanspaces.com/Images/Playbooks/Web-Scraping-Playbook/cloudflare/how-to-bypass-cloudflare-challenge-page.png" alt="如何绕过 Cloudflare - 挑战页面" loading="lazy"></figure>
<p>已经开发了许多 Cloudflare 求解器：</p>
<ul>
<li><a href="https://github.com/VeNoMouS/cloudscraper">cloudscraper</a> <a href="https://scrapeops.io/python-web-scraping-playbook/python-cloudscraper/">Guide here</a></li>
<li><a href="https://github.com/Anorov/cloudflare-scrape">cloudflare-scrape</a></li>
<li><a href="https://github.com/RyuzakiH/CloudflareSolverRe">CloudflareSolverRe</a></li>
<li><a href="https://github.com/ninja-beans/cloudflare-iuam-solver">Cloudflare-IUAM-Solver</a></li>
<li><a href="https://github.com/devgianlu/cloudflare-bypass">cloudflare-bypass</a> [Archived]</li>
<li><a href="https://github.com/RyuzakiH/CloudflareSolverRe">CloudflareSolverRe</a></li>
</ul>
<p>然而，它们经常会因 Cloudflare 更新而过时并停止工作。</p>
<p>目前，性能最好的 Cloudflare 求解器是 <a href="https://github.com/FlareSolverr/FlareSolverr">FlareSolverr</a>。</p>
<h3 id="flaresolverr">FlareSolverr</h3>
<p><a href="https://github.com/FlareSolverr/FlareSolverr">FlareSolverr</a> 是一个代理服务器，可用于绕过 Cloudflare 和 DDoS-GUARD 保护。</p>
<p>运行时，<strong>FlareSolverr会启动一个代理服务器，该服务器使用</strong><a href="https://github.com/puppeteer/puppeteer">puppeteer</a>和<a href="https://github.com/berstend/puppeteer-extra/tree/master/packages/puppeteer-extra-plugin-stealth">Stealth Plugin</a>将您的请求转发到 Cloudflare 受保护的网站，并等待 Cloudflare 挑战解决（或超时），然后将响应和 Cookie 返回到您的抓取工具。</p>
<p>从这里，您可以使用这些 cookie 使用普通的 HTTP 客户端绕过 Cloudflare。</p>
<p>与针对每个请求使用强化的无头浏览器相比，这种方法的优点在于，您只需要使用 FlareSolverr 检索有效的 Cloudflare cookie，然后可以使用资源密集程度低得多的 HTTP 客户端（例如 Python 请求、HTTPX、Node Axios 等）继续进行抓取.)。</p>
<p>您可以使用 Docker（已包含 Firefox 浏览器）在服务器上安装 FlareSolverr，因此设置非常简单。</p>
<pre><code class="language-shell">docker run -d \
  --name=flaresolverr \
  -p 8191:8191 \
  -e LOG_LEVEL=info \
  --restart unless-stopped \
  ghcr.io/flaresolverr/flaresolverr:latest
</code></pre>
<p>运行时，<strong>FlareSolverr</strong>会启动一个服务器，该服务器使用 <a href="https://www.selenium.dev/">Python Selenium</a>和未检测到的 <a href="https://github.com/ultrafunkamsterdam/undetected-chromedriver">chromedriver</a>，通过模拟真实的 Web 浏览器来解决 Cloudflares Javascript 和浏览器指纹识别挑战。</p>
<p>FlareSolverr 使用 Selenium 浏览器打开目标 URL，并等待 Cloudflare 挑战得到解决，然后将 HTML 和 cookies Cloudflare 返回到浏览器。</p>
<p>由于 FlareSolverr在幕后使用 Selenium未检测到的 <a href="https://github.com/ultrafunkamsterdam/undetected-chromedriver">chromedriver来绕过 Cloudflare，因此在某些情况下也可用于绕过 DataDome（可能需要修改）。</a></p>
<p>要使用 FlareSolverr，您需要配置抓取器以将要抓取的 URL 发送到 FlareSolverr 服务器：</p>
<pre><code class="language-python">import requests

post_body = {
  &quot;cmd&quot;: &quot;request.get&quot;,
  &quot;url&quot;:&quot;https://cloudflare.com/&quot;,
  &quot;maxTimeout&quot;: 60000
}

response = requests.post('http://localhost:8191/v1', headers={'Content-Type': 'application/json'}, json=post_body)

print(response.json())
</code></pre>
<p>然后它将使用 cookie 和 HTML 响应进行响应：</p>
<pre><code class="language-json">{
    &quot;status&quot;: &quot;ok&quot;,
    &quot;message&quot;: &quot;Challenge not detected!&quot;,
    &quot;solution&quot;: {
        &quot;url&quot;: &quot;https://cloudflare.com/&quot;,
        &quot;status&quot;: 200,
        &quot;cookies&quot;: [
            {
                &quot;domain&quot;: &quot;.cloudflare.com&quot;,
                &quot;expiry&quot;: 1705160731,
                &quot;httpOnly&quot;: false,
                &quot;name&quot;: &quot;datadome&quot;,
                &quot;path&quot;: &quot;/&quot;,
                &quot;sameSite&quot;: &quot;Lax&quot;,
                &quot;secure&quot;: true,
                &quot;value&quot;: &quot;5H6S1eVa4qoqPbzbQxo4fGjFNdeY7ZUE40Qlk0ZQTiLk5b8aqv4nYNE6-JC1MQtUs4k4lBXf-ScmiijLOk1QlolRRVVlUTtc1i_maPBzFSz4AJVtM~_iWqJGNPZpbJge&quot;
            }
			...
        ],
        &quot;userAgent&quot;: &quot;Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/108.0.0.0 Safari/537.36&quot;,
        &quot;headers&quot;: {},
        &quot;response&quot;: &quot;&lt;html&gt;&lt;head&gt;...&lt;/head&gt;&lt;body&gt;...&lt;/body&gt;&lt;/html&gt;&quot;
    },
    &quot;startTimestamp&quot;: 1673459546891,
    &quot;endTimestamp&quot;: 1673459560345,
    &quot;version&quot;: &quot;3.0.2&quot;
}
</code></pre>
<p>使用 FlareSolverr 绕过 Cloudflare 并不可靠，因为它无法检测 Cloudflare 挑战和禁令，因此您需要自己验证响应。</p>
<p><strong>内存问题</strong></p>
<p>由于无头浏览器可能会消耗大量内存，并且对 FlareSolverr 的每个请求都会启动一个新的浏览器窗口，因此如果您向 FlareSolverr 发送许多请求并且您的计算机没有足够的 RAM，FlareSolverr 可能会导致您的服务器崩溃。因此，您需要限制发送的请求数量和/或将其部署在更大的服务器上。</p>
<p>有时，CloudFlare 不仅提供数学计算和需要解决的 Javascript 浏览器测试，有时还要求用户解决验证码。尽管 FlareSolverr 确实支持通过第三方验证码求解器进行验证码求解，但目前，没有任何自动化验证码求解解决方案能够像 Cloudflare 使用 hCAPTCHA 那样工作。</p>
<hr>
<figure data-type="image" tabindex="6"><img src="https://res.cloudinary.com/dyaskan9k/image/fetch/f_auto,q_auto/https://assets-scrapeops.nyc3.digitaloceanspaces.com/Images/Playbooks/Web-Scraping-Playbook/cloudflare/bypass-cloudflare-heading-option-4-fortified-headless-browsers.jpg" alt="如何绕过 Cloudflare - 选项 #4：使用强化的无头浏览器进行抓取" loading="lazy"></figure>
<h2 id="四-使用强化的无头浏览器">四、使用强化的无头浏览器</h2>
<p>另一种选择是使用无头浏览器完成整个抓取工作，该浏览器经过强化，看起来像真正的用户浏览器。</p>
<p>Vanilla 无头浏览器会在 JS 指纹中泄露其身份，反机器人系统可以轻松检测到。然而，开发人员已经发布了许多强化的无头浏览器来修补最大的漏洞：</p>
<ul>
<li><strong>Puppeteer：</strong><a href="https://github.com/puppeteer/puppeteer">puppeteer</a>的<a href="https://github.com/berstend/puppeteer-extra/tree/master/packages/puppeteer-extra-plugin-stealth">隐形</a>插件。</li>
<li><strong>Playwright：</strong> Playwright 即将推出<a href="https://www.npmjs.com/package/playwright-stealth">隐形插件</a>。关注<a href="https://github.com/berstend/puppeteer-extra/issues/454">这里</a>和<a href="https://github.com/berstend/puppeteer-extra/tree/master/packages/playwright-extra">这里的</a>事态发展。</li>
<li><strong>Selenium：</strong><a href="https://github.com/ultrafunkamsterdam/undetected-chromedriver">未检测到的 chromedriver</a>是一个优化的 Selenium Chromedriver 补丁。</li>
</ul>
<p>例如，Puppeteer、Playwright 和 Selenium 等无头浏览器中存在的众所周知的泄漏是<code>navigator.webdriver</code>. 在普通浏览器中，该值设置为<code>false</code>，但是，在<strong>未经强化的</strong>无头浏览器中，它设置为<code>true</code>。</p>
<figure data-type="image" tabindex="7"><img src="https://res.cloudinary.com/dyaskan9k/image/fetch/f_auto,q_auto/https://assets-scrapeops.nyc3.digitaloceanspaces.com/Images/Playbooks/Web-Scraping-Playbook/cloudflare/how-to-bypass-cloudflare-browser-api-checks.jpg" alt="无头浏览器 navigator.webdriver 泄露" loading="lazy"></figure>
<p>这些隐形插件试图修补 200 多个已知的无头浏览器漏洞。然而，由于浏览器不断变化，人们相信这个数字要高得多，并且不透露他们所知道的所有泄漏符合浏览器开发人员和反机器人公司的利益。</p>
<p>无头浏览器隐形插件修补了大部分浏览器漏洞，并且通常可以绕过许多反机器人服务，例如<a href="https://www.cloudflare.com/en-gb/products/bot-management/">Cloudflare</a>、<a href="https://www.perimeterx.com/">PerimeterX</a>、<a href="https://www.imperva.com/">Incapsula</a>、<a href="https://cloudflare.com/">DataDome</a>，具体取决于它们在网站上实施的安全级别。</p>
<p>然而，他们并没有得到全部。要真正使您的无头浏览器看起来像真正的浏览器，那么您必须自己完成此操作。</p>
<p>让无头浏览器更不易被检测到的另一种方法是将它们与高质量的住宅或移动代理配对。这些代理通常比数据中心代理具有更高的 IP 地址信誉评分，并且反机器人服务更不愿意阻止它们，从而使它们更加可靠。</p>
<p>将无头浏览器与住宅/移动代理配对的缺点是成本会快速增加。</p>
<p>由于住宅和移动代理通常按使用的 GB 带宽收费，并且使用无头浏览器呈现的页面平均可消耗 2MB（而没有无头浏览器则为 250kb）。这意味着随着规模的扩大，它可能会变得非常昂贵。</p>
<p>以下是使用来自<a href="https://brightdata.grsm.io/residential9584">BrightData</a>的住宅代理和无头浏览器的示例，假设每页 2MB。</p>
<table>
<thead>
<tr>
<th style="text-align:left">页数</th>
<th style="text-align:left">带宽</th>
<th style="text-align:left">每 GB 成本</th>
<th style="text-align:left">总成本</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">25,000</td>
<td style="text-align:left">50GB</td>
<td style="text-align:left">13 美元</td>
<td style="text-align:left">$625</td>
</tr>
<tr>
<td style="text-align:left">100,000</td>
<td style="text-align:left">200GB</td>
<td style="text-align:left">10 美元</td>
<td style="text-align:left">2000 美元</td>
</tr>
<tr>
<td style="text-align:left">百万</td>
<td style="text-align:left">2TB</td>
<td style="text-align:left">8 美元</td>
<td style="text-align:left">16,000 美元</td>
</tr>
</tbody>
</table>
<p><strong>寻找廉价的住宅和移动代理:</strong></p>
<p>如果您想比较代理提供商，您可以使用这个<a href="https://scrapeops.io/proxy-providers/comparison/">免费的代理比较工具</a>，它可以比较<a href="https://scrapeops.io/proxy-providers/comparison/best-residential-proxies">住宅代理计划</a>和<a href="https://scrapeops.io/proxy-providers/comparison/best-mobile-proxies">移动代理计划</a>。</p>
<h3 id="示例selenium-未检测到的-chromedriver">示例：Selenium 未检测到的 ChromeDriver</h3>
<p>例如，以下是如何使用 Selenium 的<a href="https://github.com/ultrafunkamsterdam/undetected-chromedriver">unDetected-chromedriver</a>来抓取受 Cloudflare 保护的网站。</p>
<p>首先，您只需通过 pip安装<a href="https://pypi.org/project/undetected-chromedriver/2.1.1/">unDetected-chromedriver包：</a></p>
<pre><code class="language-shell">pip install undetected-chromedriver
</code></pre>
<p>现在安装了**未检测到的 chromedriver，**我们可以设置我们的 scraper/bot 来使用它而不是默认的 Chromedriver。</p>
<pre><code class="language-python">import undetected_chromedriver as uc

driver = uc.Chrome()
driver.get('https://cloudflare.com/')
</code></pre>
<p>为了启用经过身份验证的代理，在下面的示例中，我们将从<a href="https://pypi.org/project/undetected-chromedriver/2.1.1/">unDetected-chromedriver</a><code>undetected_chromedriver</code>包加载<code>seleniumwire</code>而不是直接从unDetected-chromedriver 包加载，并将代理设置传递到Chromedriver 的属性中。<code>seleniumwire_options</code></p>
<pre><code class="language-python">import seleniumwire.undetected_chromedriver as uc

## Chrome Options
chrome_options = uc.ChromeOptions()

## Proxy Options
proxy_options = {
    'proxy': {
        'http': 'http://user:pass@ip:port',
        'https': 'https://user:pass@ip:port',
        'no_proxy': 'localhost,127.0.0.1'
    }
}

## Create Chrome Driver
driver = uc.Chrome(
    options=chrome_options,
    seleniumwire_options=proxy_options
)

driver.get('https://cloudflare.com/')
</code></pre>
<p>标准<a href="https://chromedriver.chromium.org/downloads">Selenium ChromeDriver</a>泄露了大量信息，反机器人系统可以使用这些信息来确定它是自动浏览器/抓取工具还是访问网站的真实用户。</p>
<p>Selenium <a href="https://github.com/ultrafunkamsterdam/undetected-chromedriver">UnDetected ChromeDriver</a>通过修补反机器人系统用于检测 Selenium bot/scraper 的绝大多数方式来增强标准 Selenium ChromeDriver。</p>
<p>使 DataDome、Imperva、Perimeterx、Botprotect.io 和 Cloudflare 等反机器人系统更难检测和阻止您的 Selenium 机器人/抓取工具。</p>
<p>有关如何使用 Selenium UnDetected ChromeDriver 的更多信息，请<a href="https://scrapeops.io/selenium-web-scraping-playbook/python-selenium-undetected-chromedriver/">在此处查看我们的指南</a>。</p>
<hr>
<figure data-type="image" tabindex="8"><img src="https://res.cloudinary.com/dyaskan9k/image/fetch/f_auto,q_auto/https://assets-scrapeops.nyc3.digitaloceanspaces.com/Images/Playbooks/Web-Scraping-Playbook/cloudflare/bypass-cloudflare-heading-option-5-smart-proxy.jpg" alt="如何绕过 Cloudflare - 选项 #5：具有 Cloudflare 内置绕过功能的智能代理" loading="lazy"></figure>
<h2 id="五-具有-cloudflare-内置旁路的智能代理">五、具有 Cloudflare 内置旁路的智能代理</h2>
<p>使用开源 <a href="https://scrapeops.io/web-scraping-playbook/how-to-bypass-cloudflare/#option-3-cloudflare-solvers">Cloudflare 求解器</a>和<a href="https://scrapeops.io/web-scraping-playbook/how-to-bypass-cloudflare/#option-4-scrape-with-fortified-headless-browsers">预强化无头浏览器</a>的缺点是，像 Cloudflare 这样的反机器人公司可以看到他们如何绕过他们的反机器人保护系统并轻松修补他们利用的问题。</p>
<p>因此，大多数开源 Cloudflare 绕过在停止工作之前只有几个月的保质期。</p>
<p>使用开源 Cloudflare 旁路的替代方案是使用智能代理来开发和维护自己的私有 Cloudflare 旁路。</p>
<p>这些通常更可靠，因为 Cloudflare 更难为它们开发补丁，而且它们是由代理公司开发的，这些公司有经济动机保持领先 Cloudflare 1 步，并在它们停止工作的那一刻修复它们的旁路。</p>
<p>大多数智能代理提供商（<a href="https://www.scraperapi.com/?fp_ref=scrapeops">ScraperAPI</a>、<a href="https://www.scrapingbee.com/?fpr=scrapeops">Scrapingbee</a>、<a href="https://oxylabs.go2cloud.org/aff_c?offer_id=7&amp;aff_id=379&amp;url_id=32">Oxylabs</a>、<a href="https://prf.hn/click/camref:1100loxdG/%5Bp_id:1100l442001%5D/destination:https%3A%2F%2Fsmartproxy.com%2Fscraping%2Fweb">Smartproxy</a>）都有某种形式的 Cloudflare 绕过，这些绕过在不同程度上起作用，成本也各不相同。</p>
<p>但是，最好的选择之一是使用 <a href="https://scrapeops.io/proxy-aggregator/">ScrapeOps 代理聚合器</a>，因为它将 20 多个代理提供商集成到同一个代理 API 中，并为您的目标域找到最佳/最便宜的代理提供商。</p>
<p>您只需将 API 请求添加到即可激活 <a href="https://scrapeops.io/docs/proxy-aggregator/advanced-functionality/anti-bot-bypass/">ScrapeOps 的 Cloudflare</a> Bypass，ScrapeOps 代理将使用适用于您的目标域的最佳和最便宜的 Cloudflare 绕过。<code>bypass=cloudflare_level_1</code></p>
<pre><code class="language-python">import requests

response = requests.get(
  url='https://proxy.scrapeops.io/v1/',
  params={
      'api_key': 'YOUR_API_KEY',
      'url': 'http://example.com/', ## Cloudflare protected website 
      'bypass': 'cloudflare_level_1',
  },
)

print('Body: ', response.content)
</code></pre>
<p><strong>提示：</strong></p>
<p><a href="https://www.cloudflare.com/en-gb/products/bot-management/">Cloudflare</a> 是当今网站最常用的反机器人系统，绕过它取决于网站启用了哪些安全设置。</p>
<p>为了解决这个问题，我们提供了 3 种不同的 Cloudflare 绕过方法，旨在解决每个安全级别的 Cloudflare 挑战。</p>
<table>
<thead>
<tr>
<th>安全级别</th>
<th>旁路</th>
<th>API 积分</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>低</strong></td>
<td><code>cloudflare_level_1</code></td>
<td><strong>10</strong></td>
<td>用于绕过启用低安全设置的<a href="https://www.cloudflare.com/en-gb/products/bot-management/">受 Cloudflare</a> 保护的站点。</td>
</tr>
<tr>
<td><strong>中等</strong></td>
<td><code>cloudflare_level_2</code></td>
<td><strong>35</strong></td>
<td>用于绕过启用了中等安全设置的<a href="https://www.cloudflare.com/en-gb/products/bot-management/">受 Cloudflare</a> 保护的站点。在大型计划中，信用倍数将增加，以保持每千个请求 3.50 美元的统一费率。</td>
</tr>
<tr>
<td><strong>高</strong></td>
<td><code>cloudflare_level_3</code></td>
<td><strong>50</strong></td>
<td>用于绕过启用了高安全设置的<a href="https://www.cloudflare.com/en-gb/products/bot-management/">受 Cloudflare</a> 保护的站点。在大型计划中，信用倍数将增加，以保持每千个请求 4 美元的统一费率。</td>
</tr>
</tbody>
</table>
<p>您可以<a href="https://scrapeops.io/app/register/proxy">在此处</a>注册，获得具有 <strong>1,000 个免费 API 积分</strong>的 <strong>ScrapeOps API 密钥</strong>。</p>
<p>采用这种方法的好处是，您可以使用普通的 HTTP 客户端，而不必担心：</p>
<ul>
<li>查找源服务器</li>
<li>强化无头浏览器</li>
<li>管理大量无头浏览器实例并处理内存问题</li>
<li>对 Cloudflare 反机器人保护进行逆向工程</li>
</ul>
<p>因为这一切都在 <a href="https://scrapeops.io/proxy-aggregator/">ScrapeOps 代理聚合器</a>中管理</p>
<hr>
<figure data-type="image" tabindex="9"><img src="https://res.cloudinary.com/dyaskan9k/image/fetch/f_auto,q_auto/https://assets-scrapeops.nyc3.digitaloceanspaces.com/Images/Playbooks/Web-Scraping-Playbook/cloudflare/bypass-cloudflare-heading-option-6-cloudflare.jpg" alt="如何绕过 Cloudflare - 选项 #6：逆向工程 Cloudflare 反机器人保护" loading="lazy"></figure>
<h2 id="六-逆向工程-cloudflare-反机器人保护">六、逆向工程 Cloudflare 反机器人保护</h2>
<p>绕过 Cloudflare 反机器人保护的最后也是最复杂的方法是对 Cloudflare 的反机器人保护系统进行实际逆向工程，并开发一种绕过所有 Cloudflare 反机器人检查的方法，而无需使用完全强化的无头浏览器实例。</p>
<p>这种方法有效（并且是许多智能代理解决方案所做的），但是，它不适合胆小的人。</p>
<p>**优点：**这种方法的优点是，如果您进行大规模抓取，并且不想运行数百个（如果不是数千个）昂贵的完整无头浏览器实例。相反，您可以开发资源效率最高的 Cloudflare 旁路。一款专门为通过 Cloudflare JS、TLS 和 IP 指纹测试而设计的。</p>
<p>**缺点：**这种方法的缺点是，您必须深入研究故意使其难以从外部理解的反机器人系统，并分别测试不同的技术来欺骗他们的验证系统。然后在 Cloudflare 继续开发其反机器人保护时维护此系统。</p>
<p>可以这样做，但我只会建议某人采取这种方法，除非他们是：</p>
<ol>
<li>真正对逆向工程复杂的反机器人系统的智力挑战感兴趣，或者</li>
<li>拥有更具成本效益的 Cloudflare 旁路所带来的经济回报，保证了您必须投入数天或数周的工程时间来构建和维护它。</li>
</ol>
<p>对于抓取量非常大（每月超过 5 亿页）的公司或业务依赖经济高效的方式访问站点的智能代理解决方案的公司来说，构建您自己的自定义 Cloudflare 旁路可能是一个不错的选择。</p>
<p>对于大多数其他开发人员来说，您最好使用其他五种 Cloudflare 绕过方法之一。</p>
<p><a href="https://www.cloudflare.com/waf/">对于那些确实想要尝试的人，以下是 Cloudflare 的Web 应用程序防火墙 (WAF)</a>的工作原理以及如何绕过它的详细介绍。</p>
<h3 id="了解-cloudflare-的机器人管理器">了解 Cloudflare 的机器人管理器</h3>
<p>当我们说我们想要绕过 Cloudflare 时，我们真正的意思是我们想要绕过他们的<a href="https://www.cloudflare.com/en-gb/products/bot-management/">Bot Manager ，该管理器是他们的</a><a href="https://www.cloudflare.com/waf/">Web 应用程序防火墙 (WAF)</a>的一部分。</p>
<p>旨在减轻恶意机器人攻击而不影响真实用户的系统。</p>
<p><strong>Cloudflares 机器人检测系统可分为两类：</strong></p>
<ul>
<li>**后端检测技术：**这些是在后端服务器上执行的机器人指纹识别技术。</li>
<li>**客户端检测技术：**这些是在用户浏览器（客户端）中执行的机器人指纹识别技术。</li>
</ul>
<p>要绕过 Cloudflare，您必须通过两组验证测试。</p>
<h3 id="通过-cloudflare-的后端检测技术">通过 Cloudflare 的后端检测技术</h3>
<p>以下是 Cloudflare 在服务器端执行的已知后端机器人指纹识别技术以及如何传递它们：</p>
<h4 id="代理质量">代理质量</h4>
<p>Cloudflare 进行的最基本测试之一是计算您用于发送请求的 IP 地址的 IP 地址信誉评分。考虑到它是否属于任何已知机器人网络的一部分、其位置、ISP、声誉历史等因素。</p>
<p>要获得最高的 IP 地址信誉分数，您应该使用住宅/移动代理而不是数据中心代理或与 VPN 关联的任何代理。但是，如果数据中心代理质量很高，那么它们仍然可以工作。</p>
<h4 id="http-浏览器标头">HTTP 浏览器标头</h4>
<p>Cloudflare 还会分析您随请求发送的 HTTP 标头，并将它们与已知浏览器标头模式的数据库进行比较。</p>
<p>大多数 HTTP 客户端默认发送用户代理和其他明确标识它们的标头，因此您需要覆盖这些标头并使用与您想要显示的浏览器类型相匹配的完整浏览器标头集。在本<a href="https://scrapeops.io/web-scraping-playbook/web-scraping-guide-header-user-agents/">标头优化指南</a>中，我们详细介绍了如何执行此操作，您可以使用我们的<a href="https://scrapeops.io/docs/fake-user-agent-headers-api/overview/">免费假浏览器标头 API</a>来生成假浏览器标头列表。</p>
<h4 id="tls-和-http2-指纹">TLS 和 HTTP/2 指纹</h4>
<p>Cloudflare 使用的更复杂的指纹检测系统是 TLS 和 HTTP/2 指纹识别。每个 HTTP 请求客户端都会生成一个静态 TLS 和 HTTP/2 指纹，Cloudflare 可以使用它们来确定请求是来自真实用户还是机器人。</p>
<p>不同版本的浏览器和 HTTP 客户端往往具有不同的 TLS 和 HTTP/2 指纹，然后 Cloudflare 可以将这些指纹与您发送的浏览器标头进行比较，以确保您确实是您所设置的浏览器标头中声称存在的人。</p>
<p>问题在于，伪造 TLS 和 HTTP/2 指纹比简单地向请求中添加伪造的浏览器标头要困难得多。您首先需要捕获并分析来自要模拟的浏览器的数据包，然后更改用于发出请求的 TLS 和 HTTP/2 指纹。</p>
<p>但是，许多 HTTP 客户端（例如<a href="https://requests.readthedocs.io/">Python Requests）</a>无法让您更改这些 TLS 和 HTTP/2 指纹。您将需要使用编程语言和 HTTP 客户端，例如<a href="https://pkg.go.dev/net/http">Golang HTTP</a>或<a href="https://github.com/sindresorhus/got">Got</a>，它为您提供了对请求的足够低级控制，以便您可以伪造 TLS 和 HTTP/2 指纹。</p>
<p><a href="https://github.com/Danny-Dasilva/CycleTLS">像CycleTLS</a>、<a href="https://github.com/apify/got-scraping">Got Scraping</a>这样的库。<a href="https://github.com/refraction-networking/utls">utls</a>帮助您在 GO 和 Javascript 中欺骗 TLS/JA3 指纹。</p>
<p>这是一个复杂的主题，因此我建议您深入了解 TLS 和 HTTP/2 指纹识别的工作原理。以下是一些可以帮助您的资源：</p>
<ul>
<li><a href="https://www.akamai.com/us/en/multimedia/documents/white-paper/passive-fingerprinting-of-http2-clients-white-paper.pdf">HTTP/2 客户端的被动指纹识别（Akami 白皮书，2017 年）</a></li>
<li><a href="https://www.cloudflare.com/en-gb/learning/ssl/what-happens-in-a-tls-handshake/">TLS 握手中会发生什么？</a></li>
<li><a href="https://engineering.salesforce.com/tls-fingerprinting-with-ja3-and-ja3s-247362855967/">使用 JA3 和 JA3S 进行 TLS 指纹识别</a></li>
</ul>
<p>**重要提示：**匹配浏览器标头、TLS 和 HTTP/2 指纹</p>
<p>Cloudflare 使用这些指纹识别方法检测您的抓取工具的方式是，当您使用用户代理和浏览器标头发出请求时，表明您是 Chrome 浏览器，但是，您的 TLS 和 HTTP/2 指纹表明您正在使用 Python 请求 HTTP 客户端。</p>
<p>因此，要欺骗 Cloudflares 指纹测试，您需要确保浏览器标头、TLS 和 HTTP/2 指纹全部一致，并告诉 Cloudflare 请求来自真实的浏览器。</p>
<p>当您使用自动浏览器发出请求时，所有这些都会为您处理。然而，当您尝试使用普通 HTTP 客户端发出请求时，事情会变得相当棘手。</p>
<p>Cloudflare 的服务器端检测技术是其第一道防线。如果您未通过这些测试中的任何一项，您的请求将受到 Cloudflare 的质疑或阻止。</p>
<p>服务器端检测技术会为您的请求分配一个风险评分，然后 Cloudflare 使用该评分来确定在客户端向您显示哪些挑战（如果有）。</p>
<p>每个网站都可以设置自己的反机器人保护风险阈值，以确定应该挑战谁以及挑战什么（后台客户端挑战或验证码）。因此，您的目标是获得尽可能最低的风险评分。特别是对于最受保护的网站。</p>
<h3 id="通过-cloudflare-的客户端检测技术">通过 Cloudflare 的客户端检测技术</h3>
<p>好的，假设您已经能够构建一个系统来通过所有 Cloudflares 服务器端反机器人检查，现在您需要处理其客户端验证测试。</p>
<p>当 Cloudflare 在允许您访问网站之前向您显示其<strong>安全页面</strong>时，会发生这些客户端验证测试。<a href="https://en.aw-lab.com/">这是一个例子</a>。</p>
<figure data-type="image" tabindex="10"><img src="https://assets-scrapeops.nyc3.digitaloceanspaces.com/Images/Playbooks/Web-Scraping-Playbook/cloudflare/how-to-bypass-cloudflare-challenge-page.png" alt="如何绕过 Cloudflare - 挑战页面" loading="lazy"></figure>
<p>当您（或您的抓取工具）首次访问网站时，Cloudflare 将显示此页面，并且您的浏览器在后台解决各种挑战，以向 Cloudflare 证明您不是机器人。</p>
<p>如果您被标记为机器人，那么您将收到<strong>403 Access Denied / Forbidden 错误</strong>。</p>
<p>您的请求在服务器端测试期间获得的风险评分可能会影响它运行的客户端验证测试。最重要的是，它是否需要您解决 CAPTHCA。</p>
<p>有三种通用方法可以解决您在此页面等待时出现的客户端反机器人挑战：</p>
<ul>
<li>**使用自动化浏览器：**前面提到过，如果您使用<a href="#scrape-with-fortified-headless-browsers">强化的浏览器</a>打开页面，那么它将承担解决 Cloudflare JavaScript 挑战的大量繁重工作。</li>
<li><strong>在沙箱中模拟浏览器：您可以使用</strong><a href="https://github.com/jsdom/jsdom">JSDOM</a>等库在沙箱中模拟浏览器，这会减少资源占用，并让您更好地控制您希望它呈现的内容。</li>
<li>**构建挑战求解器算法：**构建无需浏览器即可通过检查的算法。这是最难的方法，因为您需要完全理解 Cloudflares 客户端检查、对 Javascript 挑战脚本进行反混淆，然后创建算法来解决它们。</li>
</ul>
<p>以下是 Cloudflare 在用户浏览器中执行的主要客户端机器人指纹识别技术，您需要通过这些技术：</p>
<h4 id="浏览器-web-api">浏览器 Web API</h4>
<p>现代浏览器有<a href="https://developer.mozilla.org/en-US/docs/Web/API">数百个 API</a>，允许我们作为开发人员设计与用户浏览器交互的应用程序。不幸的是，当 Cloudflare 在用户浏览器中加载时，它也可以访问所有这些 API。</p>
<p>允许它访问有关浏览器环境的大量信息，然后可以使用这些信息来检测撒谎其真实身份的爬虫。例如Cloudflare可以查询：</p>
<ol>
<li>**特定于浏览器的 API：**某些 Web API<code>window.chrome</code>仅存在于 Chrome 浏览器上。因此，如果您的浏览器标头、TLS 和 HTTP/2 指纹都表明您正在使用 Chrome 浏览器发出请求，但当<code>window.chrome</code>Cloudflare 检查浏览器时 API 不存在，那么这显然表明您正在伪造指纹。</li>
<li><strong>自动化浏览器 API：</strong><a href="https://www.selenium.dev/">Selenium</a>等自动化浏览器具有<code>window.document.__selenium_unwrapped</code>. 如果 Cloudflare 发现这些 API 存在，那么它就知道您不是真正的用户。</li>
<li><strong>沙盒浏览器模拟器 API：像</strong><a href="https://github.com/jsdom/jsdom">JSDOM</a>这样的沙盒浏览器浏览器模拟器，运行在 NodeJs 中，具有<code>process</code>仅存在于 NodeJs 中的对象。</li>
<li>**环境 API：**如果您的用户代理表示您正在使用 MacO 或 Windows 计算机，但该<code>navigator.platform</code>值设置为<code>Linux x86_64</code>，那么这会使您的请求看起来可疑。</li>
</ol>
<p>如果您使用的是强化浏览器，它将修复很多此类漏洞，但是，您可能需要修复更多漏洞，并确保您的浏览器标头以及 TLS 和 HTTP/2 指纹与从浏览器 Web API 返回的值相匹配。</p>
<h4 id="画布指纹识别">画布指纹识别</h4>
<p>Cloudflare 用于检测抓取工具的另一种技术是画布指纹识别，该技术允许 Cloudflare 对所使用的设备类型（浏览器、操作系统和系统图形硬件的组合）进行分类。</p>
<p>Cloudflare 使用<a href="https://research.google/pubs/pub45581/">Google 的毕加索指纹识别。</a>生成画布指纹。</p>
<p>Canvas 指纹识别是最常见的浏览器指纹识别技术之一，它使用 HTML5 API 通过 Javascript 绘制页面的图形和动画，然后可用于生成设备的指纹。</p>
<p>检查您的画布指纹</p>
<p>您可以使用<a href="https://browserleaks.com/canvas">BrowserLeaks Live Demo</a>查看浏览器画布指纹。</p>
<p>Cloudflare 维护着大量合法画布指纹和用户代理对的数据集。因此，当请求来自一个在标头中声称是在 Windows 计算机上运行的 Firefox 浏览器的用户，但他们的画布指纹表明他们实际上是在 Linux 计算机上运行的 Chrome 浏览器时，这就是 Cloudflare 的标志：质疑或阻止请求。</p>
<h4 id="事件跟踪">事件跟踪</h4>
<p>如果您需要浏览网页或与网页交互以获取所需的数据，那么您将不得不应对 Cloudflares 事件跟踪。</p>
<p>Cloudflare 向网页添加事件侦听器，以便它可以监视用户操作，例如鼠标移动、单击和按键。如果您有一个需要与页面交互的抓取工具，但鼠标从未移动，那么这对 Cloudflare 来说是一个明确的信号，表明该请求来自自动浏览器而不是真正的用户。</p>
<h4 id="验证码">验证码</h4>
<p>在抓取 Cloudflare 受保护的网站时，您将面临的最困难的 Cloudflare 反机器人挑战可能是解决其 CAPTCHA 挑战。</p>
<figure data-type="image" tabindex="11"><img src="https://assets-scrapeops.nyc3.digitaloceanspaces.com/Images/Playbooks/Web-Scraping-Playbook/cloudflare/how-to-bypass-cloudflare-hcaptcha.jpg" alt="如何绕过 Cloudflare - hCAPTCHA 挑战" loading="lazy"></figure>
<p>Cloudflare 仅在以下情况下向用户显示验证码挑战：</p>
<ol>
<li>Cloudflare 为该请求提供了高风险评分。</li>
<li><strong>该网站已将其安全性配置为有时</strong>或<strong>始终</strong>显示验证码挑战。</li>
</ol>
<p>幸运的是，大多数网站不愿意显示验证码挑战，因为众所周知它们会损害用户体验。</p>
<p>在极少数情况下，网站管理员已将 Cloudflare 配置为在每个请求上显示验证码，那么您将需要使用基于人工的验证码解决服务来解决他们的 hCaptcha 挑战，因为自动验证码解决程序无法解决 hCaptcha 验证<a href="https://blog.cloudflare.com/moving-from-recaptcha-to-hcaptcha/">码</a>。这并不理想，因为它会使抓取变得非常缓慢且昂贵。</p>
<p>否则，您应该尽可能优化您的抓取工具，以降低 Cloudflare 分配给它们的风险评分。这样您应该能够避免完全处理它们。</p>
<h3 id="低电平旁路">低电平旁路</h3>
<p>总的来说，实际上，为 Cloudflares 反机器人系统进行逆向工程和开发低级旁路（不使用无头浏览器）非常具有挑战性，因为您需要：</p>
<ul>
<li>加载等候室页面时拦截 Cloudflare 网络请求</li>
<li>对 Cloudflare 代码进行反混淆</li>
<li>解密混淆代码中包含的 Javascript 挑战</li>
<li>了解反混淆代码中包含的 Javascript 挑战</li>
<li>解决 Javascript 挑战并返回正确的结果。</li>
</ul>
<p>以下是 Cloudflare 执行的一些浏览器 API 测试的反混淆片段。</p>
<pre><code class="language-javascript">function _0x15ee4f(_0x4daef8) { 
	return { 
		/* .. */ 
		wb: !(!_0x4daef8.navigator || !_0x4daef8.navigator.webdriver), 
		wp: !(!_0x4daef8.callPhantom &amp;&amp; !_0x4daef8._phantom), 
		wn: !!_0x4daef8.__nightmare, 
		ch: !!_0x4daef8.chrome, 
		ws: !!( 
			_0x4daef8.document.__selenium_unwrapped || 
			_0x4daef8.document.__webdriver_evaluate || 
			_0x4daef8.document.__driver_evaluate 
		), 
		wd: !(!_0x4daef8.domAutomation &amp;&amp; !_0x4daef8.domAutomationController), 
	}; 
}
</code></pre>
<p>我们将在另一篇文章中更详细地讨论如何实际逆向工程 Cloudflare 的 Javascript 挑战，因为这是一个大主题。</p>
<h2 id="更多网页抓取指南">更多网页抓取指南</h2>
<p>因此，当谈到绕过 Cloudflare 时，您有多种选择。有些非常快速和简单，有些则复杂得多。每个都有自己的权衡。</p>
<p>如果您想了解如何抓取一些热门网站，请查看我们的其他<strong>如何抓取指南</strong>：</p>
<ul>
<li><a href="https://scrapeops.io/web-scraping-playbook/how-to-scrape-amazon/">如何抓取 Amazon.com</a></li>
<li><a href="https://scrapeops.io/web-scraping-playbook/how-to-scrape-walmart/">如何抓取 Walmart.com</a></li>
<li><a href="https://scrapeops.io/web-scraping-playbook/how-to-scrape-indeed/">如何抓取 Indeed.com</a></li>
</ul>
<p>或者，如果您想了解有关网页抓取的更多信息，请务必查看<a href="https://scrapeops.io/web-scraping-playbook/">网页抓取手册</a>，或查看我们更深入的指南之一：</p>
<ul>
<li><a href="https://scrapeops.io/blog/the-state-of-web-scraping-2022">2020 年网页抓取现状</a></li>
<li><a href="https://scrapeops.io/web-scraping-playbook/ethics-of-web-scraping/">网络抓取的道德规范</a></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Git 提交规范]]></title>
        <id>https://hualiang.online/post/git-ti-jiao-gui-fan/</id>
        <link href="https://hualiang.online/post/git-ti-jiao-gui-fan/">
        </link>
        <updated>2024-06-17T14:59:48.000Z</updated>
        <summary type="html"><![CDATA[<p>经常看到别人提交的代码记录里面包含一些feat、fix、chore等等，而我在提交时也不会区分什么，直接写下提交信息，今天就来看一下怎么个事，就拿 element-plus 举例来看一下</p>
]]></summary>
        <content type="html"><![CDATA[<p>经常看到别人提交的代码记录里面包含一些feat、fix、chore等等，而我在提交时也不会区分什么，直接写下提交信息，今天就来看一下怎么个事，就拿 element-plus 举例来看一下</p>
<!-- more -->
<p>其实这么写是一种代码提交规范，当然不是为了炫技，主要目的是为了提高提交记录的可读性和自动化处理能力。当然如果团队没有要求，不这么写也可以。</p>
<pre><code>commit message = subject + ：+ 空格 + message 主体
</code></pre>
<p>常见的 subject 种类以及含义如下：</p>
<ol>
<li>feat: 新功能（feature）
<ul>
<li>用于提交新功能。</li>
<li>例如：feat: 增加用户注册功能</li>
</ul>
</li>
<li>fix: 修复 bug
<ul>
<li>用于提交 bug 修复。</li>
<li>例如：fix: 修复登录页面崩溃的问题</li>
</ul>
</li>
<li>docs: 文档变更
<ul>
<li>用于提交仅文档相关的修改。</li>
<li>例如：docs: 更新README文件</li>
</ul>
</li>
<li>style: 代码风格变动（不影响代码逻辑）
<ul>
<li>用于提交仅格式化、标点符号、空白等不影响代码运行的变更。</li>
<li>例如：style: 删除多余的空行</li>
</ul>
</li>
<li>refactor: 代码重构（既不是新增功能也不是修复bug的代码更改）
<ul>
<li>用于提交代码重构。</li>
<li>例如：refactor: 重构用户验证逻辑</li>
</ul>
</li>
<li>perf: 性能优化
<ul>
<li>用于提交提升性能的代码修改。</li>
<li>例如：perf: 优化图片加载速度</li>
</ul>
</li>
<li>test: 添加或修改测试
<ul>
<li>用于提交测试相关的内容。</li>
<li>例如：test: 增加用户模块的单元测试</li>
</ul>
</li>
<li>chore: 杂项（构建过程或辅助工具的变动）
<ul>
<li>用于提交构建过程、辅助工具等相关的内容修改。</li>
<li>例如：chore: 更新依赖库</li>
</ul>
</li>
<li>build: 构建系统或外部依赖项的变更
<ul>
<li>用于提交影响构建系统的更改。</li>
<li>例如：build: 升级webpack到版本5</li>
</ul>
</li>
<li>ci: 持续集成配置的变更
<ul>
<li>用于提交CI配置文件和脚本的修改。</li>
<li>例如：ci: 修改GitHub Actions配置文件</li>
</ul>
</li>
<li>revert: 回滚
<ul>
<li>用于提交回滚之前的提交。</li>
<li>例如：revert: 回滚feat: 增加用户注册功能</li>
</ul>
</li>
</ol>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Java 应用中使用 Amazon S3 进行存储桶和对象操作完全指南]]></title>
        <id>https://hualiang.online/post/java-ying-yong-zhong-shi-yong-amazon-s3-jin-xing-cun-chu-tong-he-dui-xiang-cao-zuo-wan-quan-zhi-nan/</id>
        <link href="https://hualiang.online/post/java-ying-yong-zhong-shi-yong-amazon-s3-jin-xing-cun-chu-tong-he-dui-xiang-cao-zuo-wan-quan-zhi-nan/">
        </link>
        <updated>2024-06-15T07:12:21.000Z</updated>
        <summary type="html"><![CDATA[<p>转自 CSDN “<a href="https://bluetata.blog.csdn.net">bluetata</a>” 的<a href="https://blog.csdn.net/dietime1943/article/details/132531011">《【 云计算 | AWS 实践 】Java 应用中使用 Amazon S3 进行存储桶和对象操作完全指南》</a></p>
]]></summary>
        <content type="html"><![CDATA[<p>转自 CSDN “<a href="https://bluetata.blog.csdn.net">bluetata</a>” 的<a href="https://blog.csdn.net/dietime1943/article/details/132531011">《【 云计算 | AWS 实践 】Java 应用中使用 Amazon S3 进行存储桶和对象操作完全指南》</a></p>
<!-- more -->
<h2 id="一-前言">一、前言</h2>
<p>在本文中，我们将探索如何利用 <a href="https://so.csdn.net/so/search?q=Java&amp;spm=1001.2101.3001.7020">Java</a> 编程与 Amazon S3（即简单存储服务）存储系统进行互动。</p>
<p>需要牢记，S3 的结构异常简单：每个存储桶能够容纳大量的对象，这些对象可以通过 SOAP 接口或 REST 风格的 API 进行访问。</p>
<p>接下来，我们将使用适用于Java的AWS开发工具包来实现S3存储桶的创建、列举以及删除。同时，我们还将学会如何上传、列举、下载、复制、移动、重命名以及删除这些存储桶内的各个对象。</p>
<h2 id="二-所需-maven-依赖">二、所需 Maven 依赖</h2>
<p>在开始之前，我们需要在项目中声明 AWS SDK 依赖项：</p>
<pre><code class="language-java">&lt;dependency&gt;
    &lt;groupId&gt;software.amazon.awssdk&lt;/groupId&gt;
    &lt;artifactId&gt;s3&lt;/artifactId&gt;
    &lt;version&gt;2.20.52&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p><em>p.s. 要查看最新版本，我们可以检查 <a href="https://central.sonatype.com/">Maven Central</a>，或者其他的国内 Maven 仓库。</em></p>
<h2 id="三-必要的几个条件信息">三、必要的几个条件信息</h2>
<p>要使用AWS SDK，我们需要一些东西：</p>
<ol>
<li>AWS 帐户：我们需要一个 Amazon Web Services 帐户。如果我们没有，我们可以直接在 AWS 控制台创建一个帐户。</li>
<li>AWS 安全凭证：这些是我们的访问密钥，允许我们以编程方式调用 AWS API 操作。我们可以通过两种方式获取这些凭证：使用“安全凭证”页面的访问密钥部分中的 AWS 根账户凭证，或者使用<code>IAM</code>控制台中的 IAM 用户凭证。</li>
<li>选择 AWS 区域（Region）：我们还必须选择要存储 Amazon S3 数据的 AWS 区域。请记住，S3 存储价格因地区而异。有关更多详细信息，请参阅官方文档。</li>
</ol>
<h2 id="四-创建客户端连接">四、创建客户端连接</h2>
<p>首先，我们需要创建一个客户端连接来访问 Amazon S3 Web 服务。为此，我们将使用 Amazon S3 接口：</p>
<pre><code class="language-java">AWSCredentials credentials = new BasicAWSCredentials(
  &quot;&lt;AWS accesskey&gt;&quot;, 
  &quot;&lt;AWS secretkey&gt;&quot;
);
</code></pre>
<p>然后我们将配置客户端：</p>
<pre><code class="language-java">AmazonS3 s3client = AmazonS3ClientBuilder
  .standard()
  .withCredentials(new AWSStaticCredentialsProvider(credentials))
  .withRegion(Regions.US_EAST_2)
  .build();
</code></pre>
<h2 id="五-存储桶操作">五、存储桶操作</h2>
<h3 id="创建桶">创建桶</h3>
<p>需要注意的是，存储桶命名空间是由系统的所有用户共享的。<strong>因此，我们的存储桶名称在 Amazon S3 中的所有现有存储桶名称中必须是唯一的</strong>（稍后我们将了解如何检查这一点）。</p>
<p>另外，根据官方文档规定，Bucket 名称必须符合以下要求：</p>
<ul>
<li>名称不应包含下划线</li>
<li>名称长度应介于 3 到 63 个字符之间</li>
<li>名称不应以破折号结尾</li>
<li>名称不能包含相邻的句点</li>
<li>名称后面不能包含破折号（例如，“my-.bucket.com”和“my.-bucket”无效）</li>
<li>名称不能包含大写字符</li>
</ul>
<p>现在让我们创建一个存储桶：</p>
<pre><code class="language-java">String bucketName = &quot;baeldung-bucket&quot;;

if(s3client.doesBucketExist(bucketName)) {
    LOG.info(&quot;Bucket name is not available.&quot;
      + &quot; Try again with a different Bucket name.&quot;);
    return;
}
CreateBucketRequest bucketRequest = CreateBucketRequest.builder()
    .bucket(bucketName)
    .build();

s3Client.createBucket(bucketRequest);
</code></pre>
<p>在创建存储桶之前，我们必须使用<code>doesBucketExist()</code>方法检查存储桶名称是否可用。如果名称可用，那么我们将构建一个<code>CreateBucketRequest</code> 并提供存储桶名称。最后一步是将<code>bucketRequest</code>传递给 S3Client 的<code>CreateBucketRequest createBucketRequest</code>的<code>createBucket</code>。</p>
<h3 id="列出桶">列出桶</h3>
<p>现在我们已经创建了一些存储桶，让我们使用<code>listBuckets ()</code>方法打印 S3 环境中可用的所有存储桶的列表。此方法将返回一个<code>ListBucketsResponse</code>， 其中包含有关存储桶的信息。</p>
<pre><code class="language-java">ListBucketsResponse listBucketsResponse = s3Client.listBuckets();

// Display the bucket names
List&lt;Bucket&gt; buckets = listBucketsResponse.buckets();
System.out.println(&quot;Buckets:&quot;);
for (Bucket bucket : buckets) {
    System.out.println(bucket.name());
}
</code></pre>
<p>这将列出 S3 环境中存在的所有存储桶：</p>
<pre><code class="language-java">baeldung-bucket
baeldung-bucket-test2
elasticbeanstalk-us-east-2
</code></pre>
<h3 id="删除桶">删除桶</h3>
<p><strong>在删除存储桶之前，确保存储桶是空的非常重要。</strong> 否则，将会抛出异常。</p>
<p>首先，我们需要构建一个<code>DeleBucketRequest</code>实例并向其传递存储桶名称。然后，我们调用 s3Client 对象上的<code>deleteBucket</code>方法，并将请求作为参数传递。</p>
<p>另请注意，只有存储桶的所有者才能删除它，无论其权限如何（访问控制策略）：</p>
<pre><code class="language-java">try {
    DeleteBucketRequest deleteBucketRequest = DeleteBucketRequest.builder()
        .bucket(bucketName)
        .build();

    s3Client.deleteBucket(deleteBucketRequest);
    System.out.println(&quot;Successfully deleted bucket : &quot; + bucketName);
} catch (S3Exception e) {
    System.err.println(e.getMessage());
    System.exit(1);
}
</code></pre>
<h2 id="六-对象操作">六、对象操作</h2>
<p>Amazon S3 存储桶内的文件或数据集合称为对象。我们可以对对象执行多种操作，例如上传、列出、下载、复制、移动、重命名和删除。</p>
<h3 id="上传对象">上传对象</h3>
<p>上传对象是一个非常简单的过程。首先，我们将构建一个<code>PutObjectRequest</code>实例，指定存储桶名称和密钥。然后，我们将该请求和包含数据的文件的路径传递给 s3Client 的<code>putObject</code>方法：</p>
<pre><code class="language-java">PutObjectRequest request = PutObjectRequest.builder()
    .bucket(bucketName)
    .key(key)
    .build();

return s3Client.putObject(request, Path.of(file.toURI()));
</code></pre>
<h3 id="列出对象">列出对象</h3>
<p>我们将使用<code>listObjects()</code>方法列出 S3 存储桶中的所有可用对象：</p>
<pre><code class="language-java">ListObjectsV2Request listObjectsV2Request = ListObjectsV2Request.builder()
    .bucket(bucketName)
    .build();
ListObjectsV2Response listObjectsV2Response = s3Client.listObjectsV2(listObjectsV2Request);

List&lt;S3Object&gt; contents = listObjectsV2Response.contents();

System.out.println(&quot;Number of objects in the bucket: &quot; + contents.stream().count());
contents.stream().forEach(System.out::println);
</code></pre>
<p>要列出 AWS S3 存储桶中的对象，我们需要创建一个<code>ListObjectsV2Request</code>实例并指定存储桶名称。然后，我们在 s3Client 对象上调用 <code>listObjectsV2</code>方法，并将请求作为参数传递。此方法返回一个<code>ListObjectsV2Response</code>，其中包含有关存储桶中对象的信息。</p>
<h3 id="下载对象">下载对象</h3>
<p>要下载对象，我们首先创建一个<code>GetObjectRequest</code>实例并将存储桶名称和密钥作为输入参数传递给它。然后，我们将其提供给 <code>getObjectAsBytes()</code>方法并获取响应。一旦我们得到响应，我们就可以提取字节数组。最后一步是处理字节数组：</p>
<pre><code class="language-java">GetObjectRequest objectRequest = GetObjectRequest.builder()
    .bucket(bucketName)
    .key(objectKey)
    .build();

ResponseBytes&lt;GetObjectResponse&gt; responseResponseBytes = s3Client.getObjectAsBytes(objectRequest);

byte[] data = responseResponseBytes.asByteArray();

// Write the data to a local file.
java.io.File myFile = new java.io.File(&quot;/Users/user/Desktop/hello.txt&quot; );
OutputStream os = new FileOutputStream(myFile);
os.write(data);
System.out.println(&quot;Successfully obtained bytes from an S3 object&quot;);
os.close();
</code></pre>
<h3 id="复制和移动对象">复制和移动对象</h3>
<p>我们可以通过调用 s3client 上的<code>copyObject()</code>方法来复制对象，该方法接受<code>CopyObjectRequest</code>实例。因此，<code>CopyObjectRequest</code>接受四个参数：</p>
<ol>
<li>源存储桶名称</li>
<li>源存储桶中的对象键</li>
<li>目标存储桶名称（可以与源存储桶名称相同）</li>
<li>目标存储桶中的对象键</li>
</ol>
<pre><code class="language-java">CopyObjectRequest copyObjectRequest = CopyObjectRequest.builder()
    .sourceBucket(sourceBucketName)
    .sourceKey(sourceKey)
    .destinationBucket(destinationBucketName)
    .destinationKey(destinationKey)
    .build();

return s3Client.copyObject(copyObjectRequest);
</code></pre>
<p>注意：我们可以结合使用<code>copyObject()</code>方法和<code>deleteObject()</code>来执行移动和重命名任务。这将涉及首先复制对象，然后将其从旧位置删除。</p>
<h3 id="删除对象">删除对象</h3>
<p>要删除对象，我们将在s3client上调用<code>deleteObject()</code>方法并传递<code>DeleteObjectRequest</code>实例。为了创建<code>DeleteObjectRequest</code>实例，我们需要传递要删除的对象的键和存储桶名称：</p>
<pre><code class="language-java">DeleteObjectRequest deleteObjectRequest = DeleteObjectRequest.builder()
    .bucket(bucketName)
    .key(objectKey)
    .build();

s3Client.deleteObject(deleteObjectRequest);
</code></pre>
<h3 id="删除多个对象">删除多个对象</h3>
<p>要一次删除多个对象，我们首先创建<code>DeleteObjectsRequest</code>对象并传递存储桶。然后我们将传递一个包含所有要删除的对象键的 ArrayList。</p>
<p>一旦我们有了这个<code>DeleteObjectsRequest</code>对象，我们就可以将它作为参数传递给我们的 s3client 的<code>deleteObjects()</code>方法。如果成功，它将删除我们提供的所有对象：</p>
<pre><code class="language-java">ArrayList&lt;ObjectIdentifier&gt; toDelete = new ArrayList&lt;&gt;();
for(String objKey : keys) {
    toDelete.add(ObjectIdentifier.builder()
        .key(objKey)
        .build());
}

DeleteObjectsRequest deleteObjectRequest = DeleteObjectsRequest.builder()
    .bucket(bucketName)
    .delete(Delete.builder()
        .objects(toDelete).build())
    .build();

s3Client.deleteObjects(deleteObjectRequest);
</code></pre>
<h2 id="七-文末总结">七、文末总结</h2>
<p>在本文中，我们重点介绍了在存储桶级别和对象级别与 Amazon S3 Web 服务交互的基础知识。重点在 Java 应用中使用 Amazon S3（Simple Storage Service）进行存储桶和对象操作的方法、详细说明了如何进行存储桶操作，包括创建桶和列出桶。在对象操作部分，我们涵盖了上传、列出和下载对象的过程，还介绍了复制、重命名、移动对象以及删除对象的方法。此外，我们还提供了删除多个对象的操作步骤。通过本文，小伙伴们可以了解如何通过 Java 代码有效地与 Amazon S3 进行交互，实现对存储桶和对象的各种操作。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[DelayQueue 实现临时上传文件的过期定时清理]]></title>
        <id>https://hualiang.online/post/delayqueue-shi-xian-lin-shi-shang-chuan-wen-jian-de-ding-shi-qing-li/</id>
        <link href="https://hualiang.online/post/delayqueue-shi-xian-lin-shi-shang-chuan-wen-jian-de-ding-shi-qing-li/">
        </link>
        <updated>2024-06-07T12:49:03.000Z</updated>
        <summary type="html"><![CDATA[<p>学院的智慧党建项目要实现图片上传功能，为了解决图片临时上传后的清理问题，考虑到数据量不大，我想到了用 Java 的 DelayQueue 延迟队列来处理。</p>
]]></summary>
        <content type="html"><![CDATA[<p>学院的智慧党建项目要实现图片上传功能，为了解决图片临时上传后的清理问题，考虑到数据量不大，我想到了用 Java 的 DelayQueue 延迟队列来处理。</p>
<!-- more -->
<h1 id="需求分析">需求分析</h1>
<p>前端在编辑器里编辑文章时，有时需要上传本地图片，如下：</p>
<figure data-type="image" tabindex="1"><img src="https://hualiang.online/post-images/1717766874461.png" alt="1" loading="lazy"></figure>
<p>上传图片：</p>
<figure data-type="image" tabindex="2"><img src="https://hualiang.online/post-images/1717767213521.png" alt="2" loading="lazy"></figure>
<p>要求：</p>
<ol>
<li>上传到后端后，后端需要返回一个可访问的 URL 链接给前端用来在文章中引用</li>
<li>临时上传到后端的图片若一定时间内未被任何文章引用，需要定时清理</li>
</ol>
<h1 id="技术选型">技术选型</h1>
<p>说到定时任务，首先想到两种方案：</p>
<ol>
<li>SpringBoot 提供的定时任务实现</li>
<li>Java 提供的延迟队列来实现</li>
</ol>
<p>考虑到该项目原本就是在学院内使用，数据量不大。而且定时任务时效性差，不能针对单独文件进行计时，故选择第二种方案。</p>
<h1 id="代码实现">代码实现</h1>
<p>延迟队列实现的思路还是很简单的：</p>
<p>将临时上传的图片都放入队列中并设置好过期时间，一旦图片过期就从队列中取出并删除。如果在过期前有文章引用了该图片，那么从队列中将其删除即可。</p>
<h2 id="ttlfile">TTLFile</h2>
<p>我们先创建一个过期文件类 <code>TTLFile</code> 实现 <code>Delayed</code> 接口，这里我们定义的是抽象类，实现一些基础功能，留下一些关键功能，如 <code>clean()</code> 方法，便于后续拓展功能。比如，用于清理本地文件的 <code>LocalFile</code>，或是存储在 Minio 上的 <code>MinioFile</code></p>
<pre><code class="language-java">package com.dangjian.clean;

import java.util.concurrent.Delayed;
import java.util.concurrent.TimeUnit;

import lombok.Getter;
// 延迟队列中的元素都需要实现 Delayed 接口来判断过期时间
public abstract class TTLFile implements Delayed {

    protected final String uuid;

    protected long ttl; // 过期时间

    @Getter
    protected int failedCount = 0; // 任务失败次数

    public TTLFile(String uuid, long delay) {
        this.uuid = uuid;
        this.ttl = System.currentTimeMillis() + delay;
    }

    /**
     * 查看当前任务还有多久到期
     * 
     * @param unit
     * @return 剩余时间
     */
    @Override
    public long getDelay(TimeUnit unit) {
        return unit.convert(ttl - System.currentTimeMillis(), TimeUnit.MILLISECONDS);
    }

    /**
     * 延迟队列需要到期时间升序入队，所以我们需要实现compareTo进行到期时间比较
     * 
     * @param delayed
     * @return 比较结果
     */
    @Override
    public int compareTo(Delayed delayed) {
        return Long.compare(this.ttl, ((TTLFile) delayed).ttl);
    }

    /**
     * 判断任务是否失败
     * 
     * @return 是否失败
     */
    public boolean isFailed() {
        return ++failedCount &gt;= 3;
    }

    /**
     * 延时
     */
    public void delay(long delay) {
        ttl = System.currentTimeMillis() + delay;
    }

    @Override
    public int hashCode() {
        final int prime = 31;
        int result = 1;
        result = prime * result + ((uuid == null) ? 0 : uuid.hashCode());
        return result;
    }

    @Override
    public boolean equals(Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TTLFile other = (TTLFile) obj;
        if (uuid == null) {
            if (other.uuid != null)
                return false;
        } else if (!uuid.equals(other.uuid))
            return false;
        return true;
    }

    /**
     * 清理文件
     * 
     * @return 是否清理成功
     */
    public abstract boolean clean();

    /**
     * 获取文件路径
     * 
     * @return 文件路径
     */
    public abstract String getPath();

}
</code></pre>
<p>注意：这里重写的 <code>hashCode()</code> 和 <code>equal()</code> 只将 <code>uuid</code> 作为唯一标识，因为要想从延迟队列中移除元素，我们就必须传入一个等价的元素才行。将 <code>uuid</code> 作为唯一标识，到时候移除元素只需要传入一个具有相同 <code>uuid</code> 的对象实例即可。</p>
<p><em>p.s. 一般情况，文件的存储路径都是唯一的，可以用来作为 uuid</em></p>
<h2 id="localfile">LocalFile</h2>
<p>因为一开始该项目并未使用 Minio 这种专门的文件存储服务，直接将文件存在本地，所以我先实现的是清理本地文件的功能。</p>
<p>得益于抽象类已经实现了一些基本功能，我们只需要实现 <code>clean()</code> 和 <code>getPath()</code> 即可。</p>
<pre><code class="language-java">package com.dangjian.clean;

import java.io.File;

public class LocalFile extends TTLFile {

    // 任务
    private File file;

    public LocalFile(String uuid, String path, long delay) {
        super(uuid, delay);
        this.file = new File(path);
    }

    /**
     * 删除文件
     * 
     * @return 是否清理成功
     */
    @Override
    public boolean clean() {
        return file.exists() ? file.delete() : true;
    }

    /**
     * 获取文件路径
     * 
     * @return 文件路径
     */
    @Override
    public String getPath() {
        return file.getPath();
    }

}
</code></pre>
<p>内置一个 <code>File</code> 类用来操作文件，<code>clean()</code> 也是判断文件存在后直接删除，实现起来并不难。</p>
<p>这样一个定时清理本地文件的类就写好了~</p>
<h2 id="cleaner">Cleaner</h2>
<p>根据之前的思路，要想从延迟队列里不断取出过期元素删除，我们就需要一个后台线程异步获取队列中的元素，而且需要在 SpringBoot 一启动就要开始执行。这里我们可以用 SpringBoot 提供的 <code>ApplicationRunner</code> 接口来实现。</p>
<p>在 SpringBoot 应用程序启动时，有时我们需要执行一些特定的任务，如加载配置、建立连接等。SpringBoot 提供了 <code>ApplicationRunner</code> 接口，允许我们在应用程序完全启动后执行自定义的逻辑。</p>
<p>下面我们通过实现该接口来定义一个“清洁工”</p>
<pre><code class="language-java">package com.dangjian.clean;

import org.springframework.boot.ApplicationArguments;
import org.springframework.boot.ApplicationRunner;
import org.springframework.stereotype.Component;

import com.dangjian.utils.FileUtil;

import lombok.extern.slf4j.Slf4j;

import java.time.LocalTime;
import java.util.List;
import java.util.concurrent.DelayQueue;

@Slf4j
@Component
public class Cleaner implements ApplicationRunner {

    private DelayQueue&lt;TTLFile&gt; cleanQueue = new DelayQueue&lt;&gt;();

    public void addTask(TTLFile ttlFile) {
        cleanQueue.add(ttlFile);
    }

    public void addTask(List&lt;TTLFile&gt; ttlFile) {
        cleanQueue.addAll(ttlFile);
    }

    public boolean removeTask(TTLFile ttlFile) {
        return cleanQueue.remove(ttlFile);
    }

    public boolean removeTask(List&lt;TTLFile&gt; ttlFile) {
        return cleanQueue.removeAll(ttlFile);
    }

    @Override
    public void run(ApplicationArguments args) {
        Thread thread = new Thread(() -&gt; {
            while (true) {
                try {
                    TTLFile ttlFile = cleanQueue.take();
                    if (ttlFile.clean()) {
                        log.info(&quot;成功清理：{}&quot;, ttlFile.getPath());
                    } else {
                        if (ttlFile.isFailed()) {
                            log.error(&quot;清理失败，文件路径：{}&quot;, ttlFile.getPath());
                            String info = String.format(&quot;[%s] - 清理失败，文件路径：%s&quot;, LocalTime.now(), ttlFile.getPath());
                            FileUtil.writeLog(FileUtil.getCurrentPath(), info);
                        } else {
                            log.warn(&quot;清理失败，重试次数：{}，文件路径：{}&quot;, ttlFile.getFailedCount(), ttlFile.getPath());
                            ttlFile.delay(60000); // 1分钟后重试
                            cleanQueue.put(ttlFile);
                        }
                    }
                } catch (Exception e) {
                    e.printStackTrace();
                }
            }
        });
        thread.setName(&quot;Cleaner&quot;);
        thread.start();
    }

}
</code></pre>
<p>在上面的代码中，我们内置了一个延迟队列，还定义了一些往队列里添加定时任务的方法。</p>
<p>其中，在重写的方法中，我们直接创建一个死循环的线程不断从队列中提取任务，这里调用的是延迟队列的 <code>take()</code> 方法，该方法在队列中没有可以取出的过期元素时，会阻塞消费者，直到有元素过期。所以并不会过多损耗 CPU 资源。</p>
<p>对于清理失败的情况，还会通过 <code>TTLFile</code> 内含的计数字段来重试 3 次清理操作。若依旧不成功，则该报错报错，该写日志写日志。</p>
<h1 id="实战">实战</h1>
<p>在我们的项目中，有一个专门的临时上传文件接口，我们可以在接收文件后将其加入到队列里。</p>
<pre><code class="language-java">@Operation(summary = &quot;临时上传文件&quot;)
@PostMapping(&quot;/upload&quot;)
public Result upload(@Parameter(description = &quot;上传的文件&quot;) @RequestPart MultipartFile file) {
    Optional.ofNullable(file).orElseThrow(() -&gt; new CustomException(&quot;上传文件为空！&quot;));

    String tempPath = FileUtil.tempUpload(file);
    String uuid = UUID.randomUUID().toString();
    redisTemplate.opsForValue().set(uuid, tempPath, 10, TimeUnit.MINUTES);
    
    // 将临时路径作为唯一标识
    cleaner.addTask(new LocalFile(tempPath, 10 * 60 * 1000)); // 10分钟后删除

    log.info(&quot;文件上传成功，临时路径：{}&quot;, tempPath);
    return Result.ok(uuid);
}
</code></pre>
<p><em>p.s. Cleaner 带有 @Component 注解，可直接注入</em></p>
<p>而当我们引用临时文件，需要将他从队列中移除时，只需要用相同的路径作为表示传入一个新对象即可。</p>
<p>下面的代码是用来设置支部活动附件，附件已经临时上传到后端了</p>
<pre><code class="language-java">@Override
public void setActivityFile(String actId, Activity activity) {
    // 通过活动 ID 从 Redis 里拿到预先存在里面的临时路径
    String path = redisTemplate.opsForValue().getAndDelete(actId);
    if (StringUtils.hasLength(path) &amp;&amp; cleaner.removeTask(new LocalFile(path, 1000))) { // 传入一个具有相同临时路径的对象来删除队列中的临时文件
        Optional.ofNullable(activity.getFile()).ifPresent(FileUtil::delete);
        activity.setFile(path);
    } else {
        throw new CustomException(&quot;临时上传的活动文件已过期，请重新上传！&quot;);
    }
}
</code></pre>
<p>经测试，在不出现 <code>IOException</code> 的情况下，只要提前将任务加入队列，一旦到期，临时上传的文件就能被成功删除。</p>
<h1 id="总结">总结</h1>
<p>清理文件时记得做好异常处理以及回滚机制，防止清理失败后未将任务重新添加回队列导致垃圾文件堆积。</p>
<p>延迟队列底层使用的无界队列，基于 JVM 内存，数据量少的情况下，这种方法简单且使用。若在企业级场景下，可能更多的会使用像 Kafka，RocketMQ 这种高性能消息队列提供的延迟队列来实现。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Vite 多页面实战]]></title>
        <id>https://hualiang.online/post/zhen-zheng-you-ya-de-vite-duo-ye-mian-shi-zhan/</id>
        <link href="https://hualiang.online/post/zhen-zheng-you-ya-de-vite-duo-ye-mian-shi-zhan/">
        </link>
        <updated>2024-06-01T07:58:44.000Z</updated>
        <summary type="html"><![CDATA[<p>转自稀土掘金 “<a href="https://juejin.cn/user/1099167356957885/posts">我想写文章啊</a>” 的<a href="https://juejin.cn/post/7128999848564981796">《真正优雅的 Vite 多页面实战》</a></p>
]]></summary>
        <content type="html"><![CDATA[<p>转自稀土掘金 “<a href="https://juejin.cn/user/1099167356957885/posts">我想写文章啊</a>” 的<a href="https://juejin.cn/post/7128999848564981796">《真正优雅的 Vite 多页面实战》</a></p>
<!-- more -->
<h1 id="vite如何支持多页">Vite如何支持多页？</h1>
<p><a href="https://link.juejin.cn?target=https%3A%2F%2Fcn.vitejs.dev%2Fguide%2Fbuild.html%23multi-page-app">cn.vitejs.dev/guide/build…</a></p>
<p>官方文档对于多页模式的描述中说到，如果想要新建一个单页，只需要在<strong>项目根目录</strong>（注意：不是 <code>src</code> 目录！）新建：</p>
<ul>
<li>nested
<ul>
<li><strong>index.html</strong></li>
<li><strong>main.js</strong></li>
</ul>
</li>
<li>package.json</li>
<li>vite.config.js</li>
</ul>
<p>然后通过访问：<code>http://localhost:5173/nested/</code>即可。</p>
<p>这里要说明两个问题：</p>
<p><em>为什么在根目录新建目录，而不是 <code>src</code> 目录？</em></p>
<p>因为vite启动时，会以根目录启动一个开发服务器，可以简单理解为开发服务器托管了整个项目的文件（但其实内部做了一些其他处理如处理public目录等）。</p>
<p>因此，在项目目录下的文件，都可以通过链接访问到，你可以访问<code>http://localhost:5173/package.json</code> 试试。</p>
<p>所以，你可以在任意目录放置自己的单页，但是开发时的访问地址也必须受限于单页的目录结构，如果你的单页入口文件放在<code>src/hello/index.html</code> ，也就只能通过<code>http://localhost:5173/src/hello/</code> 来进行访问。</p>
<p><em>为什么使用 <code>/nested/</code> 访问而不是 <code>nested</code>？</em></p>
<p>访问<code>/nested/</code> 相当于访问<code>nested</code>目录下的入口文件（一般静态服务器都会将 <code>index.html</code> 作为入口文件）。</p>
<h1 id="官方例子不符合实际开发场景">官方例子不符合实际开发场景</h1>
<p>vite官方给出的项目模板是这样的：</p>
<ul>
<li>src
<ul>
<li>main.js</li>
<li>App.vue</li>
</ul>
</li>
<li>index.html</li>
<li>package.json</li>
<li>vite.config.js</li>
</ul>
<p>按照官方例子，如果要新增一个单页，需要这样组织目录结构：</p>
<ul>
<li>src
<ul>
<li>main.js</li>
<li>App.vue</li>
</ul>
</li>
<li>nested
<ul>
<li>index.html</li>
</ul>
</li>
<li>index.html</li>
<li>package.json</li>
<li>vite.config.js</li>
</ul>
<p>新的单页居然与<code>src</code> 目录是平行的！这显然不符合正常的项目结构，一般而言，开发相关的源代码都会放在<code>src</code>目录下。因此，这个方案不适用，废弃。</p>
<p>那么，试试看第二种方案：</p>
<ul>
<li>index
<ul>
<li>index.html</li>
<li>main.js</li>
<li>App.vue</li>
</ul>
</li>
<li>nested
<ul>
<li>index.html</li>
<li>main.js</li>
<li>App.vue</li>
</ul>
</li>
<li>package.json</li>
<li>vite.config.js</li>
</ul>
<p>这种方案的好处是，单页互为平行关系，而且可以通过访问<code>/index/</code>与<code>/nested/</code>访问到对应的单页。</p>
<p>但是，其缺点就是：**如何管理公共资源？**例如，<code>index</code> 单页与<code>nested</code>单页都使用到同一款公共组件，应该放在哪里管理呢？</p>
<p>显然，这种项目结构缺陷明显，因此，这个方案也不适用，废弃。</p>
<h1 id="项目结构最佳实践">项目结构最佳实践</h1>
<p>按照以往的开发经验，这样组织项目结构可以让开发的可扩展达到最好：</p>
<ul>
<li>src
<ul>
<li>components</li>
<li>pages
<ul>
<li>index
<ul>
<li>index.html</li>
<li>main.js</li>
<li>App.vue</li>
</ul>
</li>
<li>nested
<ul>
<li>index.html</li>
<li>main.js</li>
<li>App.vue</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>package.json</li>
<li>vite.config.js</li>
</ul>
<p>这种项目结构，可以在<code>src</code>目录下管理多个单页需要使用到的公共资源，引用非常方便，并且扩展新的单页也非常方便。</p>
<p>但是！这种结构下，访问对应的单页需要使用<code>http://localhost:5173/src/pages/index/</code> 来访问，这种开发体验虽说不好，但尚且能忍受。最致命的问题是，当我们在<code>vite.config.js</code>中配置多页打包时：</p>
<pre><code class="language-typescript">export default defineConfig({
  build: {
    rollupOptions: {
      input: {
        index: path.resolve(__dirname, 'src/pages/index/index.html'),
        nested: path.resolve(__dirname, 'src/pages/nested/index.html'),
      }
    }
  }
})
</code></pre>
<p>打出来的包是这样的：</p>
<ul>
<li>dist
<ul>
<li>src
<ul>
<li>pages
<ul>
<li>index
<ul>
<li>index.html</li>
</ul>
</li>
<li>nested
<ul>
<li>index.html</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>assets</li>
</ul>
</li>
</ul>
<p>也就是说，线上用于也必须通过<code>http://localhost/src/pages/index/</code> 来访问！这显然不符合预期，我们预期的输出应该是：</p>
<ul>
<li>dist
<ul>
<li>assets</li>
<li>index.html</li>
<li>nested.html</li>
</ul>
</li>
</ul>
<p>为了解决这个问题，网上的一篇热门文章，采用了一种更改打包后的文件路径的方法来解决此问题，此方案的基本思路是：</p>
<ol>
<li>将<code>src/pages</code>下所有子目录下的<code>index.html</code>文件复制到根目录，并且改名为其父目录的名称，如<code>src/pages/nested/index.html</code>复制到跟目录，改名为<code>nested.html</code></li>
<li>将<code>nested.html</code> 下的样式、js等引用改为正确的引用路径。</li>
</ol>
<p>这个方案看似解决了打包后的路径问题，但是给开发人员带来了额外的认知成本，虽勉强解决了问题，但思路不够优雅。（居然是google搜索“vite 多页面”的第一篇文章，有点误人子弟了。）</p>
<h1 id="优雅的解决方案">优雅的解决方案</h1>
<p>首先，我们的项目结构必须以此为准：</p>
<ul>
<li>src
<ul>
<li>pages
<ul>
<li>index</li>
<li>nested</li>
</ul>
</li>
</ul>
</li>
<li>package.json</li>
<li>vite.config.js</li>
</ul>
<p>其次，打包出来的结构必须以此为准：</p>
<ul>
<li>dist
<ul>
<li>assets</li>
<li>index.html</li>
<li>nested.html</li>
</ul>
</li>
</ul>
<p>为了实现第二点，必须要求项目根目录下至少有这两个文件：</p>
<ul>
<li>src</li>
<li>index.html</li>
<li>nested.html</li>
<li>package.json</li>
<li>vite.config.js</li>
</ul>
<p>然后，在<code>src/pages/index</code>目录下，不放置<code>index.html</code>，只放<code>main.js</code>、<code>App.vue</code>等，让<code>src/index.html</code> 反向引用<code>src/pages/index/main.js</code>即可：</p>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
  &lt;head&gt;
    &lt;meta charset=&quot;UTF-8&quot; /&gt;
    &lt;link rel=&quot;icon&quot; type=&quot;image/svg+xml&quot; href=&quot;/vite.svg&quot; /&gt;
    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot; /&gt;
    &lt;title&gt;Vite + Vue + TS&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    &lt;div id=&quot;app&quot;&gt;&lt;/div&gt;
    &lt;script type=&quot;module&quot; src=&quot;/src/pages/index/main.js&quot;&gt;&lt;/script&gt;
  &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>至此，vite多页面架构的问题已完美解决！</p>
<h1 id="总结">总结</h1>
<ul>
<li>官方文档虽然权威，但不一定适合所有场景</li>
<li>在框架内解决问题，勉强的方案虽能解决问题，但后患无穷</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[背包问题的套路]]></title>
        <id>https://hualiang.online/post/bag-problem-solving-skills/</id>
        <link href="https://hualiang.online/post/bag-problem-solving-skills/">
        </link>
        <updated>2024-01-23T05:20:36.000Z</updated>
        <summary type="html"><![CDATA[<p>最近跟着代码随想录学到了线性规划章节，背包问题也是很常见的题型，在此总结一些做题套路。</p>
]]></summary>
        <content type="html"><![CDATA[<p>最近跟着代码随想录学到了线性规划章节，背包问题也是很常见的题型，在此总结一些做题套路。</p>
<!-- more -->
<p><strong>常见的背包问题有：</strong></p>
<ol>
<li>
<p>组合问题</p>
</li>
<li>
<p>True, False问题</p>
</li>
<li>
<p>最大最小问题。</p>
</li>
</ol>
<hr>
<p><strong>一、组合问题：</strong></p>
<ul>
<li>377. 组合总和 Ⅳ</li>
<li>494. 目标和</li>
<li>518. 零钱兑换 II</li>
</ul>
<p><strong>二、True, False问题：</strong></p>
<ul>
<li>139. 单词拆分</li>
<li>416. 分割等和子集</li>
</ul>
<p><strong>三、最大最小问题：</strong></p>
<ul>
<li>474. 一和零</li>
<li>322. 零钱兑换</li>
</ul>
<hr>
<p><strong>组合问题公式</strong></p>
<pre><code class="language-c++">dp[i] += dp[i - num]
</code></pre>
<p><strong>True, False问题公式</strong></p>
<pre><code class="language-c++">dp[i] = dp[i] || dp[i - num]
</code></pre>
<p><strong>最大最小问题公式</strong></p>
<pre><code class="language-c++">dp[i] = max(dp[i], dp[i - num] + 1)
dp[i] = min(dp[i], dp[i - num] + 1)
</code></pre>
<p>以上三组公式是解决对应问题的核心公式。</p>
<hr>
<h2 id="当然拿到问题后需要做到以下几个步骤">当然拿到问题后，需要做到以下几个步骤：</h2>
<ol>
<li>
<p>分析是否为背包问题。</p>
</li>
<li>
<p>是以上三种背包问题中的哪一种。</p>
</li>
<li>
<p>是0-1背包问题还是完全背包问题。也就是题目给的 nums 数组中的元素是否可以重复使用。</p>
</li>
<li>
<p>如果是组合问题，是否需要考虑元素之间的顺序。需要考虑顺序有顺序的解法，不需要考虑顺序又有对应的解法。</p>
</li>
</ol>
<hr>
<h2 id="背包问题的判定">背包问题的判定</h2>
<p>背包问题具备的特征：给定一个 target，target 可以是数字也可以是字符串，再给定一个数组 nums，nums 中装的可能是数字，也可能是字符串，问：能否使用 nums 中的元素做各种排列组合得到 target</p>
<p>背包问题技巧：</p>
<p>1.如果是 0-1 背包，即数组中的元素不可重复使用，nums 放在外循环，target 在内循环，且内循环倒序；</p>
<pre><code class="language-c++">for (int num : nums)
    for (int i = target; i &gt;= num; i--)
</code></pre>
<p>2.如果是完全背包，即数组中的元素可重复使用，nums 放在外循环，target 在内循环。且内循环正序。</p>
<pre><code class="language-c++">for (int num : nums)
    for (int i = 1; i &lt;= target; i++)
</code></pre>
<p>3.如果组合问题需考虑元素之间的顺序，需将 target 放在外循环，将 nums 放在内循环。</p>
<pre><code class="language-c++">for (int i = 1; i &lt;= target; i++)
    for (int num : nums)
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[synchronized 的 8 种使用案例分析]]></title>
        <id>https://hualiang.online/post/eight-use-cases-of-synchronized/</id>
        <link href="https://hualiang.online/post/eight-use-cases-of-synchronized/">
        </link>
        <updated>2023-12-18T08:46:35.000Z</updated>
        <summary type="html"><![CDATA[<p>你知道 synchronized 是怎么用的吗？😮</p>
]]></summary>
        <content type="html"><![CDATA[<p>你知道 synchronized 是怎么用的吗？😮</p>
<!-- more -->
<p>现在有一个 <code>HuaWei</code> 类，它有三个方法 <code>sendMail</code>，<code>sendSMS</code>，<code>hello</code></p>
<pre><code class="language-java">class HuaWei {
    
    public synchronized void sendMail() {
        System.out.println(Thread.currentThread().getName() + &quot;: send Mail&quot;);
    }

    public synchronized void sendSMS() {
        System.out.println(Thread.currentThread().getName() + &quot;: send SMS&quot;);
    }

    public void hello() {
        System.out.println(Thread.currentThread().getName() + &quot;: say hello&quot;);
    }

}
</code></pre>
<p>我们会在以下 8 种案例下对方法进行访问并分析原因：</p>
<ol>
<li>标准访问有 A，B 两个线程，请问先打印邮件还是短信？</li>
<li>sendEmail 方法中加入暂停 3 秒钟，请问先打印邮件还是短信？</li>
<li>添加一个普通的 hello 方法，请问先打印邮件还是 hello？</li>
<li>有 2 部手机，请问先打印邮件还是短信？</li>
<li>有两个静态同步方法，有 1 部手机，请问先打印邮件还是短信？</li>
<li>有两个静态同步方法，有 2 部手机，请问先打印邮件还是短信？</li>
<li>有 1 个静态同步方法，有 1 个普通同步方法，有 1 部手机，请问先打印邮件还是短信？</li>
<li>有 1 个静态同步方法，有 1 个普通同步方法，有 2 部手机，请问先打印邮件还是短信？</li>
</ol>
<hr>
<h2 id="标准访问有-ab-两个线程请问先打印邮件还是短信">标准访问有 A，B 两个线程，请问先打印邮件还是短信？</h2>
<pre><code class="language-java">public class MyTest {
    public static void main(String[] args) throws InterruptedException {
        HuaWei huaWei = new HuaWei();
        new Thread(() -&gt; {
            huaWei.sendMail();
        }, &quot;线程A&quot;).start();
        Thread.sleep(200); // 保证线程 A 先执行
        new Thread(() -&gt; {
            huaWei.sendSMS();
        }, &quot;线程B&quot;).start();
    }
}
</code></pre>
<h3 id="结果如下">结果如下：</h3>
<pre><code>线程A: send Mail
线程B: send SMS
</code></pre>
<h3 id="结果分析">结果分析：</h3>
<p>加在普通方法上的 <code>synchronized</code> 使用当前对象作为监视器，是对象锁，同一时刻仅允许单个线程访问同一个对象实例。这里线程 A 先启动抢占锁，线程 B 后启动等待。虽然访问方法不同，但是是同一对象，必须等待锁释放后 B 才能访问该对象实例，故 A 先一步完成并打印结果。</p>
<h2 id="sendemail-方法中加入暂停-3-秒钟请问先打印邮件还是短信">sendEmail 方法中加入暂停 3 秒钟，请问先打印邮件还是短信？</h2>
<pre><code class="language-java">class HuaWei {
    
    public synchronized void sendMail() {
        try {
            Thread.sleep(1000);
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
        System.out.println(Thread.currentThread().getName() + &quot;: send Mail&quot;);
    }

    public synchronized void sendSMS() {
        System.out.println(Thread.currentThread().getName() + &quot;: send SMS&quot;);
    }

    public void hello() {
        System.out.println(Thread.currentThread().getName() + &quot;: say hello&quot;);
    }

}
</code></pre>
<h3 id="结果如下-2">结果如下：</h3>
<pre><code>线程A: send Mail
线程B: send SMS
</code></pre>
<h3 id="结果分析-2">结果分析：</h3>
<p>虽然 A 睡了 1 秒，但 <code>Thread.sleep()</code> 方法不会使线程释放锁，因此 B 依旧无法拿到锁被迫等待，A 醒来后执行完毕并释放锁，B 拿到锁后才能执行。</p>
<h2 id="添加一个普通的-hello-方法请问先打印邮件还是-hello">添加一个普通的 hello 方法，请问先打印邮件还是 hello？</h2>
<p><code>HuaWei</code> 类不变，<code>MyTest</code> 如下：</p>
<pre><code class="language-java">public class MyTest {
    public static void main(String[] args) throws InterruptedException {
        HuaWei huaWei = new HuaWei();
        new Thread(() -&gt; {
            huaWei.sendMail();
        }, &quot;线程A&quot;).start();
        Thread.sleep(200);
        new Thread(() -&gt; {
            huaWei.hello(); // 调用 hello()
        }, &quot;线程B&quot;).start();
    }
}
</code></pre>
<h3 id="结果如下-3">结果如下：</h3>
<pre><code>线程B: say hello
线程A: send Mail
</code></pre>
<h3 id="结果分析-3">结果分析：</h3>
<p>加在普通方法上的 <code>synchronized</code> 对象锁，仅会锁住对象实例上的所有同步方法，与普通方法不产生互斥。这里 A 先启动抢占锁后睡眠，B 后启动访问普通方法 <code>hello()</code> 不会产生互斥，无需等待锁释放即可执行完毕，因此 B 先一步打印结果。</p>
<h2 id="有-2-部手机请问先打印邮件还是短信">有 2 部手机，请问先打印邮件还是短信？</h2>
<p><code>HuaWei</code> 类不变，<code>MyTest</code> 如下：</p>
<pre><code class="language-java">public class MyTest {
    public static void main(String[] args) throws InterruptedException {
        new Thread(() -&gt; {
            new HuaWei().sendMail();
        }, &quot;线程A&quot;).start();
        Thread.sleep(200);
        new Thread(() -&gt; {
            new HuaWei().sendSMS();
        }, &quot;线程B&quot;).start();
    }
</code></pre>
<h3 id="结果如下-4">结果如下：</h3>
<pre><code>线程B: say hello
线程A: send Mail
</code></pre>
<h3 id="结果分析-4">结果分析：</h3>
<p>对象锁只锁同一对象实例，不同对象之间的锁不相同。B 无需等待 A 的锁释放就能拿到自己对象的锁并执行方法，两者拿到的是不同的锁。又因为 A 要睡 1 秒，所以 B 先打印结果。</p>
<h2 id="有两个静态同步方法有-1-部手机请问先打印邮件还是短信">有两个静态同步方法，有 1 部手机，请问先打印邮件还是短信？</h2>
<pre><code class="language-java">package fun.hualiang.test;

class HuaWei {
    
    public static synchronized void sendMail() {
        try {
            Thread.sleep(1000);
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
        System.out.println(Thread.currentThread().getName() + &quot;: send Mail&quot;);
    }

    public static synchronized void sendSMS() {
        System.out.println(Thread.currentThread().getName() + &quot;: send SMS&quot;);
    }

    public void hello() {
        System.out.println(Thread.currentThread().getName() + &quot;: say hello&quot;);
    }

}

public class MyTest {
    public static void main(String[] args) throws InterruptedException {
        HuaWei huaWei = new HuaWei();
        new Thread(() -&gt; {
            huaWei.sendMail();
        }, &quot;线程A&quot;).start();
        Thread.sleep(200);
        new Thread(() -&gt; {
            huaWei.sendSMS();
        }, &quot;线程B&quot;).start();
    }
}
</code></pre>
<h3 id="结果如下-5">结果如下：</h3>
<pre><code>线程A: send Mail
线程B: send SMS
</code></pre>
<h3 id="结果分析-5">结果分析：</h3>
<p>加在静态方法上的 <code>synchronized</code> 使用当前类作为监视器，是类锁，同一时刻仅允许单个线程访问同一个类。这里的原因跟情况二类似，区别在于情况二拿到的是对象锁，这里拿到的是类锁。</p>
<h2 id="有两个静态同步方法有-2-部手机请问先打印邮件还是短信">有两个静态同步方法，有 2 部手机，请问先打印邮件还是短信？</h2>
<p><code>HuaWei</code> 类不变，<code>MyTest</code> 如下：</p>
<pre><code class="language-java">public class MyTest {
    public static void main(String[] args) throws InterruptedException {
        new Thread(() -&gt; {
            new HuaWei().sendMail();
        }, &quot;线程A&quot;).start();
        Thread.sleep(200);
        new Thread(() -&gt; {
            new HuaWei().sendSMS();
        }, &quot;线程B&quot;).start();
    }
}
</code></pre>
<h3 id="结果如下-6">结果如下：</h3>
<pre><code>线程A: send Mail
线程B: send SMS
</code></pre>
<h3 id="结果分析-6">结果分析：</h3>
<p>这种情况与情况四的区别在于，情况四中 A 和 B 分别拿到的是不同对象上的对象锁，不会产生互斥。而这里因为静态方法是属于类的，所以 A 和 B 拿到的是同一个类的类锁，既然是同一把锁就会产生互斥。A 先启动拿到锁，B 就只能等 A 执行完释放锁后在执行，故 A 先一步打印结果。</p>
<h2 id="有-1-个静态同步方法有-1-个普通同步方法有-1-部手机请问先打印邮件还是短信">有 1 个静态同步方法，有 1 个普通同步方法，有 1 部手机，请问先打印邮件还是短信？</h2>
<pre><code class="language-java">package fun.hualiang.test;

class HuaWei {
    
    public static synchronized void sendMail() {
        try {
            Thread.sleep(1000);
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
        System.out.println(Thread.currentThread().getName() + &quot;: send Mail&quot;);
    }

    public synchronized void sendSMS() {
        System.out.println(Thread.currentThread().getName() + &quot;: send SMS&quot;);
    }

    public void hello() {
        System.out.println(Thread.currentThread().getName() + &quot;: say hello&quot;);
    }

}

public class MyTest {
    public static void main(String[] args) throws InterruptedException {
        HuaWei huaWei = new HuaWei();
        new Thread(() -&gt; {
            huaWei.sendMail();
        }, &quot;线程A&quot;).start();
        Thread.sleep(200);
        new Thread(() -&gt; {
            huaWei.sendSMS();
        }, &quot;线程B&quot;).start();
    }
}
</code></pre>
<h3 id="结果如下-7">结果如下：</h3>
<pre><code>线程B: send SMS
线程A: send Mail
</code></pre>
<h3 id="结果分析-7">结果分析：</h3>
<p>B 拿的是对象锁，A 拿的是类锁，不同的锁不会发生互斥，因此结果与情况四类似，都是拿到不同的锁。</p>
<h2 id="有-1-个静态同步方法有-1-个普通同步方法有-2-部手机请问先打印邮件还是短信">有 1 个静态同步方法，有 1 个普通同步方法，有 2 部手机，请问先打印邮件还是短信？</h2>
<p><code>HuaWei</code> 类不变，<code>MyTest</code> 如下：</p>
<pre><code class="language-java">public class MyTest {
    public static void main(String[] args) throws InterruptedException {
        new Thread(() -&gt; {
            new HuaWei().sendMail();
        }, &quot;线程A&quot;).start();
        Thread.sleep(200);
        new Thread(() -&gt; {
            new HuaWei().sendSMS();
        }, &quot;线程B&quot;).start();
    }
}
</code></pre>
<h3 id="结果如下-8">结果如下：</h3>
<pre><code>线程B: send SMS
线程A: send Mail
</code></pre>
<h3 id="结果分析-8">结果分析：</h3>
<p>与情况七一致，即使是不同对象，也不影响 B 拿的是对象锁，A 拿的是类锁，因此结果一致。</p>
<hr>
<h2 id="总结">总结</h2>
<p>综上所述，多线程是否同步的关键在于：<strong>它们是否拿到的是同一把锁</strong>。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[23 种设计模式（Design Patterns）]]></title>
        <id>https://hualiang.online/post/23-design-patterns/</id>
        <link href="https://hualiang.online/post/23-design-patterns/">
        </link>
        <updated>2023-11-25T08:47:11.000Z</updated>
        <summary type="html"><![CDATA[<p>可复用面向对象软件的基础</p>
]]></summary>
        <content type="html"><![CDATA[<p>可复用面向对象软件的基础</p>
<!-- more -->
<p>设计模式是一套被反复使用.多数人知晓的.经过分类编目的.代码设计经验的总结。使用设计模式是为了可重用代码.让代码更容易被他人理解.保证代码可靠性。 毫无疑问，设计模式于己于他人于系统都是多赢的，设计模式使代码编制真正工程化，设计模式是软件工程的基石，如同大厦的一块块砖石一样。项目中合理的运用设计模式可以完美的解决很多问题，每种模式在现在中都有相应的原理来与之对应，每一个模式描述了一个在我们周围不断重复发生的问题，以及该问题的核心解决方案，这也是它能被广泛应用的原因。</p>
<h2 id="一-设计模式的分类">一、设计模式的分类</h2>
<p>总体来说设计模式分为三大类：</p>
<p>创建型模式，共五种：工厂模式.抽象工厂模式.单例模式.建造者模式.原型模式。</p>
<p>结构型模式，共七种：适配器模式.装饰器模式.代理模式.外观模式.桥接模式.组合模式.享元模式。</p>
<p>行为型模式，共十一种：策略模式.模板方法模式.观察者模式.迭代子模式.责任链模式.命令模式.备忘录模式.状态模式.访问者模式.中介者模式.解释器模式。</p>
<p>其实还有两类：并发型模式和线程池模式。用一个图片来整体描述一下：</p>
<h2 id="二-设计模式的六大原则">二、设计模式的六大原则</h2>
<h3 id="1开闭原则">1.开闭原则</h3>
<p>开闭原则（Open Close Principle）就是说对扩展开放，对修改关闭。在程序需要进行拓展的时候，不能去修改原有的代码，实现一个热插拔的效果。所以一句话概括就是：为了使程序的扩展性好，易于维护和升级。想要达到这样的效果就需要使用接口和抽象类，后面的具体设计中我们会提到这点。</p>
<h3 id="2里氏代换原则">2.里氏代换原则</h3>
<p>里氏代换原则（Liskov Substitution Principle）面向对象设计的基本原则之一。 里氏代换原则中说，任何基类可以出现的地方，子类一定可以出现。 LSP是继承复用的基石，只有当衍生类可以替换掉基类，软件单位的功能不受到影响时，基类才能真正被复用，而衍生类也能够在基类的基础上增加新的行为。里氏代换原则是对“开-闭”原则的补充。实现“开-闭”原则的关键步骤就是抽象化。而基类与子类的继承关系就是抽象化的具体实现，所以里氏代换原则是对实现抽象化的具体步骤的规范。—— From Baidu 百科</p>
<h3 id="3依赖倒转原则">3.依赖倒转原则</h3>
<p>依赖倒转原则（Dependence Inversion Principle）是开闭原则的基础，具体内容：真对接口编程，依赖于抽象而不依赖于具体。</p>
<h3 id="4接口隔离原则">4.接口隔离原则</h3>
<p>接口隔离原则（Interface Segregation Principle）的意思是：使用多个隔离的接口，比使用单个接口要好。还是一个降低类之间的耦合度的意思，从这儿我们看出，其实设计模式就是一个软件的设计思想，从大型软件架构出发，为了升级和维护方便。所以上文中多次出现：降低依赖，降低耦合。</p>
<h3 id="5迪米特法则">5.迪米特法则</h3>
<p>迪米特法则（Demeter Principle）也叫最少知道原则，至于为什么，就是说：一个实体应当尽量少的与其他实体之间发生相互作用，使得系统功能模块相对独立。</p>
<h3 id="6合成复用原则">6.合成复用原则</h3>
<p>合成复用原则（Composite Reuse Principle）是尽量使用合成/聚合的方式，而不是使用继承。</p>
<h2 id="三-java-中的设计模式">三、Java 中的设计模式</h2>
<p>从这一块开始，我们详细介绍 Java 中 23 种设计模式的概念，应用场景等情况，并结合他们的特点及设计模式的原则进行分析。</p>
<h3 id="1工厂模式">1.工厂模式</h3>
<p>工厂模式分为三种：</p>
<ul>
<li>普通工厂模式</li>
<li>多工厂模式</li>
<li>静态工厂模式</li>
</ul>
<p><strong>普通工厂模式</strong>就是建立一个工厂类，对实现了同一接口的一些类进行实例的创建。</p>
<p>首先我们举一个发送邮件和短信的例子，创建二者的共同接口：</p>
<pre><code class="language-java">public interface Sender {
	public void Send();
}
</code></pre>
<p>其次，创建实现类：</p>
<pre><code class="language-java">public class MailSender implements Sender {
	@Override
	public void Send() {
		System.out.println(&quot;this is mailsender!&quot;);
	}
}
public class SmsSender implements Sender {

	@Override
	public void Send() {
		System.out.println(&quot;this is sms sender!&quot;);
	}
}
</code></pre>
<p>最后，建工厂类：</p>
<pre><code class="language-java">public class SendFactory {

	public Sender produce(String type) {
		if (&quot;mail&quot;.equals(type)) {
			return new MailSender();
		} else if (&quot;sms&quot;.equals(type)) {
			return new SmsSender();
		} else {
			System.out.println(&quot;请输入正确的类型!&quot;);
			return null;
		}
	}
}
</code></pre>
<p>我们来测试下：</p>
<pre><code class="language-java">public class FactoryTest {

	public static void main(String[] args) {
		SendFactory factory = new SendFactory();
		Sender sender = factory.produce(&quot;sms&quot;);
		sender.Send();
	}
}
</code></pre>
<pre><code>输出：this is sms sender!
</code></pre>
<p><strong>多工厂模式</strong>是对普通工厂模式的改进。</p>
<p>在普通工厂模式中，如果传递的字符串出错，则不能正确创建对象，而多个工厂模式是提供多个工厂，分别创建对象。</p>
<p>将上面的代码做下修改，改动下SendFactory类就行，如下：</p>
<pre><code class="language-java">public class SendFactory {
	
	public Sender produceMail(){
		return new MailSender();
	}
	
	public Sender produceSms(){
		return new SmsSender();
	}
}
</code></pre>
<p>测试类如下：</p>
<pre><code class="language-java">public class FactoryTest {

	public static void main(String[] args) {
		SendFactory factory = new SendFactory();
		Sender sender = factory.produceMail();
		sender.Send();
	}
}
</code></pre>
<p>结果如下：</p>
<pre><code>this is mailsender!
</code></pre>
<p><strong>静态工厂模式</strong>就是将上面的多个工厂模式里的方法置为静态的，不需要创建实例，直接调用即可。</p>
<pre><code class="language-java">public class SendFactory {
	
	public static Sender produceMail(){
		return new MailSender();
	}
	
	public static Sender produceSms(){
		return new SmsSender();
	}
}
public class FactoryTest {

	public static void main(String[] args) {	
		Sender sender = SendFactory.produceMail();
		sender.Send();
	}
}
</code></pre>
<p>结果如下：</p>
<pre><code>this is mailsender!
</code></pre>
<p>总体来说，工厂模式适合：凡是出现了大量的产品需要创建，并且具有共同的接口时，可以通过工厂模式进行创建。在以上的三种模式中，第一种如果传入的字符串有误，不能正确创建对象，第三种相对于第二种，不需要实例化工厂类，所以，大多数情况下，我们会选用第三种——静态工厂模式。</p>
<h3 id="2抽象工厂模式">2.抽象工厂模式</h3>
<p>工厂模式有一个问题就是，类的创建依赖工厂类，也就是说，如果想要拓展程序，必须对工厂类进行修改，这违背了闭包原则，所以，从设计角度考虑，有一定的问题，如何解决？就用到抽象工厂模式，创建多个工厂类，这样一旦需要增加新的功能，直接增加新的工厂类就可以了，不需要修改之前的代码。</p>
<p>请看例子：</p>
<pre><code class="language-java">public interface Sender {
	public void Send();
}
</code></pre>
<p>两个实现类：</p>
<pre><code class="language-java">public class MailSender implements Sender {
	@Override
	public void Send() {
		System.out.println(&quot;this is mailsender!&quot;);
	}
}
public class SmsSender implements Sender {

	@Override
	public void Send() {
		System.out.println(&quot;this is sms sender!&quot;);
	}
}
</code></pre>
<p>两个工厂类：</p>
<pre><code class="language-java">public class SendMailFactory implements Provider {
	
	@Override
	public Sender produce(){
		return new MailSender();
	}
}
public class SendSmsFactory implements Provider{

	@Override
	public Sender produce() {
		return new SmsSender();
	}
}
</code></pre>
<p>再提供一个接口：</p>
<pre><code class="language-java">public interface Provider {
	public Sender produce();
}
</code></pre>
<p>测试类：</p>
<pre><code class="language-java">public class Test {

	public static void main(String[] args) {
		Provider provider = new SendMailFactory();
		Sender sender = provider.produce();
		sender.Send();
	}
}
</code></pre>
<p>其实这个模式的好处就是，如果你现在想增加一个功能：发及时信息，则只需做一个实现类，实现Sender接口，同时做一个工厂类，实现Provider接口，就OK了，无需去改动现成的代码。这样做，拓展性较好！</p>
<h3 id="3单例模式">3.单例模式</h3>
<p>单例对象（Singleton）是一种常用的设计模式。在Java应用中，单例对象能保证在一个JVM中，该对象只有一个实例存在。这样的模式有几个好处：</p>
<ul>
<li>
<p>某些类创建比较频繁，对于一些大型的对象，这是一笔很大的系统开销。</p>
</li>
<li>
<p>省去了new操作符，降低了系统内存的使用频率，减轻GC压力。</p>
</li>
<li>
<p>有些类如交易所的核心交易引擎，控制着交易流程，如果该类可以创建多个的话，系统完全乱了。（比如一个军队出现了多个司令员同时指挥，肯定会乱成一团），所以只有使用单例模式，才能保证核心交易服务器独立控制整个流程。</p>
</li>
</ul>
<p>首先我们写一个简单的单例类：</p>
<pre><code class="language-java">public class Singleton {

	/* 持有私有静态实例，防止被引用，此处赋值为null，目的是实现延迟加载 */
	private static Singleton instance = null;
	 
	/* 私有构造方法，防止被实例化 */
	private Singleton() {
	}
	 
	/* 静态工程方法，创建实例 */
	public static Singleton getInstance() {
		if (instance == null) {
			instance = new Singleton();
		}
		return instance;
	}
	 
	/* 如果该对象被用于序列化，可以保证对象在序列化前后保持一致 */
	public Object readResolve() {
		return instance;
	}
}
</code></pre>
<p>这个类可以满足基本要求，但是，像这样毫无线程安全保护的类，如果我们把它放入多线程的环境下，肯定就会出现问题了，如何解决？我们首先会想到对 getInstance 方法加 synchronized 关键字，如下：</p>
<pre><code class="language-java">public static synchronized Singleton getInstance() {
	if (instance == null) {
		instance = new Singleton();
	}
	return instance;
}
</code></pre>
<p>但是，synchronized 关键字锁住的是这个对象，这样的用法，在性能上会有所下降，因为每次调用 getInstance()，都要对对象上锁，事实上，只有在第一次创建对象的时候需要加锁，之后就不需要了，所以，这个地方需要改进。我们改成下面这个：</p>
<pre><code class="language-java">public static Singleton getInstance() {
	if (instance == null) {
		synchronized (instance) {
			if (instance == null) {
				instance = new Singleton();
			}
		}
	}
	return instance;
}
</code></pre>
<p>似乎解决了之前提到的问题，将 synchronized 关键字加在了内部，也就是说当调用的时候是不需要加锁的，只有在 instance 为 null，并创建对象的时候才需要加锁，性能有一定的提升。但是这样的情况，还是有可能有问题的。如下情况：</p>
<p>在 Java 指令中创建对象和赋值操作是分开进行的，也就是说 instance = new Singleton(); 语句是分两步执行的。但是JVM并不保证这两个操作的先后顺序，也就是说有可能JVM会为新的 Singleton 实例分配空间，然后直接赋值给 instance 成员，然后再去初始化这个Singleton 实例。这样就可能出错了，我们以A, B两个线程为例：</p>
<ol>
<li>
<p>A.B线程同时进入了第一个if判断</p>
</li>
<li>
<p>A首先进入 synchronized 块，由于 instance 为 null，所以它执行 instance = new Singleton();</p>
</li>
<li>
<p>由于JVM内部的优化机制，JVM先画出了一些分配给 Singleton 实例的空白内存，并赋值给 instance 成员（注意此时JVM没有开始初始化这个实例），然后A离开了 synchronized 块。</p>
</li>
<li>
<p>B进入 synchronized 块，由于 instance 此时不是 null，因此它马上离开了 synchronized 块并将结果返回给调用该方法的程序。</p>
</li>
<li>
<p>此时B线程打算使用 Singleton 实例，却发现它没有被初始化，于是错误发生了。</p>
</li>
</ol>
<p>所以程序还是有可能发生错误，其实程序在运行过程是很复杂的，从这点我们就可以看出，尤其是在写多线程环境下的程序更有难度，有挑战性。我们对该程序做进一步优化：</p>
<pre><code class="language-java">private static class SingletonFactory{         
    private static Singleton instance = new Singleton();         
}         

public static Singleton getInstance(){         
    return SingletonFactory.instance;         
} 
</code></pre>
<p>实际情况是，单例模式使用内部类来维护单例的实现，JVM内部的机制能够保证当一个类被加载的时候，这个类的加载过程是线程互斥的。这样当我们第一次调用 getInstance 的时候，JVM能够帮我们保证 instance 只被创建一次，并且会保证把赋值给 instance 的内存初始化完毕，这样我们就不用担心上面的问题。同时该方法也只会在第一次调用的时候使用互斥机制，这样就解决了低性能问题。这样我们暂时总结一个完美的单例模式：</p>
<pre><code class="language-java">public class Singleton {

	/* 私有构造方法，防止被实例化 */
	private Singleton() {
	}
	 
	/* 此处使用一个内部类来维护单例 */
	private static class SingletonFactory {
		private static Singleton instance = new Singleton();
	}
	 
	/* 获取实例 */
	public static Singleton getInstance() {
		return SingletonFactory.instance;
	}
	 
	/* 如果该对象被用于序列化，可以保证对象在序列化前后保持一致 */
	public Object readResolve() {
		return getInstance();
	}
}
</code></pre>
<p>其实说它完美，也不一定，如果在构造函数中抛出异常，实例将永远得不到创建，也会出错。所以说，十分完美的东西是没有的，我们只能根据实际情况，选择最适合自己应用场景的实现方法。也有人这样实现：因为我们只需要在创建类的时候进行同步，所以只要将创建和getInstance() 分开，单独为创建加 synchronized 关键字，也是可以的：</p>
<pre><code class="language-java">public class SingletonTest {

	private static SingletonTest instance = null;
	 
	private SingletonTest() {
	}
	 
	private static synchronized void syncInit() {
		if (instance == null) {
			instance = new SingletonTest();
		}
	}
	 
	public static SingletonTest getInstance() {
		if (instance == null) {
			syncInit();
		}
		return instance;
	}
}
</code></pre>
<p>考虑性能的话，整个程序只需创建一次实例，所以性能也不会有什么影响。</p>
<p>补充：采用&quot;影子实例&quot;的办法为单例对象的属性同步更新</p>
<pre><code class="language-java">public class SingletonTest {

	private static SingletonTest instance = null;
	private Vector properties = null;
	 
	public Vector getProperties() {
		return properties;
	}
	 
	private SingletonTest() {
	}
	 
	private static synchronized void syncInit() {
		if (instance == null) {
			instance = new SingletonTest();
		}
	}
	 
	public static SingletonTest getInstance() {
		if (instance == null) {
			syncInit();
		}
		return instance;
	}
	 
	public void updateProperties() {
		SingletonTest shadow = new SingletonTest();
		properties = shadow.getProperties();
	}
}
</code></pre>
<p>通过单例模式的学习告诉我们：</p>
<ul>
<li>
<p>单例模式理解起来简单，但是具体实现起来还是有一定的难度。</p>
</li>
<li>
<p>synchronized关键字锁定的是对象，在用的时候，一定要在恰当的地方使用（注意需要使用锁的对象和过程，可能有的时候并不是整个对象及整个过程都需要锁）。</p>
</li>
</ul>
<p>到这儿，单例模式基本已经讲完了，结尾处，笔者突然想到另一个问题，就是采用类的静态方法，实现单例模式的效果，也是可行的，此处二者有什么不同？</p>
<ul>
<li>
<p>首先，静态类不能实现接口（从类的角度说是可以的，但是那样就破坏了静态了。因为接口中不允许有static修饰的方法，所以即使实现了也是非静态的）</p>
</li>
<li>
<p>其次，单例可以被延迟初始化，静态类一般在第一次加载是初始化。之所以延迟加载，是因为有些类比较庞大，所以延迟加载有助于提升性能。</p>
</li>
<li>
<p>再次，单例类可以被继承，他的方法可以被覆写。但是静态类内部方法都是static，无法被覆写。</p>
</li>
<li>
<p>最后，单例类比较灵活，毕竟从实现上只是一个普通的Java类，只要满足单例的基本需求，你可以在里面随心所欲的实现一些其它功能，但是静态类不行。从上面这些概括中，基本可以看出二者的区别，但是，从另一方面讲，我们上面最后实现的那个单例模式，内部就是用一个静态类来实现的，所以，二者有很大的关联，只是我们考虑问题的层面不同罢了。两种思想的结合，才能造就出完美的解决方案，就像HashMap采用数组+链表来实现一样，其实生活中很多事情都是这样，单用不同的方法来处理问题，总是有优点也有缺点，最完美的方法是，结合各个方法的优点，才能最好的解决问题！</p>
</li>
</ul>
<h3 id="4建造者模式">4.建造者模式</h3>
<p>工厂类模式提供的是创建单个类的模式，而建造者模式则是将各种产品集中起来进行管理，用来创建复合对象，所谓复合对象就是指某个类具有不同的属性，其实建造者模式就是前面抽象工厂模式和最后的Test结合起来得到的。我们看一下代码：</p>
<p>还和前面一样，一个 Sender 接口，两个实现类 MailSender 和 SmsSender。最后，建造者类如下：</p>
<pre><code class="language-java">public class Builder {
	
	private List&lt;Sender&gt; list = new ArrayList&lt;Sender&gt;();
	
	public void produceMailSender(int count){
		for(int i=0; i&lt;count; i++){
			list.add(new MailSender());
		}
	}
	
	public void produceSmsSender(int count){
		for(int i=0; i&lt;count; i++){
			list.add(new SmsSender());
		}
	}
}
</code></pre>
<p>测试类：</p>
<pre><code class="language-java">public class Test {

	public static void main(String[] args) {
		Builder builder = new Builder();
		builder.produceMailSender(10);
	}
}
</code></pre>
<p>从这点看出，建造者模式将很多功能集成到一个类里，这个类可以创造出比较复杂的东西。所以与工程模式的区别就是：工厂模式关注的是创建单个产品，而建造者模式则关注创建符合对象，多个部分。因此，是选择工厂模式还是建造者模式，依实际情况而定。</p>
<h3 id="5原型模式">5.原型模式</h3>
<p>原型模式虽然是创建型的模式，但是与工程模式没有关系，从名字即可看出，该模式的思想就是将一个对象作为原型，对其进行复制.克隆，产生一个和原对象类似的新对象。本小结会通过对象的复制，进行讲解。在Java中，复制对象是通过clone()实现的，先创建一个原型类：</p>
<pre><code class="language-java">public class Prototype implements Cloneable {

	public Object clone() throws CloneNotSupportedException {
		Prototype proto = (Prototype) super.clone();
		return proto;
	}
}
</code></pre>
<p>很简单，一个原型类，只需要实现Cloneable接口，覆写clone方法，此处clone方法可以改成任意的名称，因为Cloneable接口是个空接口，你可以任意定义实现类的方法名，如cloneA或者cloneB，因为此处的重点是super.clone()这句话，super.clone()调用的是Object的clone()方法，而在Object类中，clone()是native的，具体怎么实现，我会在另一篇文章中，关于解读Java中本地方法的调用，此处不再深究。</p>
<p>在这儿，我将结合对象的浅复制和深复制来说一下，首先需要了解对象深，浅复制的概念：</p>
<ul>
<li><strong>浅复制</strong>：将一个对象复制后，基本数据类型的变量都会重新创建，而引用类型，指向的还是原对象所指向的。</li>
<li><strong>深复制</strong>：将一个对象复制后，不论是基本数据类型还有引用类型，都是重新创建的。简单来说，就是深复制进行了完全彻底的复制，而浅复制不彻底。</li>
</ul>
<p>此处，写一个深浅复制的例子：</p>
<pre><code class="language-java">public class Prototype implements Cloneable, Serializable {

	private static final long serialVersionUID = 1L;
	private String string;
	 
	private SerializableObject obj;
	 
	/* 浅复制 */
	public Object clone() throws CloneNotSupportedException {
		Prototype proto = (Prototype) super.clone();
		return proto;
	}
	 
	/* 深复制 */
	public Object deepClone() throws IOException, ClassNotFoundException {
	 
		/* 写入当前对象的二进制流 */
		ByteArrayOutputStream bos = new ByteArrayOutputStream();
		ObjectOutputStream oos = new ObjectOutputStream(bos);
		oos.writeObject(this);
	 
		/* 读出二进制流产生的新对象 */
		ByteArrayInputStream bis = new ByteArrayInputStream(bos.toByteArray());
		ObjectInputStream ois = new ObjectInputStream(bis);
		return ois.readObject();
	}
	 
	public String getString() {
		return string;
	}
	 
	public void setString(String string) {
		this.string = string;
	}
	 
	public SerializableObject getObj() {
		return obj;
	}
	 
	public void setObj(SerializableObject obj) {
		this.obj = obj;
	}

}

class SerializableObject implements Serializable {
	private static final long serialVersionUID = 1L;
}
</code></pre>
<p>要实现深复制，需要采用流的形式读入当前对象的二进制输入，再写出二进制数据对应的对象。</p>
<h3 id="6适配器模式">6.适配器模式</h3>
<p>适配器模式将某个类的接口转换成客户端期望的另一个接口表示，目的是消除由于接口不匹配所造成的类的兼容性问题。主要分为三类：类的适配器模式、对象的适配器模式、接口的适配器模式。</p>
<p>核心思想就是：有一个 Source 类，拥有一个方法，待适配，目标接口时 Targetable，通过 Adapter 类，将 Source 的功能扩展到Targetable 里，看代码：</p>
<pre><code class="language-java">public class Source {

	public void method1() {
		System.out.println(&quot;this is original method!&quot;);
	}
}
public interface Targetable {

	/* 与原类中的方法相同 */
	public void method1();
	 
	/* 新类的方法 */
	public void method2();

	public class Adapter extends Source implements Targetable {

		@Override
		public void method2() {
			System.out.println(&quot;this is the targetable method!&quot;);
		}
	}
}
</code></pre>
<p>Adapter 类继承 Source 类，实现 Targetable 接口，下面是测试类：</p>
<pre><code class="language-java">public class AdapterTest {

	public static void main(String[] args) {
		Targetable target = new Adapter();
		target.method1();
		target.method2();
	}
}
</code></pre>
<p>结果如下：</p>
<pre><code>this is original method!
this is the targetable method!
</code></pre>
<p>这样 Targetable 接口的实现类就具有了 Source 类的功能。</p>
<p>对象的适配器模式</p>
<p>基本思路和类的适配器模式相同，只是将Adapter类作修改，这次不继承Source类，而是持有Source类的实例，以达到解决兼容性的问题。看图：</p>
<p>只需要修改 Adapter 类的源码即可：</p>
<pre><code class="language-java">public class Wrapper implements Targetable {

	private Source source;
	
	public Wrapper(Source source){
		super();
		this.source = source;
	}
	@Override
	public void method2() {
		System.out.println(&quot;this is the targetable method!&quot;);
	}
	 
	@Override
	public void method1() {
		source.method1();
	}
}
</code></pre>
<p>测试类：</p>
<pre><code class="language-java">public class AdapterTest {

	public static void main(String[] args) {
		Source source = new Source();
		Targetable target = new Wrapper(source);
		target.method1();
		target.method2();
	}
}
</code></pre>
<p>输出与第一种一样，只是适配的方法不同而已。</p>
<p>第三种适配器模式是接口的适配器模式，接口的适配器是这样的：有时我们写的一个接口中有多个抽象方法，当我们写该接口的实现类时，必须实现该接口的所有方法，这明显有时比较浪费，因为并不是所有的方法都是我们需要的，有时只需要某一些，此处为了解决这个问题，我们引入了接口的适配器模式，借助于一个抽象类，该抽象类实现了该接口，实现了所有的方法，而我们不和原始的接口打交道，只和该抽象类取得联系，所以我们写一个类，继承该抽象类，重写我们需要的方法就行。</p>
<p>这个很好理解，在实际开发中，我们也常会遇到这种接口中定义了太多的方法，以致于有时我们在一些实现类中并不是都需要。看代码：</p>
<pre><code class="language-Java">public interface Sourceable {
	
	public void method1();
	public void method2();
}
</code></pre>
<p>抽象类 Wrapper2：</p>
<pre><code class="language-java">public abstract class Wrapper2 implements Sourceable {
	
	public void method1(){}
	public void method2(){}
}

public class SourceSub1 extends Wrapper2 {
	public void method1(){
		System.out.println(&quot;the sourceable interface's first Sub1!&quot;);
	}
}
public class SourceSub2 extends Wrapper2 {
	public void method2(){
		System.out.println(&quot;the sourceable interface's second Sub2!&quot;);
	}
}
public class WrapperTest {

	public static void main(String[] args) {
		Sourceable source1 = new SourceSub1();
		Sourceable source2 = new SourceSub2();
		
		source1.method1();
		source1.method2();
		source2.method1();
		source2.method2();
	}
}
</code></pre>
<p>测试输出：</p>
<pre><code>the sourceable interface's first Sub1!
the sourceable interface's second Sub2!
</code></pre>
<p>达到了我们的效果！</p>
<p>讲了这么多，总结一下三种适配器模式的应用场景：</p>
<ul>
<li><strong>类的适配器模式</strong>：当希望将一个类转换成满足另一个新接口的类时，可以使用类的适配器模式，创建一个新类，继承原有的类，实现新的接口即可。</li>
<li><strong>对象的适配器模式</strong>：当希望将一个对象转换成满足另一个新接口的对象时，可以创建一个Wrapper类，持有原类的一个实例，在Wrapper类的方法中，调用实例的方法就行。</li>
<li><strong>接口的适配器模式</strong>：当不希望实现一个接口中所有的方法时，可以创建一个抽象类Wrapper，实现所有方法，我们写别的类的时候，继承抽象类即可。</li>
</ul>
<h3 id="7装饰器模式">7.装饰器模式</h3>
<p>顾名思义，装饰模式就是给一个对象增加一些新的功能，而且是动态的，要求装饰对象和被装饰对象实现同一个接口，装饰对象持有被装饰对象的实例，关系图如下：</p>
<p>Source 类是被装饰类，Decorator 类是一个装饰类，可以为 Source 类动态的添加一些功能，代码如下：</p>
<pre><code class="language-java">public interface Sourceable {
	public void method();
}
public class Source implements Sourceable {

	@Override
	public void method() {
		System.out.println(&quot;the original method!&quot;);
	}
}
public class Decorator implements Sourceable {

	private Sourceable source;
	
	public Decorator(Sourceable source){
		super();
		this.source = source;
	}
	@Override
	public void method() {
		System.out.println(&quot;before decorator!&quot;);
		source.method();
		System.out.println(&quot;after decorator!&quot;);
	}
}
</code></pre>
<p>测试类：</p>
<pre><code class="language-java">public class DecoratorTest {

	public static void main(String[] args) {
		Sourceable source = new Source();
		Sourceable obj = new Decorator(source);
		obj.method();
	}
}
</code></pre>
<p>输出：</p>
<pre><code>before decorator!
the original method!
after decorator!
</code></pre>
<p>装饰器模式的应用场景：</p>
<ul>
<li>
<p>需要扩展一个类的功能。</p>
</li>
<li>
<p>动态的为一个对象增加功能，而且还能动态撤销。（继承不能做到这一点，继承的功能是静态的，不能动态增删。）</p>
</li>
</ul>
<p><strong>缺点</strong>：产生过多相似的对象，不易排错！</p>
<h3 id="8代理模式">8.代理模式</h3>
<p>其实每个模式名称就表明了该模式的作用，代理模式就是多一个代理类出来，替原对象进行一些操作，比如我们在租房子的时候回去找中介，为什么呢？因为你对该地区房屋的信息掌握的不够全面，希望找一个更熟悉的人去帮你做，此处的代理就是这个意思。再如我们有的时候打官司，我们需要请律师，因为律师在法律方面有专长，可以替我们进行操作，表达我们的想法。</p>
<p>根据上文的阐述，代理模式就比较容易的理解了，我们看下代码：</p>
<pre><code class="language-java">public interface Sourceable {
	public void method();
}
public class Source implements Sourceable {

	@Override
	public void method() {
		System.out.println(&quot;the original method!&quot;);
	}
}
public class Proxy implements Sourceable {

	private Source source;
	public Proxy(){
		super();
		this.source = new Source();
	}
	@Override
	public void method() {
		before();
		source.method();
		atfer();
	}
	private void atfer() {
		System.out.println(&quot;after proxy!&quot;);
	}
	private void before() {
		System.out.println(&quot;before proxy!&quot;);
	}
}
</code></pre>
<p>测试类：</p>
<pre><code class="language-java">public class ProxyTest {

	public static void main(String[] args) {
		Sourceable source = new Proxy();
		source.method();
	}

}
</code></pre>
<p>输出：</p>
<pre><code>before proxy!
the original method!
after proxy!
</code></pre>
<p>代理模式的应用场景：</p>
<p>如果已有的方法在使用的时候需要对原有的方法进行改进，此时有两种办法：</p>
<ul>
<li>
<p>修改原有的方法来适应。这样违反了“对扩展开放，对修改关闭”的原则。</p>
</li>
<li>
<p>就是采用一个代理类调用原有的方法，且对产生的结果进行控制。这种方法就是代理模式。</p>
</li>
</ul>
<p>使用代理模式，可以将功能划分的更加清晰，有助于后期维护！</p>
<h3 id="9外观模式">9.外观模式</h3>
<p>外观模式是为了解决类与类之家的依赖关系的，像spring一样，可以将类和类之间的关系配置到配置文件中，而外观模式就是将他们的关系放在一个Facade类中，降低了类类之间的耦合度，该模式中没有涉及到接口。</p>
<p>我们先看下实现类：</p>
<pre><code class="language-java">public class CPU {
	
	public void startup(){
		System.out.println(&quot;cpu startup!&quot;);
	}
	
	public void shutdown(){
		System.out.println(&quot;cpu shutdown!&quot;);
	}
}
public class Memory {
	
	public void startup(){
		System.out.println(&quot;memory startup!&quot;);
	}
	
	public void shutdown(){
		System.out.println(&quot;memory shutdown!&quot;);
	}
}
public class Disk {
	
	public void startup(){
		System.out.println(&quot;disk startup!&quot;);
	}
	
	public void shutdown(){
		System.out.println(&quot;disk shutdown!&quot;);
	}
}
public class Computer {
	private CPU cpu;
	private Memory memory;
	private Disk disk;
	
	public Computer(){
		cpu = new CPU();
		memory = new Memory();
		disk = new Disk();
	}
	
	public void startup(){
		System.out.println(&quot;start the computer!&quot;);
		cpu.startup();
		memory.startup();
		disk.startup();
		System.out.println(&quot;start computer finished!&quot;);
	}
	
	public void shutdown(){
		System.out.println(&quot;begin to close the computer!&quot;);
		cpu.shutdown();
		memory.shutdown();
		disk.shutdown();
		System.out.println(&quot;computer closed!&quot;);
	}
}
</code></pre>
<p>User类如下：</p>
<pre><code class="language-java">public class User {

	public static void main(String[] args) {
		Computer computer = new Computer();
		computer.startup();
		computer.shutdown();
	}
}
</code></pre>
<p>输出：</p>
<pre><code>start the computer!
cpu startup!
memory startup!
disk startup!
start computer finished!
begin to close the computer!
cpu shutdown!
memory shutdown!
disk shutdown!
computer closed!
</code></pre>
<p>如果我们没有Computer类，那么，CPU、Memory、Disk他们之间将会相互持有实例，产生关系，这样会造成严重的依赖，修改一个类，可能会带来其他类的修改，这不是我们想要看到的，有了Computer类，他们之间的关系被放在了Computer类里，这样就起到了解耦的作用，这，就是外观模式！</p>
<h3 id="10桥接模式">10.桥接模式</h3>
<p>桥接模式就是把事物和其具体实现分开，使他们可以各自独立的变化。桥接的用意是：将抽象化与实现化解耦，使得二者可以独立变化，像我们常用的JDBC桥DriverManager一样，JDBC进行连接数据库的时候，在各个数据库之间进行切换，基本不需要动太多的代码，甚至丝毫不用动，原因就是JDBC提供统一接口，每个数据库提供各自的实现，用一个叫做数据库驱动的程序来桥接就行了。</p>
<p>先定义接口：</p>
<pre><code class="language-java">public interface Sourceable {
	public void method();
}
</code></pre>
<p>分别定义两个实现类：</p>
<pre><code class="language-java">public class SourceSub1 implements Sourceable {

	@Override
	public void method() {
		System.out.println(&quot;this is the first sub!&quot;);
	}
}
public class SourceSub2 implements Sourceable {

	@Override
	public void method() {
		System.out.println(&quot;this is the second sub!&quot;);
	}
}
</code></pre>
<p>定义一个桥，持有Sourceable的一个实例：</p>
<pre><code class="language-java">public abstract class Bridge {
	private Sourceable source;

	public void method(){
		source.method();
	}
	
	public Sourceable getSource() {
		return source;
	}
	 
	public void setSource(Sourceable source) {
		this.source = source;
	}
}
public class MyBridge extends Bridge {
	public void method(){
		getSource().method();
	}
}
</code></pre>
<p>测试类：</p>
<pre><code class="language-java">public class BridgeTest {
	
	public static void main(String[] args) {
		
		Bridge bridge = new MyBridge();
		
		/*调用第一个对象*/
		Sourceable source1 = new SourceSub1();
		bridge.setSource(source1);
		bridge.method();
		
		/*调用第二个对象*/
		Sourceable source2 = new SourceSub2();
		bridge.setSource(source2);
		bridge.method();
	}
}
</code></pre>
<p>结果如下：</p>
<pre><code>this is the first sub!
this is the second sub!
</code></pre>
<p>这样，就通过对Bridge类的调用，实现了对接口Sourceable的实现类SourceSub1和SourceSub2的调用。接下来我再画个图，大家就应该明白了，因为这个图是我们JDBC连接的原理，有数据库学习基础的，一结合就都懂了。</p>
<h3 id="11组合模式">11.组合模式</h3>
<p>组合模式有时又叫部分-整体模式在处理类似树形结构的问题时比较方便，看看关系图：</p>
<p>直接来看代码：</p>
<pre><code class="language-java">public class TreeNode {
	
	private String name;
	private TreeNode parent;
	private Vector&lt;TreeNode&gt; children = new Vector&lt;TreeNode&gt;();
	
	public TreeNode(String name){
		this.name = name;
	}
	 
	public String getName() {
		return name;
	}
	 
	public void setName(String name) {
		this.name = name;
	}
	 
	public TreeNode getParent() {
		return parent;
	}
	 
	public void setParent(TreeNode parent) {
		this.parent = parent;
	}
	
	//添加孩子节点
	public void add(TreeNode node){
		children.add(node);
	}
	
	//删除孩子节点
	public void remove(TreeNode node){
		children.remove(node);
	}
	
	//取得孩子节点
	public Enumeration&lt;TreeNode&gt; getChildren(){
		return children.elements();
	}
}

public class Tree {

	TreeNode root = null;
	 
	public Tree(String name) {
		root = new TreeNode(name);
	}
	 
	public static void main(String[] args) {
		Tree tree = new Tree(&quot;A&quot;);
		TreeNode nodeB = new TreeNode(&quot;B&quot;);
		TreeNode nodeC = new TreeNode(&quot;C&quot;);
		
		nodeB.add(nodeC);
		tree.root.add(nodeB);
		System.out.println(&quot;build the tree finished!&quot;);
	}
}
</code></pre>
<p>使用场景：将多个对象组合在一起进行操作，常用于表示树形结构中，例如二叉树，数等。</p>
<h3 id="12享元模式">12.享元模式</h3>
<p>享元模式的主要目的是实现对象的共享，即共享池，当系统中对象多的时候可以减少内存的开销，通常与工厂模式一起使用。</p>
<p>FlyWeightFactory负责创建和管理享元单元，当一个客户端请求时，工厂需要检查当前对象池中是否有符合条件的对象，如果有，就返回已经存在的对象，如果没有，则创建一个新对象，FlyWeight是超类。一提到共享池，我们很容易联想到Java里面的JDBC连接池。</p>
<p>想想每个连接的特点，我们不难总结出：</p>
<p>适用于作共享的一些个对象，他们有一些共有的属性，就拿数据库连接池来说：url、driverClassName、username、password及dbname，这些属性对于每个连接来说都是一样的，所以就适合用享元模式来处理，建一个工厂类，将上述类似属性作为内部数据，其它的作为外部数据，在方法调用时，当做参数传进来，这样就节省了空间，减少了实例的数量。</p>
<p>看下数据库连接池的代码：</p>
<pre><code class="language-java">public class ConnectionPool {
	
	private Vector&lt;Connection&gt; pool;
	
	/*公有属性*/
	private String url = &quot;jdbc:mysql://localhost:3306/test&quot;;
	private String username = &quot;root&quot;;
	private String password = &quot;root&quot;;
	private String driverClassName = &quot;com.mysql.jdbc.Driver&quot;;
	 
	private int poolSize = 100;
	private static ConnectionPool instance = null;
	Connection conn = null;
	 
	/*构造方法，做一些初始化工作*/
	private ConnectionPool() {
		pool = new Vector&lt;Connection&gt;(poolSize);
	 
		for (int i = 0; i &lt; poolSize; i++) {
			try {
				Class.forName(driverClassName);
				conn = DriverManager.getConnection(url, username, password);
				pool.add(conn);
			} catch (ClassNotFoundException e) {
				e.printStackTrace();
			} catch (SQLException e) {
				e.printStackTrace();
			}
		}
	}
	 
	/* 返回连接到连接池 */
	public synchronized void release() {
		pool.add(conn);
	}
	 
	/* 返回连接池中的一个数据库连接 */
	public synchronized Connection getConnection() {
		if (pool.size() &gt; 0) {
			Connection conn = pool.get(0);
			pool.remove(conn);
			return conn;
		} else {
			return null;
		}
	}
}
</code></pre>
<p>通过连接池的管理，实现了数据库连接的共享，不需要每一次都重新创建连接，节省了数据库重新创建的开销，提升了系统的性能！</p>
<h3 id="13策略模式">13.策略模式</h3>
<p>策略模式定义了一系列算法，并将每个算法封装起来，使他们可以相互替换，且算法的变化不会影响到使用算法的客户。需要设计一个接口，为一系列实现类提供统一的方法，多个实现类实现该接口，设计一个抽象类（可有可无，属于辅助类），提供辅助函数。</p>
<p>AbstractCalculator是辅助类，提供辅助方法，接下来，依次实现下每个类：</p>
<p>首先统一接口：</p>
<pre><code class="language-java">public interface ICalculator {
	public int calculate(String exp);
}
</code></pre>
<p>辅助类：</p>
<pre><code class="language-java">public abstract class AbstractCalculator {
	
	public int[] split(String exp,String opt){
		String array[] = exp.split(opt);
		int arrayInt[] = new int[2];
		arrayInt[0] = Integer.parseInt(array[0]);
		arrayInt[1] = Integer.parseInt(array[1]);
		return arrayInt;
	}
}
</code></pre>
<p>三个实现类：</p>
<pre><code class="language-java">public class Plus extends AbstractCalculator implements ICalculator {

	@Override
	public int calculate(String exp) {
		int arrayInt[] = split(exp,&quot;\\+&quot;);
		return arrayInt[0]+arrayInt[1];
	}
}
public class Minus extends AbstractCalculator implements ICalculator {

	@Override
	public int calculate(String exp) {
		int arrayInt[] = split(exp,&quot;-&quot;);
		return arrayInt[0]-arrayInt[1];
	}

}
public class Multiply extends AbstractCalculator implements ICalculator {

	@Override
	public int calculate(String exp) {
		int arrayInt[] = split(exp,&quot;\\*&quot;);
		return arrayInt[0]*arrayInt[1];
	}
}
</code></pre>
<p>简单的测试类：</p>
<pre><code class="language-java">public class StrategyTest {

	public static void main(String[] args) {
		String exp = &quot;2+8&quot;; // 输出：10
		ICalculator cal = new Plus();
		int result = cal.calculate(exp);
		System.out.println(result);
	}
}
</code></pre>
<p>策略模式的决定权在用户，系统本身提供不同算法的实现，新增或者删除算法，对各种算法做封装。因此，策略模式多用在算法决策系统中，外部用户只需要决定用哪个算法即可。</p>
<h3 id="14模板方法模式">14.模板方法模式</h3>
<p>解释一下模板方法模式，就是指：一个抽象类中，有一个主方法，再定义1...n个方法，可以是抽象的，也可以是实际的方法，定义一个类，继承该抽象类，重写抽象方法，通过调用抽象类，实现对子类的调用，先看个关系图：</p>
<p>就是在AbstractCalculator类中定义一个主方法calculate，calculate()调用spilt()等，Plus和Minus分别继承AbstractCalculator类，通过对AbstractCalculator的调用实现对子类的调用，看下面的例子：</p>
<pre><code class="language-java">public abstract class AbstractCalculator {
	
	/*主方法，实现对本类其它方法的调用*/
	public final int calculate(String exp,String opt){
		int array[] = split(exp,opt);
		return calculate(array[0],array[1]);
	}
	
	/*被子类重写的方法*/
	abstract public int calculate(int num1,int num2);
	
	public int[] split(String exp,String opt){
		String array[] = exp.split(opt);
		int arrayInt[] = new int[2];
		arrayInt[0] = Integer.parseInt(array[0]);
		arrayInt[1] = Integer.parseInt(array[1]);
		return arrayInt;
	}
}

public class Plus extends AbstractCalculator {

	@Override
	public int calculate(int num1,int num2) {
		return num1 + num2;
	}
}
</code></pre>
<p>测试类：</p>
<pre><code class="language-java">public class StrategyTest {

	public static void main(String[] args) {
		String exp = &quot;8+8&quot;;
		AbstractCalculator cal = new Plus();
		int result = cal.calculate(exp, &quot;\\+&quot;);
		System.out.println(result);
	}
}
</code></pre>
<p>我跟踪下这个小程序的执行过程：首先将exp和&quot;\+&quot;做参数，调用AbstractCalculator类里的calculate(String,String)方法，在calculate(String,String)里调用同类的split()，之后再调用calculate(int ,int)方法，从这个方法进入到子类中，执行完return num1 + num2后，将值返回到AbstractCalculator类，赋给result，打印出来。正好验证了我们开头的思路。</p>
<h3 id="15观察者模式">15.观察者模式</h3>
<p>包括这个模式在内的接下来的四个模式，都是类和类之间的关系，不涉及到继承，学的时候应该 记得归纳，记得本文最开始的那个图。观察者模式很好理解，类似于邮件订阅和RSS订阅，当我们浏览一些博客或wiki时，经常会看到RSS图标，就这的意思是，当你订阅了该文章，如果后续有更新，会及时通知你。其实，简单来讲就一句话：当一个对象变化时，其它依赖该对象的对象都会收到通知，并且随着变化！对象之间是一种一对多的关系。</p>
<p>我解释下这些类的作用：MySubject类就是我们的主对象，Observer1和Observer2是依赖于MySubject的对象，当MySubject变化时，Observer1和Observer2必然变化。AbstractSubject类中定义着需要监控的对象列表，可以对其进行修改：增加或删除被监控对象，且当MySubject变化时，负责通知在列表内存在的对象。我们看实现代码：</p>
<p>一个Observer接口：</p>
<pre><code class="language-java">public interface Observer {
	public void update();
}
</code></pre>
<p>两个实现类：</p>
<pre><code class="language-java">public class Observer1 implements Observer {

	@Override
	public void update() {
		System.out.println(&quot;observer1 has received!&quot;);
	}
}
public class Observer2 implements Observer {

	@Override
	public void update() {
		System.out.println(&quot;observer2 has received!&quot;);
	}

}
</code></pre>
<p>Subject接口及实现类：</p>
<pre><code class="language-java">public interface Subject {
	
	/*增加观察者*/
	public void add(Observer observer);
	
	/*删除观察者*/
	public void del(Observer observer);
	
	/*通知所有的观察者*/
	public void notifyObservers();
	
	/*自身的操作*/
	public void operation();
}
public abstract class AbstractSubject implements Subject {

	private Vector&lt;Observer&gt; vector = new Vector&lt;Observer&gt;();
	@Override
	public void add(Observer observer) {
		vector.add(observer);
	}
	 
	@Override
	public void del(Observer observer) {
		vector.remove(observer);
	}
	 
	@Override
	public void notifyObservers() {
		Enumeration&lt;Observer&gt; enumo = vector.elements();
		while(enumo.hasMoreElements()){
			enumo.nextElement().update();
		}
	}
}

public class MySubject extends AbstractSubject {

	@Override
	public void operation() {
		System.out.println(&quot;update self!&quot;);
		notifyObservers();
	}

}
</code></pre>
<p>测试类：</p>
<pre><code class="language-java">public class ObserverTest {

	public static void main(String[] args) {
		Subject sub = new MySubject();
		sub.add(new Observer1());
		sub.add(new Observer2());
		
		sub.operation();
	}

}
</code></pre>
<p>输出：</p>
<pre><code>update self!
observer1 has received!
observer2 has received!
</code></pre>
<h3 id="16迭代器模式">16.迭代器模式</h3>
<p>顾名思义，迭代器模式就是顺序访问聚集中的对象，一般来说，集合中非常常见，如果对集合类比较熟悉的话，理解本模式会十分轻松。这句话包含两层意思：一是需要遍历的对象，即聚集对象，二是迭代器对象，用于对聚集对象进行遍历访问。我们看下关系图：</p>
<p>这个思路和我们常用的一模一样，MyCollection中定义了集合的一些操作，MyIterator中定义了一系列迭代操作，且持有Collection实例，我们来看看实现代码：</p>
<p>两个接口：</p>
<pre><code class="language-java">public interface Collection {
	
	public Iterator iterator();
	
	/*取得集合元素*/
	public Object get(int i);
	
	/*取得集合大小*/
	public int size();
}
public interface Iterator {
	//前移
	public Object previous();
	
	//后移
	public Object next();
	public boolean hasNext();
	
	//取得第一个元素
	public Object first();
}
</code></pre>
<p>两个实现：</p>
<pre><code class="language-java">public class MyCollection implements Collection {

	public String string[] = {&quot;A&quot;,&quot;B&quot;,&quot;C&quot;,&quot;D&quot;,&quot;E&quot;};
	@Override
	public Iterator iterator() {
		return new MyIterator(this);
	}
	 
	@Override
	public Object get(int i) {
		return string[i];
	}
	 
	@Override
	public int size() {
		return string.length;
	}
}

public class MyIterator implements Iterator {

	private Collection collection;
	private int pos = -1;
	
	public MyIterator(Collection collection){
		this.collection = collection;
	}
	
	@Override
	public Object previous() {
		if(pos &gt; 0){
			pos--;
		}
		return collection.get(pos);
	}
	 
	@Override
	public Object next() {
		if(pos&lt;collection.size()-1){
			pos++;
		}
		return collection.get(pos);
	}
	 
	@Override
	public boolean hasNext() {
		if(pos&lt;collection.size()-1){
			return true;
		}else{
			return false;
		}
	}
	 
	@Override
	public Object first() {
		pos = 0;
		return collection.get(pos);
	}

}
</code></pre>
<p>测试类：</p>
<pre><code class="language-java">public class Test {

	public static void main(String[] args) {
		Collection collection = new MyCollection();
		Iterator it = collection.iterator();
		
		while(it.hasNext()){
			System.out.println(it.next()); // 输出：A B C D E
		}
	}
}
</code></pre>
<p>此处我们貌似模拟了一个集合类的过程，感觉是不是很爽？其实JDK中各个类也都是这些基本的东西，加一些设计模式，再加一些优化放到一起的，只要我们把这些东西学会了，掌握好了，我们也可以写出自己的集合类，甚至框架！</p>
<h3 id="17责任链模式">17.责任链模式</h3>
<p>接下来我们将要谈谈责任链模式，有多个对象，每个对象持有对下一个对象的引用，这样就会形成一条链，请求在这条链上传递，直到某一对象决定处理该请求。但是发出者并不清楚到底最终那个对象会处理该请求，所以，责任链模式可以实现，在隐瞒客户端的情况下，对系统进行动态的调整。先看看关系图：</p>
<p>Abstracthandler类提供了get和set方法，方便MyHandle类设置和修改引用对象，MyHandle类是核心，实例化后生成一系列相互持有的对象，构成一条链。</p>
<pre><code class="language-java">public interface Handler {
	public void operator();
}
public abstract class AbstractHandler {
	
	private Handler handler;
	 
	public Handler getHandler() {
		return handler;
	}
	 
	public void setHandler(Handler handler) {
		this.handler = handler;
	}

}
public class MyHandler extends AbstractHandler implements Handler {

	private String name;
	 
	public MyHandler(String name) {
		this.name = name;
	}
	 
	@Override
	public void operator() {
		System.out.println(name+&quot;deal!&quot;);
		if(getHandler()!=null){
			getHandler().operator();
		}
	}
}

public class Test {

	public static void main(String[] args) {
		MyHandler h1 = new MyHandler(&quot;h1&quot;);
		MyHandler h2 = new MyHandler(&quot;h2&quot;);
		MyHandler h3 = new MyHandler(&quot;h3&quot;);
	 
		h1.setHandler(h2);
		h2.setHandler(h3);
	 
		h1.operator();
	}
}
</code></pre>
<p>输出：</p>
<pre><code>h1deal!
h2deal!
h3deal!
</code></pre>
<p>此处强调一点就是，链接上的请求可以是一条链，可以是一个树，还可以是一个环，模式本身不约束这个，需要我们自己去实现，同时，在一个时刻，命令只允许由一个对象传给另一个对象，而不允许传给多个对象。</p>
<h3 id="18命令模式">18.命令模式</h3>
<p>命令模式很好理解，举个例子，司令员下令让士兵去干件事情，从整个事情的角度来考虑，司令员的作用是，发出口令，口令经过传递，传到了士兵耳朵里，士兵去执行。这个过程好在，三者相互解耦，任何一方都不用去依赖其他人，只需要做好自己的事儿就行，司令员要的是结果，不会去关注到底士兵是怎么实现的。我们看看关系图：</p>
<p>Invoker是调用者（司令员），Receiver是被调用者（士兵），MyCommand是命令，实现了Command接口，持有接收对象</p>
<p>实现代码：</p>
<pre><code class="language-java">public interface Command {
	public void exe();
}
public class MyCommand implements Command {

	private Receiver receiver;
	
	public MyCommand(Receiver receiver) {
		this.receiver = receiver;
	}
	 
	@Override
	public void exe() {
		receiver.action();
	}
}
public class Receiver {
	public void action(){
		System.out.println(&quot;command received!&quot;);
	}
}
public class Invoker {
	
	private Command command;
	
	public Invoker(Command command) {
		this.command = command;
	}
	 
	public void action(){
		command.exe();
	}
}
public class Test {

	public static void main(String[] args) { // 输出：command received!
		Receiver receiver = new Receiver();
		Command cmd = new MyCommand(receiver);
		Invoker invoker = new Invoker(cmd);
		invoker.action();
	}
}
</code></pre>
<p>这个很哈理解，命令模式的目的就是达到命令的发出者和执行者之间解耦，实现请求和执行分开，熟悉Struts的同学应该知道，Struts其实就是一种将请求和呈现分离的技术，其中必然涉及命令模式的思想</p>
<h3 id="19备忘录模式">19.备忘录模式</h3>
<p>主要目的是保存一个对象的某个状态，以便在适当的时候恢复对象，个人觉得叫备份模式更形象些，通俗的讲下：假设有原始类A，A中有各种属性，A可以决定需要备份的属性，备忘录类B是用来存储A的一些内部状态，类C呢，就是一个用来存储备忘录的，且只能存储，不能修改等操作。做个图来分析一下：</p>
<p>Original类是原始类，里面有需要保存的属性value及创建一个备忘录类，用来保存value值。Memento类是备忘录类，Storage类是存储备忘录的类，持有Memento类的实例，该模式很好理解。直接看源码：</p>
<pre><code class="language-java">public class Original {
	
	private String value;
	
	public String getValue() {
		return value;
	}
	 
	public void setValue(String value) {
		this.value = value;
	}
	 
	public Original(String value) {
		this.value = value;
	}
	 
	public Memento createMemento(){
		return new Memento(value);
	}
	
	public void restoreMemento(Memento memento){
		this.value = memento.getValue();
	}
}

public class Memento {
	
	private String value;
	 
	public Memento(String value) {
		this.value = value;
	}
	 
	public String getValue() {
		return value;
	}
	 
	public void setValue(String value) {
		this.value = value;
	}
}

public class Storage {
	
	private Memento memento;
	
	public Storage(Memento memento) {
		this.memento = memento;
	}
	 
	public Memento getMemento() {
		return memento;
	}
	 
	public void setMemento(Memento memento) {
		this.memento = memento;
	}
}
</code></pre>
<p>测试类：</p>
<pre><code class="language-java">public class Test {

	public static void main(String[] args) {
		
		// 创建原始类
		Original origi = new Original(&quot;egg&quot;);
	 
		// 创建备忘录
		Storage storage = new Storage(origi.createMemento());
	 
		// 修改原始类的状态
		System.out.println(&quot;初始化状态为：&quot; + origi.getValue());
		origi.setValue(&quot;niu&quot;);
		System.out.println(&quot;修改后的状态为：&quot; + origi.getValue());
	 
		// 回复原始类的状态
		origi.restoreMemento(storage.getMemento());
		System.out.println(&quot;恢复后的状态为：&quot; + origi.getValue());
	}
}
</code></pre>
<p>输出：</p>
<pre><code>初始化状态为：egg
修改后的状态为：niu
恢复后的状态为：egg
</code></pre>
<p>简单描述下：新建原始类时，value被初始化为egg，后经过修改，将value的值置为niu，最后倒数第二行进行恢复状态，结果成功恢复了。其实我觉得这个模式叫“备份-恢复”模式最形象。</p>
<h3 id="20状态模式">20.状态模式</h3>
<p>核心思想就是：当对象的状态改变时，同时改变其行为，很好理解！就拿QQ来说，有几种状态，在线、隐身、忙碌等，每个状态对应不同的操作，而且你的好友也能看到你的状态，所以，状态模式就两点：1、可以通过改变状态来获得不同的行为。2、你的好友能同时看到你的变化。看图：</p>
<p>State类是个状态类，Context类可以实现切换，我们来看看代码：</p>
<pre><code class="language-java">package com.xtfggef.dp.state;

/**
 * 状态类的核心类
 * 2012-12-1
 * @author erqing
 *
 */
public class State {
	
	private String value;
	
	public String getValue() {
		return value;
	}

	public void setValue(String value) {
		this.value = value;
	}

	public void method1(){
		System.out.println(&quot;execute the first opt!&quot;);
	}
	
	public void method2(){
		System.out.println(&quot;execute the second opt!&quot;);
	}
}
</code></pre>
<pre><code class="language-java">package com.xtfggef.dp.state;

/**
 * 状态模式的切换类   2012-12-1
 * @author erqing
 * 
 */
public class Context {

	private State state;

	public Context(State state) {
		this.state = state;
	}

	public State getState() {
		return state;
	}

	public void setState(State state) {
		this.state = state;
	}

	public void method() {
		if (state.getValue().equals(&quot;state1&quot;)) {
			state.method1();
		} else if (state.getValue().equals(&quot;state2&quot;)) {
			state.method2();
		}
	}
}
</code></pre>
<p>测试类：</p>
<pre><code class="language-java">public class Test {

	public static void main(String[] args) {
		
		State state = new State();
		Context context = new Context(state);
		
		//设置第一种状态
		state.setValue(&quot;state1&quot;);
		context.method();
		
		//设置第二种状态
		state.setValue(&quot;state2&quot;);
		context.method();
	}
}
</code></pre>
<p>输出：</p>
<pre><code>execute the first opt!
execute the second opt!
</code></pre>
<p>根据这个特性，状态模式在日常开发中用的挺多的，尤其是做网站的时候，我们有时希望根据对象的某一属性，区别开他们的一些功能，比如说简单的权限控制等。</p>
<h3 id="21访问者模式">21.访问者模式</h3>
<p>访问者模式把数据结构和作用于结构上的操作解耦合，使得操作集合可相对自由地演化。访问者模式适用于数据结构相对稳定算法又易变化的系统。因为访问者模式使得算法操作增加变得容易。若系统数据结构对象易于变化，经常有新的数据对象增加进来，则不适合使用访问者模式。访问者模式的优点是增加操作很容易，因为增加操作意味着增加新的访问者。访问者模式将有关行为集中到一个访问者对象中，其改变不影响系统数据结构。其缺点就是增加新的数据结构很困难。—— From 百科</p>
<p>简单来说，访问者模式就是一种分离对象数据结构与行为的方法，通过这种分离，可达到为一个被访问者动态添加新的操作而无需做其它的修改的效果。</p>
<p>来看看原码，一个Visitor类，存放要访问的对象</p>
<pre><code class="language-java">public interface Visitor {
	public void visit(Subject sub);
}
public class MyVisitor implements Visitor {

	@Override
	public void visit(Subject sub) {
		System.out.println(&quot;visit the subject：&quot;+sub.getSubject());
	}
}
</code></pre>
<p>Subject类，accept方法，接受将要访问它的对象，getSubject()获取将要被访问的属性</p>
<pre><code class="language-java">public interface Subject {
	public void accept(Visitor visitor);
	public String getSubject();
}
public class MySubject implements Subject {

	@Override
	public void accept(Visitor visitor) {
		visitor.visit(this);
	}
	 
	@Override
	public String getSubject() {
		return &quot;love&quot;;
	}
}
</code></pre>
<p>测试：</p>
<pre><code class="language-java">public class Test {

	public static void main(String[] args) {
		
		Visitor visitor = new MyVisitor();
		Subject sub = new MySubject();
		sub.accept(visitor);	
	}
}
</code></pre>
<p>输出：</p>
<pre><code>visit the subject：love
</code></pre>
<p>该模式适用场景：如果我们想为一个现有的类增加新功能，不得不考虑几个事情：1、新功能会不会与现有功能出现兼容性问题？2、以后会不会再需要添加？3、如果类不允许修改代码怎么办？面对这些问题，最好的解决方法就是使用访问者模式，访问者模式适用于数据结构相对稳定的系统，把数据结构和算法解耦，</p>
<h3 id="22中介者模式">22.中介者模式</h3>
<p>中介者模式也是用来降低类类之间的耦合的，因为如果类类之间有依赖关系的话，不利于功能的拓展和维护，因为只要修改一个对象，其它关联的对象都得进行修改。如果使用中介者模式，只需关心和Mediator类的关系，具体类类之间的关系及调度交给Mediator就行，这有点像spring容器的作用。先看看图：</p>
<p>User类统一接口，User1和User2分别是不同的对象，二者之间有关联，如果不采用中介者模式，则需要二者相互持有引用，这样二者的耦合度很高，为了解耦，引入了Mediator类，提供统一接口，MyMediator为其实现类，里面持有User1和User2的实例，用来实现对User1和User2的控制。这样User1和User2两个对象相互独立，他们只需要保持好和Mediator之间的关系就行，剩下的全由MyMediator类来维护！基本实现：</p>
<pre><code class="language-java">public interface Mediator {
	public void createMediator();
	public void workAll();
}
public class MyMediator implements Mediator {

	private User user1;
	private User user2;
	
	public User getUser1() {
		return user1;
	}
	 
	public User getUser2() {
		return user2;
	}
	 
	@Override
	public void createMediator() {
		user1 = new User1(this);
		user2 = new User2(this);
	}
	 
	@Override
	public void workAll() {
		user1.work();
		user2.work();
	}
}

public abstract class User {
	
	private Mediator mediator;
	
	public Mediator getMediator(){
		return mediator;
	}
	
	public User(Mediator mediator) {
		this.mediator = mediator;
	}
	 
	public abstract void work();
}
public class User1 extends User {

	public User1(Mediator mediator){
		super(mediator);
	}
	
	@Override
	public void work() {
		System.out.println(&quot;user1 exe!&quot;);
	}
}
public class User2 extends User {

	public User2(Mediator mediator){
		super(mediator);
	}
	
	@Override
	public void work() {
		System.out.println(&quot;user2 exe!&quot;);
	}
}
</code></pre>
<p>测试类：</p>
<pre><code class="language-java">public class Test {

	public static void main(String[] args) {
		Mediator mediator = new MyMediator();
		mediator.createMediator();
		mediator.workAll();
	}
}
</code></pre>
<p>输出：</p>
<pre><code>user1 exe!
user2 exe!
</code></pre>
<h3 id="23解释器模式">23.解释器模式</h3>
<p>解释器模式是我们暂时的最后一讲，一般主要应用在OOP开发中的编译器的开发中，所以适用面比较窄。</p>
<p>Context类是一个上下文环境类，Plus和Minus分别是用来计算的实现，代码如下：</p>
<pre><code class="language-java">public interface Expression {
	public int interpret(Context context);
}
public class Plus implements Expression {

	@Override
	public int interpret(Context context) {
		return context.getNum1()+context.getNum2();
	}
}
public class Minus implements Expression {

	@Override
	public int interpret(Context context) {
		return context.getNum1()-context.getNum2();
	}
}
public class Context {
	
	private int num1;
	private int num2;
	
	public Context(int num1, int num2) {
		this.num1 = num1;
		this.num2 = num2;
	}
	
	public int getNum1() {
		return num1;
	}
	public void setNum1(int num1) {
		this.num1 = num1;
	}
	public int getNum2() {
		return num2;
	}
	public void setNum2(int num2) {
		this.num2 = num2;
	}

}
</code></pre>
<pre><code class="language-java">public class Test {

	public static void main(String[] args) {
	 
		// 计算9+2-8的值
		int result = new Minus().interpret((new Context(new Plus().interpret(new Context(9, 2)), 8)));
		System.out.println(result); // 结果：3
	}
}
</code></pre>
<p>基本就这样，解释器模式用来做各种各样的解释器，如正则表达式等的解释器等等！</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Activiti 工作流引擎]]></title>
        <id>https://hualiang.online/post/activiti-workflow-engine/</id>
        <link href="https://hualiang.online/post/activiti-workflow-engine/">
        </link>
        <updated>2023-11-01T08:50:00.000Z</updated>
        <summary type="html"><![CDATA[<p>你听过工作流引擎吗？🤔<br>
我在学习 SSM 框架进行项目实战时，跟着教程接触到了工作流引擎，所以借此机会记录一下。</p>
]]></summary>
        <content type="html"><![CDATA[<p>你听过工作流引擎吗？🤔<br>
我在学习 SSM 框架进行项目实战时，跟着教程接触到了工作流引擎，所以借此机会记录一下。</p>
<!-- more -->
<h1 id="一-了解工作流">一、了解工作流</h1>
<h2 id="1-什么是工作流">1、什么是工作流</h2>
<p><strong>工作流（Workflow）</strong>，就是通过计算机对业务流程自动化执行管理。它主要解决的是“使在多个参与者之间按照某种<strong>预定义的规则</strong>自动进行传递文档、信息或任务的过程，从而实现某个预期的业务目标，或者促使此目标的实现”。**通俗来讲，就是业务上一个完整的审批流程。**例如员工的请假，出差，外出采购，合同审核等等，这些过程，都是一个工作流。</p>
<h2 id="2-工作流引擎">2、工作流引擎</h2>
<p>对于工作流的处理，如果采用原始的方式，我们需要拿着各种文件到各个负责人那里去签字，需要在多个部门之间不断审批，这种方式费时费力。而我们可以借助软件系统来协助我们处理这些审批流程，这样就出现了工作流系统，使用工作流系统后可以极大的提高工作效率。</p>
<p>在学习工作流的过程中，我们肯定看到过这个模型:填写请假单-&gt;部门经理审批-&gt;总经理审批-&gt;人事备案。</p>
<p>（1）要实现上述的流程，我们自己可以通过字段标识来实现这个审批效果，在业务表中加个字段，比如填写请假单用1标识，部门经理用2标识，总经理用3标识，人事备案用4标识，好像看起来没啥问题，也实现了审批效果。可是一旦我们的流程出现了变化，这个时候我们就需要改动我们的代码了，这显然是不可取的，那么有没有专业的方式来实现工作流的管理呢？并且可以做到业务流程变化之后，我们的程序可以不用改变，如果可以实现这样的效果，那么我们的业务系统的适应能力就得到了极大提升。在这样的背景下，就出现了<strong>工作流引擎</strong>。</p>
<p><strong>为什么使用工作流引擎，能实现业务流程改变，不用修改代码，流程还能自动推进？</strong></p>
<p>（1）我们先来说说为什么流程改变，不用修改代码：我们的工作流引擎都实现了一个规范，这个规范要求我们的流程管理与状态字段无关，始终都是读取业务流程图的下一个节点。当业务更新的时候我们只需要更新业务流程图就行了。这就实现了业务流程改变，不用修改代码。</p>
<p>（2）再来说说流程自动推进，这个原理就更简单了，就拿上面的请假模型来说，工作流引擎会用一张表来记录当前处在的节点。当填写完请假单后肯定是要轮到部门经理来审批了，所以我们一旦完成了请假单填写那么这条记录将会被从这张表删除掉，并且会把下一个节点部门经理的信息插入到这张表中，当我们用部门经理的信息去这张表中查询的时候就能查出部门经理相关的审批的信息了，以此类推，这样层层递进，就实现了流程的自动递交了。</p>
<h2 id="3-常见工作流引擎">3、常见工作流引擎</h2>
<p>主流的框架有：<strong>Activiti</strong>、jBPM、Camunda 、Flowable 、还有我们国产的盘古BPM、云程</p>
<h2 id="4-activiti7概述">4、Activiti7概述</h2>
<h3 id="41-activiti介绍">4.1、Activiti介绍</h3>
<p>activiti是一个工作流引擎，可以将业务系统中复杂的业务流程抽取出来，使用专门的建模语言BPMN进行定义，业务流程按照预先定义的流程进行执行。实现了系统的流程由activiti进行管理，减少业务系统由于流程变更进行系统升级改造的工作流量，从而提高系统的健壮性，同时也减少了系统开发维护成本。</p>
<p>官方网站：https://www.activiti.org</p>
<h3 id="42-建模语言bpmn">4.2、建模语言BPMN</h3>
<p>BPM（Business Process Management）即业务流程管理，是一种规范化的构造端到端的业务流程，以持续提高组织业务效率</p>
<p>BPM 软件就是根据企业中业务环境的变化，推进人与人之间、人与系统之间以及系统与系统之间的整理及调整的经营方法与解决方案的 IT 工具。使用 BPM 软件对企业内部及外部的业务流程的整个生命周期进行建模、自动化、管理监控和优化，可以降低企业成本，提高利润</p>
<p>BPMN（Business Process Model AndNotation）即业务流程模型和符号，是一套标准的业务流程建模符号，使用 BPMN 提供的符号可以创建业务流程。Activit 就是使用 BPMN 进行流程建模、流程执行管理的</p>
<p>BPMN2.0 是业务流程建模符号 2.0 的缩写，它由 Business Process Management Initiative 这个非营利协会创建并不断发展。BPMN2.0 是使用一些符号来明确业务流程设计流程图的一套符号规范，能增进业务建模时的沟通效率。目前 BPMN2.0 是最新的版本，它用于在 BPM 上下文中进行布局和可视化的沟通</p>
<p>BPMN2.0 的基本符号主要包含：</p>
<ul>
<li>
<p><strong>事件 Event</strong></p>
<p>开始：表示一个流程的开始</p>
<p>中间：发生的开始和结束事件之间，影响处理的流程</p>
<p>结束：表示该过程结束</p>
<figure data-type="image" tabindex="1"><img src="https://hualiang.online/post-images/1671776604019.png" alt="67177660401" loading="lazy"></figure>
</li>
<li>
<p><strong>活动 Activities</strong></p>
<p>活动是工作或任务的一个通用术语。一个活动可以是一个任务，还可以是一个当前流程的子处理流程；其次，你还可以为活动指定不同的类型。常见活动如下：</p>
<figure data-type="image" tabindex="2"><img src="https://hualiang.online/post-images/1671776624801.png" alt="67177662480" loading="lazy"></figure>
</li>
<li>
<p><strong>网关 GateWay</strong></p>
<p>用于表示流程的分支与合并，有几种常用网关需要了解：</p>
<figure data-type="image" tabindex="3"><img src="https://hualiang.online/post-images/1671776646378.png" alt="67177664637" loading="lazy"></figure>
<ul>
<li>
<p>排他网关：只有一条路径会被选择</p>
</li>
<li>
<p>xxxxxxxxxx <code>&lt;el-button type=&quot;success&quot; icon=&quot;el-icon-plus&quot; size=&quot;mini&quot; @click=&quot;add&quot; :disabled=&quot;$hasBP('bnt.sysRole.add')  === false&quot;&gt;</code> 添加 <code>&lt;/el-button&gt;</code> vue</p>
</li>
<li>
<p>包容网关：可以同时执行多条线路，也可以在网关上设置条件</p>
</li>
<li>
<p>事件网关：专门为中间捕获事件设置的，允许设置多个输出流指向多个不同的中间捕获事件。当流程执行到事件网关后，流程处于等待状态，需要等待抛出事件才能将等待状态转换为活动状态</p>
</li>
</ul>
<figure data-type="image" tabindex="4"><img src="https://hualiang.online/post-images/image-20230209103727489.png" alt="image-20230209103727489" loading="lazy"></figure>
</li>
<li>
<p><strong>流向 Flow</strong></p>
<p>流是连接两个流程节点的连线，常见的流向包含以下几种：</p>
<p>顺序流：用一个带实心箭头的实心线表示，用于指定活动执行的顺序</p>
<p>信息流：用一条带箭头的虚线表示，用于描述两个独立的业务参与者（业务实体/业务角色）之间发送和接受的消息流动</p>
<p>关联：用一根带有线箭头的点线表示，用于将相关的数据、文本和其他人工信息与流对象联系起来。用于展示活动的输入和输出</p>
<figure data-type="image" tabindex="5"><img src="https://hualiang.online/post-images/1671776662921.png" alt="67177666292" loading="lazy"></figure>
</li>
</ul>
<p><strong>流程示例：</strong></p>
<figure data-type="image" tabindex="6"><img src="https://hualiang.online/post-images/image-20230209104411748.png" alt="image-20230209104411748" loading="lazy"></figure>
<h3 id="43-activiti使用流程">4.3、Activiti使用流程</h3>
<p><strong>第一步：</strong> 引入依赖并初始化数据库</p>
<p>既然activiti是一个框架，那么我们肯定是需要引入对应的jar包坐标的，具体参考代码中的。</p>
<p><strong>第二步：</strong>  通过工具绘画流程图</p>
<p>使用 activiti 流程建模工具(activity-designer)定义业务流程(.bpmn 文件)</p>
<p>.bpmn 文件就是业务流程定义文件，通过 xml 定义业务流程。</p>
<p><strong>第三步</strong>：流程定义部署;</p>
<p>向 activiti 部署业务流程定义（.bpmn 文件），使用 activiti 提供的 api 向 activiti 中部署.bpmn 文件，</p>
<p>通俗来讲，就是让activiti认识要使用的流程</p>
<p><strong>第四步：</strong> 启动一个流程实例（ProcessInstance）</p>
<p>启动一个流程实例表示开始一次业务流程的运行，比如员工请假流程部署完成，如果张三要请假就可以启动一个流程实例，如果李四要请假也启动一个流程实例，两个流程的执行互相不影响，就好比定义一个java类，实例化两个对象一样，部署的流程就好比java类，启动一个流程实例就好比new一个java对象</p>
<p><strong>第五步：</strong> 用户查询待办任务(Task)</p>
<p>因为现在系统的业务流程已经交给 activiti 管理，通过 activiti 就可以查询当前流程执行到哪了，当前用户需要办理什么任务了，这些 activiti帮我们管理了。实际上我们学习activiti也只是学习它的API怎么使用，因为很多功能activiti都已经封装好了，我们会调用就行了</p>
<p><strong>第六步：</strong> 用户办理任务</p>
<p>用户查询待办任务后，就可以办理某个任务，如果这个任务办理完成还需要其它用户办理，比如请假单创建后由部门经理审核，这个过程也是由 activiti 帮我们完成了，不需要我们在代码中硬编码指定下一个任务办理人了</p>
<p><strong>第七步：</strong> 流程结束</p>
<p>当任务办理完成没有下一个任务节点了，这个流程实例就完成了。</p>
<h1 id="二-activiti7">二、Activiti7</h1>
<h2 id="1-activiti使用">1、Activiti使用</h2>
<h3 id="11-数据库支持">1.1、数据库支持</h3>
<p>Activiti 运行必须要有数据库的支持，支持的数据库有：mysql、oracle、postgres、mssql、db2、h2</p>
<h3 id="12-activiti环境">1.2、Activiti环境</h3>
<p>我们直接在当前项目：guigu-oa-parent做Activiti入门讲解</p>
<h4 id="121-引入依赖">1.2.1、引入依赖</h4>
<pre><code class="language-xml">&lt;!--引入activiti的springboot启动器 --&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.activiti&lt;/groupId&gt;
    &lt;artifactId&gt;activiti-spring-boot-starter&lt;/artifactId&gt;
    &lt;version&gt;7.1.0.M6&lt;/version&gt;
    &lt;exclusions&gt;
        &lt;exclusion&gt;
            &lt;artifactId&gt;mybatis&lt;/artifactId&gt;
            &lt;groupId&gt;org.mybatis&lt;/groupId&gt;
        &lt;/exclusion&gt;
    &lt;/exclusions&gt;
&lt;/dependency&gt;
</code></pre>
<p>说明：Activiti7与SpringBoot整合后，默认集成了SpringSecurity安全框架，当前我们项目已经集成过了SpringSecurity，后续案例设置审批人时都必须是系统用户，Activiti框架会检查用户是否存在，否则会出现异常，后续大家可以在案例中测试。</p>
<figure data-type="image" tabindex="7"><img src="https://hualiang.online/post-images/1672035195682.png" alt="67203519568" loading="lazy"></figure>
<h4 id="122-添加配置">1.2.2、添加配置</h4>
<p>数据源项目已经添加，只需要如下配置即可</p>
<pre><code class="language-java">spring:    
	activiti:
      #    false:默认，数据库表不变，但是如果版本不对或者缺失表会抛出异常（生产使用）
      #    true:表不存在，自动创建（开发使用）
      #    create_drop: 启动时创建，关闭时删除表（测试使用）
      #    drop_create: 启动时删除表,在创建表 （不需要手动关闭引擎）
      database-schema-update: true
      #监测历史表是否存在，activities7默认不开启历史表
      db-history-used: true
      #none：不保存任何历史数据，流程中这是最高效的
      #activity：只保存流程实例和流程行为
      #audit：除了activity，还保存全部的流程任务以及其属性，audit为history默认值
      #full：除了audit、还保存其他全部流程相关的细节数据，包括一些流程参数
      history-level: full
      #校验流程文件，默认校验resources下的process 文件夹的流程文件
      check-process-definitions: true
</code></pre>
<h4 id="123-启动项目">1.2.3、启动项目</h4>
<p>启动项目，即可生成项目数据库表</p>
<figure data-type="image" tabindex="8"><img src="https://hualiang.online/post-images/1671693937620.png" alt="67169393762" loading="lazy"></figure>
<h4 id="124-数据库表介绍">1.2.4、数据库表介绍</h4>
<p>Activiti 的运行支持必须要有这 25 张表的支持，主要是在业务流程运行过程中，记录参与流程的用户主体，用户组信息，以及流程的定义，流程执行时的信息，和流程的历史信息等等</p>
<p><strong>1、 表的命名规则和作用</strong></p>
<p>观察创建的表，我们发现 Activiti 的表都以 act_ 开头，紧接着是表示表的用途的两个字母标识，也和 Activiti 所提供的服务的 API 对应：</p>
<ul>
<li>ACT_RE：RE 表示 repository，这个前缀的表包含了流程定义和流程静态资源 （图片、规则、等等）</li>
<li>ACT_RU：RU 表示 runtime，这些表运行时，会包含流程实例、任务、变量、异步任务等流程业务进行中的数据。Activiti 只在流程实例执行过程中保存这些数据，在流程结束时就会删除这些记录。这样表就可以一直保持很小的体积，并且速度很快</li>
<li>ACT_HI：HI 表示 history，这些表包含一些历史数据，比如历史流程实例、变量、任务等等</li>
<li>ACT_GE：GE 表示 general，通用数据</li>
</ul>
<p><strong>2、Activiti 数据表介绍</strong></p>
<table>
<thead>
<tr>
<th><strong>表分类</strong></th>
<th><strong>表名</strong></th>
<th><strong>解释</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>一般数据</td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td>[ACT_GE_BYTEARRAY]</td>
<td>通用的流程定义和流程资源</td>
</tr>
<tr>
<td></td>
<td>[ACT_GE_PROPERTY]</td>
<td>系统相关属性</td>
</tr>
<tr>
<td>流程历史记录</td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td>[ACT_HI_ACTINST]</td>
<td>历史的流程实例</td>
</tr>
<tr>
<td></td>
<td>[ACT_HI_ATTACHMENT]</td>
<td>历史的流程附件</td>
</tr>
<tr>
<td></td>
<td>[ACT_HI_COMMENT]</td>
<td>历史的说明性信息</td>
</tr>
<tr>
<td></td>
<td>[ACT_HI_DETAIL]</td>
<td>历史的流程运行中的细节信息</td>
</tr>
<tr>
<td></td>
<td>[ACT_HI_IDENTITYLINK]</td>
<td>历史的流程运行过程中用户关系</td>
</tr>
<tr>
<td></td>
<td>[ACT_HI_PROCINST]</td>
<td>历史的流程实例</td>
</tr>
<tr>
<td></td>
<td>[ACT_HI_TASKINST]</td>
<td>历史的任务实例</td>
</tr>
<tr>
<td></td>
<td>[ACT_HI_VARINST]</td>
<td>历史的流程运行中的变量信息</td>
</tr>
<tr>
<td>流程定义表</td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td>[ACT_RE_DEPLOYMENT]</td>
<td>部署单元信息</td>
</tr>
<tr>
<td></td>
<td>[ACT_RE_MODEL]</td>
<td>模型信息</td>
</tr>
<tr>
<td></td>
<td>[ACT_RE_PROCDEF]</td>
<td>已部署的流程定义</td>
</tr>
<tr>
<td>运行实例表</td>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td>[ACT_RU_EVENT_SUBSCR]</td>
<td>运行时事件</td>
</tr>
<tr>
<td></td>
<td>[ACT_RU_EXECUTION]</td>
<td>运行时流程执行实例</td>
</tr>
<tr>
<td></td>
<td>[ACT_RU_IDENTITYLINK]</td>
<td>运行时用户关系信息，存储任务节点与参与者的相关信息</td>
</tr>
<tr>
<td></td>
<td>[ACT_RU_JOB]</td>
<td>运行时作业</td>
</tr>
<tr>
<td></td>
<td>[ACT_RU_TASK]</td>
<td>运行时任务</td>
</tr>
<tr>
<td></td>
<td>[ACT_RU_VARIABLE]</td>
<td>运行时变量表</td>
</tr>
</tbody>
</table>
<h3 id="13-activiti常用service服务接口">1.3、Activiti常用Service服务接口</h3>
<p>简单介绍一下各个 Service 的实现类：</p>
<ul>
<li>
<p>RepositoryService</p>
<p>Activiti 的资源管理类，该服务负责部署流程定义，管理流程资源。在使用 Activiti 时，一开始需要先完成流程部署，即将使用建模工具设计的业务流程图通过 RepositoryService 进行部署</p>
</li>
<li>
<p>RuntimeService</p>
<p>Activiti 的流程运行管理类，用于开始一个新的流程实例，获取关于流程执行的相关信息。流程定义用于确定一个流程中的结构和各个节点间行为，而流程实例则是对应的流程定义的一个执行，可以理解为 Java 中类和对象的关系</p>
</li>
<li>
<p>TaskService</p>
<p>Activiti 的任务管理类，用于处理业务运行中的各种任务，例如查询分给用户或组的任务、创建新的任务、分配任务、确定和完成一个任务</p>
</li>
<li>
<p>HistoryService</p>
<p>Activiti 的历史管理类，可以查询历史信息。执行流程时，引擎会保存很多数据，比如流程实例启动时间、任务的参与者、完成任务的时间、每个流程实例的执行路径等等。这个服务主要通过查询功能来获得这些数据</p>
</li>
<li>
<p>ManagementService</p>
<p>Activiti 的引擎管理类，提供了对 Activiti 流程引擎的管理和维护功能，这些功能不在工作流驱动的应用程序中使用，主要用于 Activiti 系统的日常维护</p>
</li>
</ul>
<h3 id="14-流程设计工具">1.4、流程设计工具</h3>
<p>IDEA版本小于等于2019，可使用Activiti插件actiBPM，大于该版本的IDEA可使用Activiti BPMN visualizer插件绘制流程设计。</p>
<p>今天我们主角是：<strong>Activiti Modeler</strong></p>
<p>Activiti Modeler 是 Activiti 官方提供的一款在线流程设计的前端插件，开发人员可以方便在线进行流程设计，保存流程模型，部署至流程定义等等，后续我们的项目也是集成Activiti Modeler绘制流程定义。</p>
<h4 id="141-下载activiti-explorer">1.4.1、下载activiti-explorer</h4>
<p>官网下载：https://www.activiti.org/get-started</p>
<figure data-type="image" tabindex="9"><img src="https://hualiang.online/post-images/1671797894399.png" alt="67179789439" loading="lazy"></figure>
<h4 id="142-解压获取部署包">1.4.2、解压获取部署包</h4>
<p>解压activiti-5.22.0.zip，在activiti-5.22.0\wars目录下获取activiti-explorer.war</p>
<h4 id="143-部署activiti-explorerwar">1.4.3、部署activiti-explorer.war</h4>
<p>将activiti-explorer.war放到tomcat部署目录，启动tomcat</p>
<h4 id="144-访问activiti-explorer">1.4.4、访问activiti-explorer</h4>
<p>http://localhost:8080/activiti-explorer</p>
<p>默认登录账号：kermit	kermit</p>
<figure data-type="image" tabindex="10"><img src="https://hualiang.online/post-images/1671798181491.png" alt="67179818149" loading="lazy"></figure>
<p>上面有很多功能，我们关注流程设计即可，如下图：</p>
<p>点击上图：流程 --&gt; 新建模型 --&gt; 输入模型名称（请假）--&gt; 创建</p>
<figure data-type="image" tabindex="11"><img src="https://hualiang.online/post-images/1671798417009.png" alt="67179841700" loading="lazy"></figure>
<h2 id="2-activiti流程操作">2、Activiti流程操作</h2>
<h3 id="21-流程定义">2.1、流程定义</h3>
<p>我们定义一个请假流程</p>
<h4 id="211-新建模型">2.1.1、新建模型</h4>
<figure data-type="image" tabindex="12"><img src="https://hualiang.online/post-images/1671799395117.png" alt="67179939511" loading="lazy"></figure>
<figure data-type="image" tabindex="13"><img src="https://hualiang.online/post-images/1671799424866.png" alt="67179942486" loading="lazy"></figure>
<figure data-type="image" tabindex="14"><img src="https://hualiang.online/post-images/1671798417009.png" alt="67179841700" loading="lazy"></figure>
<h4 id="212-开始节点">2.1.2、开始节点</h4>
<figure data-type="image" tabindex="15"><img src="https://hualiang.online/post-images/1671799530247.png" alt="67179953024" loading="lazy"></figure>
<h4 id="213-任务节点">2.1.3、任务节点</h4>
<figure data-type="image" tabindex="16"><img src="https://hualiang.online/post-images/1671799688089.png" alt="67179968808" loading="lazy"></figure>
<h4 id="214-结束节点">2.1.4、结束节点</h4>
<figure data-type="image" tabindex="17"><img src="https://hualiang.online/post-images/1671799739334.png" alt="67179973933" loading="lazy"></figure>
<h4 id="215-设置节点属性">2.1.5、设置节点属性</h4>
<p>指定标签名称：张三审批，节点任务负责人：zhangsan</p>
<figure data-type="image" tabindex="18"><img src="https://hualiang.online/post-images/1671799910878.png" alt="67179991087" loading="lazy"></figure>
<p>指定标签名称：李四审批，节点任务负责人：lisi</p>
<figure data-type="image" tabindex="19"><img src="https://hualiang.online/post-images/1672016531652.png" alt="67201653165" loading="lazy"></figure>
<h4 id="215-设置流程定义key">2.1.5、设置流程定义key</h4>
<figure data-type="image" tabindex="20"><img src="https://hualiang.online/post-images/1672016563704.png" alt="67201656370" loading="lazy"></figure>
<h4 id="216-保存流程定义模型">2.1.6、保存流程定义模型</h4>
<figure data-type="image" tabindex="21"><img src="https://hualiang.online/post-images/1671800206531.png" alt="67180020653" loading="lazy"></figure>
<h4 id="217-下载流程定义文件">2.1.7、下载流程定义文件</h4>
<figure data-type="image" tabindex="22"><img src="https://hualiang.online/post-images/1672016612079.png" alt="67201661207" loading="lazy"></figure>
<p>下载文件为：qingjia.bpmn20.xml</p>
<pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;
&lt;definitions xmlns=&quot;http://www.omg.org/spec/BPMN/20100524/MODEL&quot; xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot; xmlns:xsd=&quot;http://www.w3.org/2001/XMLSchema&quot; xmlns:activiti=&quot;http://activiti.org/bpmn&quot; xmlns:bpmndi=&quot;http://www.omg.org/spec/BPMN/20100524/DI&quot; xmlns:omgdc=&quot;http://www.omg.org/spec/DD/20100524/DC&quot; xmlns:omgdi=&quot;http://www.omg.org/spec/DD/20100524/DI&quot; typeLanguage=&quot;http://www.w3.org/2001/XMLSchema&quot; expressionLanguage=&quot;http://www.w3.org/1999/XPath&quot; targetNamespace=&quot;http://www.activiti.org/processdef&quot;&gt;
  &lt;process id=&quot;qingjia&quot; isExecutable=&quot;true&quot;&gt;
    &lt;startEvent id=&quot;sid-14A3E2A6-84E4-49E0-BF92-3DABD741430B&quot;&gt;&lt;/startEvent&gt;
    &lt;userTask id=&quot;sid-38632C81-C407-4F0D-944D-FC30F90637A3&quot; name=&quot;张三审批&quot; activiti:assignee=&quot;zhangsan&quot;&gt;&lt;/userTask&gt;
    &lt;sequenceFlow id=&quot;sid-081A176E-6756-4C4C-B36C-2649B12CFC5D&quot; sourceRef=&quot;sid-14A3E2A6-84E4-49E0-BF92-3DABD741430B&quot; targetRef=&quot;sid-38632C81-C407-4F0D-944D-FC30F90637A3&quot;&gt;&lt;/sequenceFlow&gt;
    &lt;userTask id=&quot;sid-655780D5-8492-494F-9E30-2CFD6691E98D&quot; name=&quot;李四审批&quot; activiti:assignee=&quot;lisi&quot;&gt;&lt;/userTask&gt;
    &lt;sequenceFlow id=&quot;sid-7DCE821D-4AE0-4F27-9811-80B575E7A758&quot; sourceRef=&quot;sid-38632C81-C407-4F0D-944D-FC30F90637A3&quot; targetRef=&quot;sid-655780D5-8492-494F-9E30-2CFD6691E98D&quot;&gt;&lt;/sequenceFlow&gt;
    &lt;endEvent id=&quot;sid-7EE28419-BC61-49AC-8990-C63C4D2F7C0D&quot;&gt;&lt;/endEvent&gt;
    &lt;sequenceFlow id=&quot;sid-2E583A5C-265A-4C05-B5E1-7F5DB98291F1&quot; sourceRef=&quot;sid-655780D5-8492-494F-9E30-2CFD6691E98D&quot; targetRef=&quot;sid-7EE28419-BC61-49AC-8990-C63C4D2F7C0D&quot;&gt;&lt;/sequenceFlow&gt;
  &lt;/process&gt;
  &lt;bpmndi:BPMNDiagram id=&quot;BPMNDiagram_qingjia&quot;&gt;
    &lt;bpmndi:BPMNPlane bpmnElement=&quot;qingjia&quot; id=&quot;BPMNPlane_qingjia&quot;&gt;
      &lt;bpmndi:BPMNShape bpmnElement=&quot;sid-14A3E2A6-84E4-49E0-BF92-3DABD741430B&quot; id=&quot;BPMNShape_sid-14A3E2A6-84E4-49E0-BF92-3DABD741430B&quot;&gt;
        &lt;omgdc:Bounds height=&quot;30.0&quot; width=&quot;30.0&quot; x=&quot;93.5&quot; y=&quot;75.0&quot;&gt;&lt;/omgdc:Bounds&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNShape bpmnElement=&quot;sid-38632C81-C407-4F0D-944D-FC30F90637A3&quot; id=&quot;BPMNShape_sid-38632C81-C407-4F0D-944D-FC30F90637A3&quot;&gt;
        &lt;omgdc:Bounds height=&quot;80.0&quot; width=&quot;100.0&quot; x=&quot;168.5&quot; y=&quot;50.0&quot;&gt;&lt;/omgdc:Bounds&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNShape bpmnElement=&quot;sid-655780D5-8492-494F-9E30-2CFD6691E98D&quot; id=&quot;BPMNShape_sid-655780D5-8492-494F-9E30-2CFD6691E98D&quot;&gt;
        &lt;omgdc:Bounds height=&quot;80.0&quot; width=&quot;100.0&quot; x=&quot;313.5&quot; y=&quot;50.0&quot;&gt;&lt;/omgdc:Bounds&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNShape bpmnElement=&quot;sid-7EE28419-BC61-49AC-8990-C63C4D2F7C0D&quot; id=&quot;BPMNShape_sid-7EE28419-BC61-49AC-8990-C63C4D2F7C0D&quot;&gt;
        &lt;omgdc:Bounds height=&quot;28.0&quot; width=&quot;28.0&quot; x=&quot;458.5&quot; y=&quot;76.0&quot;&gt;&lt;/omgdc:Bounds&gt;
      &lt;/bpmndi:BPMNShape&gt;
      &lt;bpmndi:BPMNEdge bpmnElement=&quot;sid-7DCE821D-4AE0-4F27-9811-80B575E7A758&quot; id=&quot;BPMNEdge_sid-7DCE821D-4AE0-4F27-9811-80B575E7A758&quot;&gt;
        &lt;omgdi:waypoint x=&quot;268.5&quot; y=&quot;90.0&quot;&gt;&lt;/omgdi:waypoint&gt;
        &lt;omgdi:waypoint x=&quot;313.5&quot; y=&quot;90.0&quot;&gt;&lt;/omgdi:waypoint&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNEdge bpmnElement=&quot;sid-081A176E-6756-4C4C-B36C-2649B12CFC5D&quot; id=&quot;BPMNEdge_sid-081A176E-6756-4C4C-B36C-2649B12CFC5D&quot;&gt;
        &lt;omgdi:waypoint x=&quot;123.5&quot; y=&quot;90.0&quot;&gt;&lt;/omgdi:waypoint&gt;
        &lt;omgdi:waypoint x=&quot;168.5&quot; y=&quot;90.0&quot;&gt;&lt;/omgdi:waypoint&gt;
      &lt;/bpmndi:BPMNEdge&gt;
      &lt;bpmndi:BPMNEdge bpmnElement=&quot;sid-2E583A5C-265A-4C05-B5E1-7F5DB98291F1&quot; id=&quot;BPMNEdge_sid-2E583A5C-265A-4C05-B5E1-7F5DB98291F1&quot;&gt;
        &lt;omgdi:waypoint x=&quot;413.5&quot; y=&quot;90.0&quot;&gt;&lt;/omgdi:waypoint&gt;
        &lt;omgdi:waypoint x=&quot;458.5&quot; y=&quot;90.0&quot;&gt;&lt;/omgdi:waypoint&gt;
      &lt;/bpmndi:BPMNEdge&gt;
    &lt;/bpmndi:BPMNPlane&gt;
  &lt;/bpmndi:BPMNDiagram&gt;
&lt;/definitions&gt;
</code></pre>
<h4 id="218-下载流程定义图片">2.1.8、下载流程定义图片</h4>
<p>单击右键上图图片，图片另存为：qingjia.png</p>
<figure data-type="image" tabindex="23"><img src="https://hualiang.online/post-images/1672016670456.png" alt="67201667045" loading="lazy"></figure>
<h4 id="219-将资源文件放入项目">2.1.9、将资源文件放入项目</h4>
<p>在service-oa模块resources下新建process资源文件夹</p>
<p>将qingjia.bpmn20.xml与qingjia.png放入process目录</p>
<h3 id="22-流程定义部署">2.2、流程定义部署</h3>
<p>将上面在设计器中定义的流程部署到activiti数据库中，就是流程定义部署。通过调用activiti的api将流程定义的bpmn和png两个文件一个一个添加部署到activiti中，也可以将两个文件打成zip包进行部署。</p>
<h4 id="221-单个文件部署方式">2.2.1、单个文件部署方式</h4>
<pre><code class="language-java">package com.atguigu;

import org.activiti.engine.RepositoryService;
import org.activiti.engine.repository.Deployment;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;


@RunWith(SpringRunner.class)
@SpringBootTest
public class ProcessTest {

    @Autowired
    private RepositoryService repositoryService;

    @Test
    public void deployProcess() {
        // 流程部署
        Deployment deploy = repositoryService.createDeployment()
                .addClasspathResource(&quot;process/qingjia.bpmn20.xml&quot;)
                .addClasspathResource(&quot;process/qingjia.png&quot;)
                .name(&quot;请假申请流程&quot;)
                .deploy();
        System.out.println(deploy.getId());
        System.out.println(deploy.getName());
    }
}
</code></pre>
<h4 id="222-压缩包部署方式">2.2.2、压缩包部署方式</h4>
<pre><code class="language-java">@Test
public void deployProcessByZip() {
    // 定义zip输入流
    InputStream inputStream = this
            .getClass()
            .getClassLoader()
            .getResourceAsStream(
                    &quot;process/qingjia.zip&quot;);
    ZipInputStream zipInputStream = new ZipInputStream(inputStream);

    // 流程部署
    Deployment deployment = repositoryService.createDeployment()
            .addZipInputStream(zipInputStream)
            .name(&quot;请假申请流程&quot;)
            .deploy();
    System.out.println(&quot;流程部署id：&quot; + deployment.getId());
    System.out.println(&quot;流程部署名称：&quot; + deployment.getName());
}
</code></pre>
<h4 id="223-操作数据库表">2.2.3、操作数据库表</h4>
<p>流程定义部署后操作activiti的3张表如下：</p>
<p>act_re_deployment     流程定义部署表，每部署一次增加一条记录</p>
<p>act_re_procdef            流程定义表，部署每个新的流程定义都会在这张表中增加一条记录</p>
<p>act_ge_bytearray        流程资源表</p>
<h3 id="23-启动流程实例">2.3、启动流程实例</h3>
<p>流程定义:将bpmn文件放到activiti的三张表中,好比是java中的一个类<br>
流程实例:好比是java中的一个实例对象(一个流程定义可以对应多个流程实例)，张三可以启动一个请假流程实例，李四也可以启动一个请假流程实例，他们互不影响</p>
<pre><code class="language-java">@Autowired
private RuntimeService runtimeService;

@Test
public void startUpProcess() {
    //创建流程实例,我们需要知道流程定义的key
    ProcessInstance processInstance = runtimeService.startProcessInstanceByKey(&quot;qingjia&quot;);
    //输出实例的相关信息
    System.out.println(&quot;流程定义id：&quot; + processInstance.getProcessDefinitionId());
    System.out.println(&quot;流程实例id：&quot; + processInstance.getId());
    System.out.println(&quot;当前活动Id：&quot; + processInstance.getActivityId());
}
</code></pre>
<p><strong>操作数据表</strong></p>
<p>act_hi_actinst     流程实例执行历史</p>
<p>act_hi_identitylink  流程的参与用户历史信息</p>
<p>act_hi_procinst      流程实例历史信息</p>
<p>act_hi_taskinst       流程任务历史信息</p>
<p>act_ru_execution   流程执行信息</p>
<p>act_ru_identitylink  流程的参与用户信息</p>
<p>act_ru_task              任务信息</p>
<h3 id="24-查询任务">2.4、查询任务</h3>
<p>每个节点都配置了Assignee，流程启动后，任务的负责人就可以查询自己当前需要处理的任务，查询出来的任务都是该用户的待办任务。</p>
<pre><code class="language-java">@Autowired
private TaskService taskService;

/**
 * 查询当前个人待执行的任务
 */
@Test
public void findPendingTaskList() {
    //任务负责人
    String assignee = &quot;zhangsan&quot;;
    List&lt;Task&gt; list = taskService.createTaskQuery()
            .taskAssignee(assignee)//只查询该任务负责人的任务
            .list();
    for (Task task : list) {
        System.out.println(&quot;流程实例id：&quot; + task.getProcessInstanceId());
        System.out.println(&quot;任务id：&quot; + task.getId());
        System.out.println(&quot;任务负责人：&quot; + task.getAssignee());
        System.out.println(&quot;任务名称：&quot; + task.getName());
    }
}
</code></pre>
<p>说明：</p>
<p>​	流程实例id:一个流程只有一个,标识这个流程</p>
<p>​	任务id:流程每进行到某个节点,就会给这个节点分配一个任务id</p>
<p>输出结果如下：</p>
<p>​	流程实例id：d969f534-825e-11ed-95b4-7c57581a7819<br>
​	任务id：d96c3f28-825e-11ed-95b4-7c57581a7819<br>
​	任务负责人：zhangsan<br>
​	任务名称：张三审批</p>
<h3 id="25-处理当前任务">2.5、处理当前任务</h3>
<p>任务负责人查询待办任务，选择任务进行处理，完成任务。</p>
<pre><code class="language-java">/**
 * 完成任务
 */
@Test
public void completTask(){
    Task task = taskService.createTaskQuery()
            .taskAssignee(&quot;zhangsan&quot;)  //要查询的负责人
            .singleResult();//返回一条

    //完成任务,参数：任务id
    taskService.complete(task.getId());
}
</code></pre>
<p>完成任务后，任务自动到下一个节点</p>
<h3 id="26-查询已处理任务">2.6、查询已处理任务</h3>
<pre><code class="language-java">@Autowired
private HistoryService historyService;

/**
 * 查询已处理历史任务
 */
@Test
public void findProcessedTaskList() {
    //张三已处理过的历史任务
    List&lt;HistoricTaskInstance&gt; list = historyService.createHistoricTaskInstanceQuery().taskAssignee(&quot;zhangsan&quot;).finished().list();
    for (HistoricTaskInstance historicTaskInstance : list) {
        System.out.println(&quot;流程实例id：&quot; + historicTaskInstance.getProcessInstanceId());
        System.out.println(&quot;任务id：&quot; + historicTaskInstance.getId());
        System.out.println(&quot;任务负责人：&quot; + historicTaskInstance.getAssignee());
        System.out.println(&quot;任务名称：&quot; + historicTaskInstance.getName());
    }
}
</code></pre>
<h3 id="27-其他接口了解">2.7、其他接口（了解）</h3>
<pre><code class="language-java">/**
 * 查询流程定义
 */
@Test
public void findProcessDefinitionList(){
    List&lt;ProcessDefinition&gt; definitionList = repositoryService.createProcessDefinitionQuery()
            .orderByProcessDefinitionVersion()
            .desc()
            .list();
    //输出流程定义信息
    for (ProcessDefinition processDefinition : definitionList) {
        System.out.println(&quot;流程定义 id=&quot;+processDefinition.getId());
        System.out.println(&quot;流程定义 name=&quot;+processDefinition.getName());
        System.out.println(&quot;流程定义 key=&quot;+processDefinition.getKey());
        System.out.println(&quot;流程定义 Version=&quot;+processDefinition.getVersion());
        System.out.println(&quot;流程部署ID =&quot;+processDefinition.getDeploymentId());
    }
}

/**
 * 删除流程定义
 */
public void deleteDeployment() {
    //部署id
    String deploymentId = &quot;82e3bc6b-81da-11ed-8e03-7c57581a7819&quot;;
    //删除流程定义，如果该流程定义已有流程实例启动则删除时出错
    repositoryService.deleteDeployment(deploymentId);
    //设置true 级联删除流程定义，即使该流程有流程实例启动也可以删除，设置为false非级别删除方式
    //repositoryService.deleteDeployment(deploymentId, true);
}
</code></pre>
<h2 id="3-流程实例">3、流程实例</h2>
<h3 id="31-什么是流程实例">3.1、什么是流程实例</h3>
<p>流程定义ProcessDefinition和流程实例ProcessInstance是Activiti重要的概念，类似于Java类和Java实例的关系</p>
<p>启动一个流程实例表示开始一次业务流程的运行，比如员工请假流程部署完成，如果张三要请假就可以启动一个流程实例，如果李四要请假也启动一个流程实例，两个流程的执行互相不影响，就好比定义一个 java 类，实例化两个对象一样，部署的流程就好比 java 类，启动一个流程实例就好比 new 一个 java 对象</p>
<h3 id="32-让实际业务与activiti表关联businesskey">3.2、让实际业务与activiti表关联(BusinessKey)</h3>
<p>比如我们填写一个请假单，一定会有一个请假单的唯一标识，我们通常使用这个标识来关联activiti，这个标识在activiti中称为businesskey</p>
<p>BusinessKey：业务标识，通常为业务的主键，业务标识和流程标识一一对应，业务标识来源于业务系统，存储业务标识就是根据业务标识来关联查询业务系统的数据</p>
<p>举例：请假流程启动一个流程实例，就可以将请假单的id作为业务标识存储到activiti中，将来查询activiti的流程实例信息就可以获取请假单的id从而关联查询业务系统数据库得到请假单信息</p>
<pre><code class="language-java">/**
 * 启动流程实例，添加businessKey
 */
@Test
public void startUpProcessAddBusinessKey(){
    String businessKey = &quot;1&quot;;
    // 启动流程实例，指定业务标识businessKey，也就是请假申请单id
    ProcessInstance processInstance = runtimeService.
            startProcessInstanceByKey(&quot;qingjia&quot;,businessKey);
    // 输出
    System.out.println(&quot;业务id:&quot;+processInstance.getBusinessKey());
}
</code></pre>
<h3 id="33-挂起激活流程实例">3.3、挂起，激活流程实例</h3>
<p>某些情况可能由于流程变更需要将当前运行的流程暂停而不是直接删除，流程暂停后将不会执行；</p>
<h4 id="331-全部流程实例挂起">3.3.1、全部流程实例挂起</h4>
<p>操作流程定义为挂起状态，该流程定义下面的所有流程实例全部暂停:<br>
流程定义为挂起状态，该流程定义将不允许启动新的流程实例，同时该流程定义下所有的流程实例将全部挂起暂停执行</p>
<pre><code class="language-java">@Test
public void suspendProcessInstance() {
    ProcessDefinition qingjia = repositoryService.createProcessDefinitionQuery().processDefinitionKey(&quot;qingjia&quot;).singleResult();
    // 获取到当前流程定义是否为暂停状态 suspended方法为true是暂停的，suspended方法为false是运行的
    boolean suspended = qingjia.isSuspended();
    if (suspended) {
        // 暂定,那就可以激活
        // 参数1:流程定义的id  参数2:是否激活    参数3:时间点
        repositoryService.activateProcessDefinitionById(qingjia.getId(), true, null);
        System.out.println(&quot;流程定义:&quot; + qingjia.getId() + &quot;激活&quot;);
    } else {
        repositoryService.suspendProcessDefinitionById(qingjia.getId(), true, null);
        System.out.println(&quot;流程定义:&quot; + qingjia.getId() + &quot;挂起&quot;);
    }
}
</code></pre>
<h4 id="332-单个流程实例挂起">3.3.2、单个流程实例挂起</h4>
<p>操作流程实例对象，针对单个流程执行挂起操作，某个流程实例挂起则此流程不在继续执行，完成该流程实例的当前任务将报异常；</p>
<pre><code class="language-java">@Test
public void SingleSuspendProcessInstance() {
    String processInstanceId = &quot;8bdff984-ab53-11ed-9b17-f8e43b734677&quot;;
    ProcessInstance processInstance = runtimeService.createProcessInstanceQuery().processInstanceId(processInstanceId).singleResult();
    //获取到当前流程定义是否为暂停状态   suspended方法为true代表为暂停   false就是运行的
    boolean suspended = processInstance.isSuspended();
    if (suspended) {
        runtimeService.activateProcessInstanceById(processInstanceId);
        System.out.println(&quot;流程实例:&quot; + processInstanceId + &quot;激活&quot;);
    } else {
        runtimeService.suspendProcessInstanceById(processInstanceId);
        System.out.println(&quot;流程实例:&quot; + processInstanceId + &quot;挂起&quot;);
    }
}
</code></pre>
<h2 id="4-任务分配">4、任务分配</h2>
<p>任务分配有三种方式</p>
<ol>
<li>固定分配</li>
<li>UEL表达式分配</li>
<li>监听器分配</li>
</ol>
<h3 id="41-固定分配">4.1、固定分配</h3>
<p>在前面进行业务流程建模时指定固定的任务负责人，如：Assignee：zhangsan/lisi</p>
<h3 id="42-表达式分配">4.2、表达式分配</h3>
<p>activiti使用UEL表达式，UEL是java EE6规范的一部分,UEL即统一表达式语言，activiti支持两个UEL表达式：UEL-value和UEL-method。</p>
<h4 id="421-uel-value">4.2.1、UEL-value</h4>
<p>新建：一个加班流程</p>
<p>如图：</p>
<figure data-type="image" tabindex="24"><img src="https://hualiang.online/post-images/1671800917826.png" alt="67180091782" loading="lazy"></figure>
<p>assignee1 这个变量是 activiti 的一个流程变量</p>
<p>我们启动流程实例，这个启动实例的方法跟之前的方法基本一致,唯一的不同是在启动时,添加了一个参数</p>
<pre><code class="language-java">@Test
public void deployProcess01() {
    // 流程部署
    Deployment deploy = repositoryService.createDeployment()
        .addClasspathResource(&quot;process/jiaban01.bpmn20.xml&quot;)
        .name(&quot;加班申请流程&quot;)
        .deploy();
    System.out.println(deploy.getId());
    System.out.println(deploy.getName());
}

/**
 * 启动流程实例
 */
@Test
public void startUpProcess01() {
    Map&lt;String, Object&gt; variables = new HashMap&lt;&gt;();
    variables.put(&quot;assignee1&quot;,&quot;zhangsan&quot;);
    variables.put(&quot;assignee2&quot;,&quot;lisi&quot;);
    //创建流程实例,我们需要知道流程定义的key
    ProcessInstance processInstance = runtimeService.startProcessInstanceByKey(&quot;qingjia01&quot;, variables);
    //输出实例的相关信息
    System.out.println(&quot;流程定义id：&quot; + processInstance.getProcessDefinitionId());
    System.out.println(&quot;流程实例id：&quot; + processInstance.getId());
}
</code></pre>
<h4 id="422-uel-method-方式">4.2.2、UEL-method 方式</h4>
<p>如图：</p>
<figure data-type="image" tabindex="25"><img src="https://hualiang.online/post-images/1671801255280.png" alt="67180125528" loading="lazy"></figure>
<p>userBean 是 spring 容器中的一个 bean，表示调用该 bean 的 getUsername(int id)方法。</p>
<p>经理审批：${userBean.getUsername(1)}</p>
<p>人事审批：${userBean.getUsername(2)}</p>
<pre><code class="language-java">package com.atguigu.process.bean;

import org.springframework.stereotype.Component;

@Component
public class UserBean {

    public String getUsername(int id) {
        if(id == 1) {
            return &quot;zhangsan&quot;;
        }
        if(id == 2) {
            return &quot;lisi&quot;;
        }
        return &quot;admin&quot;;
    }
}
</code></pre>
<p>部署与启动</p>
<pre><code class="language-java">@Test
public void deployProcess02() {
    // 流程部署
    Deployment deploy = repositoryService.createDeployment()
        .addClasspathResource(&quot;process/jiaban02.bpmn20.xml&quot;)
        .name(&quot;加班申请流程&quot;)
        .deploy();
    System.out.println(deploy.getId());
    System.out.println(deploy.getName());
}

/**
 * 启动流程实例
 */
@Test
public void startUpProcess02() {
    //创建流程实例,我们需要知道流程定义的key
    ProcessInstance processInstance = runtimeService.startProcessInstanceByKey(&quot;qingjia02&quot;);
    //输出实例的相关信息
    System.out.println(&quot;流程定义id：&quot; + processInstance.getProcessDefinitionId());
    System.out.println(&quot;流程实例id：&quot; + processInstance.getId());
}
</code></pre>
<p>启动流程实例，就会调用bean方法，参数为：1，经理审批后，接着调用bean方法，参数为：2</p>
<h3 id="43-监听器分配">4.3、监听器分配</h3>
<p>使用监听器的方式来指定负责人，那么在流程设计时就不需要指定assignee。</p>
<p>任务监听器是发生对应的任务相关事件时执行自定义 java 逻辑 或表达式</p>
<p>Event的选项包含：</p>
<pre><code>Create：任务创建后触发
Assignment：任务分配后触发
Delete：任务完成后触发
All：所有事件发生都触发
</code></pre>
<p>定义任务监听类，且类必须实现 org.activiti.engine.delegate.TaskListener 接口</p>
<pre><code class="language-java">package com.atguigu.process.bean;

import org.activiti.engine.delegate.DelegateTask;
import org.activiti.engine.delegate.TaskListener;

public class MyTaskListener implements TaskListener {

    @Override
    public void notify(DelegateTask delegateTask) {
        if(delegateTask.getName().equals(&quot;经理审批&quot;)){
            //这里指定任务负责人
            delegateTask.setAssignee(&quot;zhangsan&quot;);
        } else if(delegateTask.getName().equals(&quot;人事审批&quot;)){
            //这里指定任务负责人
            delegateTask.setAssignee(&quot;lisi&quot;);
        }
    }
}
</code></pre>
<p>配置监听器</p>
<figure data-type="image" tabindex="26"><img src="https://hualiang.online/post-images/1671802034480.png" alt="67180203448" loading="lazy"></figure>
<figure data-type="image" tabindex="27"><img src="https://hualiang.online/post-images/1671801889180.png" alt="67180188918" loading="lazy"></figure>
<p>经理审批与人事审批，都设置一样的监听即可</p>
<p>部署与测试</p>
<pre><code class="language-java">@Test
public void deployProcess03() {
    // 流程部署
    Deployment deploy = repositoryService.createDeployment()
            .addClasspathResource(&quot;process/jiaban03.bpmn20.xml&quot;)
            .name(&quot;加班申请流程&quot;)
            .deploy();
    System.out.println(deploy.getId());
    System.out.println(deploy.getName());
}

/**
 * 启动流程实例
 */
@Test
public void startUpProcess03() {
    //创建流程实例,我们需要知道流程定义的key
    ProcessInstance processInstance = runtimeService.startProcessInstanceByKey(&quot;jiaban03&quot;);
    //输出实例的相关信息
    System.out.println(&quot;流程定义id：&quot; + processInstance.getProcessDefinitionId());
    System.out.println(&quot;流程实例id：&quot; + processInstance.getId());
}
</code></pre>
<p>启动流程实例，就会调用MyTaskListener监听方法</p>
<h2 id="5-流程变量">5、流程变量</h2>
<h3 id="51-什么是流程变量">5.1、什么是流程变量</h3>
<p>流程变量在 activiti 中是一个非常重要的角色，流程运转有时需要靠流程变量，业务系统和 activiti<br>
结合时少不了流程变量，流程变量就是 activiti 在管理工作流时根据管理需要而设置的变量。<br>
比如：在请假申请流程流转时如果请假天数大于2 天则由总经理审核，否则由部门经理直接审核， 请假天<br>
数就可以设置为流程变量，在流程流转时使用。</p>
<h3 id="52-流程变量的作用域">5.2、流程变量的作用域</h3>
<p>流程变量的作用可以是一个流程实例，但也可以是一个任务(task)或是一个执行实例</p>
<h4 id="521-globa变量">5.2.1、globa变量</h4>
<p>流程变量的默认作用域是流程实例。当一个流程变量的作用域为流程实例时，可以称为 global 变量</p>
<p>global 变量中变量名不允许重复，设置相同名称的变量，后设置的值会覆盖前设置的变量值。</p>
<h4 id="522-local变量">5.2.2、local变量</h4>
<p>任务和执行实例仅仅是针对一个任务和一个执行实例范围，范围没有流程实例大， 称为 local 变量。</p>
<p>Local 变量由于在不同的任务或不同的执行实例中，作用域互不影响，变量名可以相同没有影响。Local 变量名也可以和 global 变量名相同，没有影响。</p>
<h3 id="53-流程变量的使用方法">5.3、流程变量的使用方法</h3>
<p>通过UEL表达式使用流程变量</p>
<p>1、之前我们也使用过UEL表达式来设置任务处理人，例如 <code>${assignee1}</code>，activiti获取UEL表达式的值，即流程变量assignee1的值，将值作为任务的负责人进行任务分配<br>
2、我们也可以在任务和任务间的连线上使用UEL表达式，决定流程走向比如 <code>${day &gt; 2}</code> 和 <code>${day &lt;= 2}</code>，day就是一个流程变量名称，UEL表达式的执行结果是布尔类型</p>
<h3 id="54-设置globa变量">5.4、设置globa变量</h3>
<h4 id="541-启动流程时设置变量">5.4.1、启动流程时设置变量</h4>
<p>在启动流程时设置流程变量，<strong>变量的作用域是整个流程实例。</strong><br>
通过 Map&lt;key,value&gt; 设置流程变量，map 中可以设置多个变量，这个 key 就是流程变量的名字</p>
<p>这是前面的实例代码</p>
<pre><code class="language-java">@Test
public void startUpProcess() {
    Map&lt;String, Object&gt; variables = new HashMap&lt;&gt;();
    variables.put(&quot;assignee1&quot;, &quot;zhangsan&quot;);
    variables.put(&quot;assignee2&quot;, &quot;lisi&quot;);
    //创建流程实例,我们需要知道流程定义的key
    ProcessInstance processInstance = runtimeService.startProcessInstanceByKey(&quot;qingjia&quot;, variables);
    //输出实例的相关信息
    System.out.println(&quot;流程定义id：&quot; + processInstance.getProcessDefinitionId());
    System.out.println(&quot;流程实例id：&quot; + processInstance.getId());
}
</code></pre>
<h4 id="542-在任务办理时设置流程变量">5.4.2、在任务办理时设置流程变量</h4>
<p><strong>它的作用域是整个流程实例</strong> ，如果设置的流程变量的   key   在流程实例中已存在相同的名字则后设置的变量替换前边设置的变量。</p>
<p>模拟实例代码：</p>
<pre><code class="language-java">@Test
public void completTask() {
    Task task = taskService.createTaskQuery()
            .taskAssignee(&quot;zhangsan&quot;)  //要查询的负责人
            .singleResult();//返回一条

    Map&lt;String, Object&gt; variables = new HashMap&lt;&gt;();
    variables.put(&quot;assignee2&quot;, &quot;zhao&quot;);
    //完成任务,参数：任务id
    taskService.complete(task.getId(), variables);
}
</code></pre>
<h4 id="543-通过当前流程实例设置">5.4.3、通过当前流程实例设置</h4>
<p>通过流程实例 id   设置全局变量， <strong>它的作用域是整个流程实例</strong> ，该流程实例必须未执行完成。</p>
<p>模拟实例代码：</p>
<pre><code class="language-java">@Test
public void processInstanceIdSetVariables() {
    Map&lt;String, Object&gt; variables = new HashMap&lt;&gt;();
    variables.put(&quot;assignee2&quot;, &quot;wang&quot;);
    runtimeService.setVariables(&quot;1c347a90-82c6-11ed-96ca-7c57581a7819&quot;, variables);
}
</code></pre>
<h3 id="55-设置local变量">5.5、设置Local变量</h3>
<p>local 流程变量的作用域只在当前任务节点下可用</p>
<p>任务办理时设置local流程变量，当前运行的流程实例只能在该任务结束前使用，任务结束该变量无法在当前流程实例使用</p>
<p>模拟实例代码：</p>
<pre><code class="language-java">@Test
public void completLocalTask() {
    Task task = taskService.createTaskQuery()
            .taskAssignee(&quot;zhangsan&quot;)  //要查询的负责人
            .singleResult();//返回一条

    // 设置local变量，作用域为该任务
    taskService.setVariableLocal(task.getId(),&quot;assignee2&quot;,&quot;li&quot;);
    // 查看local变量
    System.out.println(taskService.getVariableLocal(task.getId(), &quot;assignee2&quot;));
    //完成任务,参数：任务id
    taskService.complete(task.getId());
}
</code></pre>
<h2 id="6-任务组">6、任务组</h2>
<h3 id="61-candidate-users候选人">6.1、Candidate-users候选人</h3>
<p><strong>1、需求</strong><br>
在流程定义中在任务结点的assignee固定设置任务负责人，在流程定义时将参与者固定设置在.bpmn文件中，如果要临时变更任务负责人则需要修改流程定义，系统扩展性很差，针对这种情况，我们可以给任务设置多个候选人，从候选人中选择参与者来完成任务</p>
<p><strong>2、设置任务候选人</strong></p>
<figure data-type="image" tabindex="28"><img src="https://hualiang.online/post-images/1672024290855.png" alt="67202429085" loading="lazy"></figure>
<figure data-type="image" tabindex="29"><img src="https://hualiang.online/post-images/1672024319632.png" alt="67202431963" loading="lazy"></figure>
<h3 id="62-组任务办理流程">6.2、组任务办理流程</h3>
<p><strong>第一步:查询组任务</strong></p>
<p>指定候选人，查询该候选人当前的待办任务<br>
候选人不能办理任务<br>
<strong>第二步:拾取(claim)任务</strong></p>
<p>该组任务的所有候选人都能拾取<br>
将候选人的组任务，变成个人任务，原来的候选人就变成了该任务的负责人<br>
如果拾取后不想办理该任务<br>
需要将已经拾取<br>
<strong>第三步:查询个人任务</strong></p>
<p>查询方式同个人任务部分，根据assignee查询用户负责的个人任务<br>
<strong>第四步:办理个人任务</strong></p>
<h3 id="63-关键代码">6.3、关键代码</h3>
<h4 id="631-部署及启动">6.3.1、部署及启动</h4>
<pre><code class="language-java">@Test
public void deployProcess04() {
    // 流程部署
    Deployment deploy = repositoryService.createDeployment()
            .addClasspathResource(&quot;process/jiaban04.bpmn20.xml&quot;)
            .name(&quot;请假申请流程&quot;)
            .deploy();
    System.out.println(deploy.getId());
    System.out.println(deploy.getName());

    ProcessInstance processInstance = runtimeService.startProcessInstanceByKey(&quot;jiaban04&quot;);
    System.out.println(processInstance.getId());
}
</code></pre>
<h4 id="632-查询组任务">6.3.2、查询组任务</h4>
<pre><code class="language-java">@Test
public void findGroupTaskList() {
    //查询组任务
    List&lt;Task&gt; list = taskService.createTaskQuery()
            .taskCandidateUser(&quot;zhangsan01&quot;)//根据候选人查询
            .list();
    for (Task task : list) {
        System.out.println(&quot;----------------------------&quot;);
        System.out.println(&quot;流程实例id：&quot; + task.getProcessInstanceId());
        System.out.println(&quot;任务id：&quot; + task.getId());
        System.out.println(&quot;任务负责人：&quot; + task.getAssignee());
        System.out.println(&quot;任务名称：&quot; + task.getName());
    }
}
</code></pre>
<h4 id="632-拾取组任务">6.3.2、拾取组任务</h4>
<pre><code class="language-java">@Test
public void claimTask(){
    //拾取任务,即使该用户不是候选人也能拾取(建议拾取时校验是否有资格)
    //校验该用户有没有拾取任务的资格
    Task task = taskService.createTaskQuery()
            .taskCandidateUser(&quot;zhangsan01&quot;)//根据候选人查询
            .singleResult();
    if(task!=null){
        //拾取任务
        taskService.claim(taskId, &quot;zhangsan01&quot;);
        System.out.println(&quot;任务拾取成功&quot;);
    }
}
</code></pre>
<p>张三01拾取任务了，张三02就不能拾取了</p>
<h4 id="633-查询个人待办任务">6.3.3、  查询个人待办任务</h4>
<p>查询方式同个人任务查询</p>
<pre><code class="language-java">@Test
public void findGroupPendingTaskList() {
    //任务负责人
    String assignee = &quot;zhangsan01&quot;;
    List&lt;Task&gt; list = taskService.createTaskQuery()
            .taskAssignee(assignee)//只查询该任务负责人的任务
            .list();
    for (Task task : list) {
        System.out.println(&quot;流程实例id：&quot; + task.getProcessInstanceId());
        System.out.println(&quot;任务id：&quot; + task.getId());
        System.out.println(&quot;任务负责人：&quot; + task.getAssignee());
        System.out.println(&quot;任务名称：&quot; + task.getName());
    }
}
</code></pre>
<h4 id="634-办理个人任务">6.3.4、  办理个人任务</h4>
<p>同个人任务办理</p>
<pre><code class="language-java">@Test
public void completGroupTask() {
    Task task = taskService.createTaskQuery()
            .taskAssignee(&quot;zhangsan01&quot;)  //要查询的负责人
            .singleResult();//返回一条
    taskService.complete(task.getId());
}
</code></pre>
<h4 id="635-归还组任务">6.3.5、归还组任务</h4>
<p>如果个人不想办理该组任务，可以归还组任务，归还后该用户不再是该任务的负责人</p>
<pre><code class="language-java">@Test
public void assigneeToGroupTask() {
    String taskId = &quot;d96c3f28-825e-11ed-95b4-7c57581a7819&quot;;
    // 任务负责人
    String userId = &quot;zhangsan01&quot;;
    // 校验userId是否是taskId的负责人，如果是负责人才可以归还组任务
    Task task = taskService
            .createTaskQuery()
            .taskId(taskId)
            .taskAssignee(userId)
            .singleResult();
    if (task != null) {
        // 如果设置为null，归还组任务,该 任务没有负责人
        taskService.setAssignee(taskId, null);
    }
}
</code></pre>
<h4 id="636-任务交接">6.3.6、  任务交接</h4>
<p>任务交接，任务负责人将任务交给其它候选人办理该任务</p>
<pre><code class="language-java">@Test
public void assigneeToCandidateUser() {
    // 当前待办任务
    String taskId = &quot;d96c3f28-825e-11ed-95b4-7c57581a7819&quot;;
    // 校验zhangsan01是否是taskId的负责人，如果是负责人才可以归还组任务
    Task task = taskService
            .createTaskQuery()
            .taskId(taskId)
            .taskAssignee(&quot;zhangsan01&quot;)
            .singleResult();
    if (task != null) {
        // 将此任务交给其它候选人zhangsan02办理该 任务
        taskService.setAssignee(taskId, &quot;zhangsan02&quot;);
    }
}
</code></pre>
<h2 id="7-网关">7、网关</h2>
<p>网关用来控制流程的流向，通常会和流程变量一起使用。</p>
<h3 id="71-排他网关">7.1、排他网关</h3>
<ul>
<li>排他网关：只有一条路径会被选择</li>
</ul>
<p>当你的流程出现这样的场景：请假申请，两天以内，部门经理审批流程就结束了，两天以上需要总经理直接审批，这个时候就需要排他网关</p>
<figure data-type="image" tabindex="30"><img src="https://hualiang.online/post-images/1672021496641.png" alt="67202149664" loading="lazy"></figure>
<h3 id="72-并行网关">7.2、并行网关</h3>
<ul>
<li>并（平）行网关：所有路径会被同时选择</li>
</ul>
<p>当出现这样的场景：请假申请开始，需要部门经理和总经理都审批，两者没有前后需要两个人全部审批才能进入下个节点人事审批。这个时候就需要并行网关</p>
<figure data-type="image" tabindex="31"><img src="https://hualiang.online/post-images/image-20230213201822157.png" alt="image-20230213201822157" loading="lazy"></figure>
<p><strong>与排他网关的主要区别是，并行网关不会解析条件。</strong> <strong>即使顺序流中定义了条件，也会被忽略。</strong></p>
<h3 id="73-包含网关">7.3、包含网关</h3>
<p>包容网关：可以同时执行多条线路，也可以在网关上设置条件，可以看做是排他网关和并行网关的结合体。<br>
当出现这样的场景：请假申请大于等于2天需要由部门总经理审批，小于2天由部门经理审批，请假申请必须经过人事经理审批。这个时候就需要包含网关</p>
<figure data-type="image" tabindex="32"><img src="https://hualiang.online/post-images/1672025869146.png" alt="67202586914" loading="lazy"></figure>
]]></content>
    </entry>
</feed>